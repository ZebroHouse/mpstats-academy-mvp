# Тарифы 2.0 — снос COURSE + мультимесяц PLATFORM — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`).
> **⚠️ ДЕНЬГИ — ЭТОТ ПЛАН РЕАЛИЗУЕТСЯ ТОЛЬКО ПОСЛЕ ДИЗАЙН-РЕВЬЮ (owner-gate).** Секция «Design-review: решения и риски» ниже — читать ПЕРВОЙ. Ряд пунктов вскрыт грепом сверх спеки.

**Goal:** Снести мёртвый тариф COURSE (0 активных подписок) из пути покупки и витрины; добавить объёмные скидки PLATFORM за 3/6 мес (7990/13990) как отдельные `SubscriptionPlan`-строки по `intervalDays`; сохранить существующую биллинг-машину (полная сумма периода вперёд + рекуррент по цене периода) и акцию «2 месяца по цене одного» на 1-мес пути.

**Architecture:** Мультимесяц = отдельные PLATFORM-планы `intervalDays` 30/90/180 (Option A спеки). Матан суммы/периода уже читается живьём из `plan.intervalDays`/`plan.price` — новой биллинг-логики нет. **Реальная работа — не исполнение, а ВЫБОР плана:** резолв «единственного PLATFORM» (`findFirst({type:'PLATFORM'})`) ломается при 3 строках; контракт покупки меняется с `planType` на явный выбор периода (`intervalDays`), и все неявные «дай PLATFORM» пинуются к нужному плану. Плюс широкий copy-sweep COURSE/1990 и паритет `/billing`↔`/pricing` через общие компоненты `components/pricing/*`.

**Tech Stack:** Next.js 14, tRPC, Prisma (Supabase), CloudPayments (клиентский виджет + рекуррент), Zod, Tailwind. Миграция планов — аддитивный tsx через Supabase Mgmt API (строки-данные, схема НЕ меняется).

## Global Constraints

- **Миграция планов — ТОЛЬКО аддитивный tsx через Mgmt API** (localhost=прод). `prisma migrate`/`db push` — НИКОГДА. Схема НЕ меняется (новые строки, не колонки) → `prisma generate` не нужен. Токен `.secrets/supabase-mgmt-token.md`, ref `saecuecevicwjkpmaoot`.
- **`pnpm --filter web build` обязателен** перед сдачей (server-only-в-client на тариф-страницах ловится только билдом). Гоча-рекуррент: `node_modules/next` в этой среде периодически повреждается → `pnpm install --force`.
- **CP-рекуррент тестировать КАРТОЙ, не СБП** (СБП не токенизируется). Тест-план `99edef8c-…` (10₽/1д) + `/admin/billing-test`.
- **Смена цен активным подпискам:** CP держит `recurrent.amount` у себя на момент создания. 141 старая ACTIVE PLATFORM продолжат по 2990/30д — это правильно, НЕ трогаем. Новая сетка — только для новых подписок.
- **Историю/доступ COURSE НЕ ломаем:** `isActive=false` на плане (не удалять строку), аналитика выручки показывает прошлые COURSE-платежи, легаси-подписки доживают.
- «cancel anytime» и производные — **запрещены** вне FAQ. Тон — редполитика (`mpstats-copywriting`).
- **Файл-пути (исправлено vs спека):** `subscription-service.ts`, `decide-recurrent-update.ts`, `consume-discount.ts` живут в **`apps/web/src/lib/cloudpayments/`**, НЕ в `packages/api/src/services/billing/`.
- Детальные карты кода (точные line-refs, текущее поведение): `scratchpad/pricing-billing-map.md`, `pricing-ui-map.md`, `pricing-copy-sweep.md`.

---

## Design-review: решения и риски (читать ПЕРВЫМ — вскрыто грепом сверх спеки)

**Требуют решения owner/ревью до реализации:**

1. **Контракт выбора плана.** Сейчас `initiatePayment`/`getApplicableDiscount` берут `planType` и резолвят `findFirst({type, hidden:false, isActive:true})`. С 3 PLATFORM это неоднозначно. **Рекомендация:** менять контракт на явный `intervalDays: 30|90|180` (спека говорит «резолв по паре (type, intervalDays)»); резолв `findFirst({type:'PLATFORM', intervalDays, hidden:false, isActive:true})`. Альтернатива — `planId` (жёстче, но UI должен знать id). Рекомендую `intervalDays` — данные из `getPlans`, меньше связности. **Затрагивает:** `billing.ts:89-92,153-155`, `promo.ts:140`, оба UI `handlePayment`.

2. **Неявные «дай PLATFORM» пинуются к 30-дневному** (спека их НЕ упоминала — вскрыто картой): `trial-subscription.ts:31-34` и `activation.ts:56-59` (`apps/web/src/lib/cloudpayments/` / referral) резолвят `findFirst({type:'PLATFORM'})`. Триал и реф-пакет должны привязываться к **базовому 30-дневному** плану. **Рекомендация:** явный `intervalDays:30` в этих резолвах. **Риск если пропустить:** триал/реф могут сесть на 90/180-план произвольно.

3. **Оффер «2 месяца» перехватит длинные планы** (подтверждено): `resolveApplicableOffer` (`offer/resolve.ts:38-92`) гейтит только `planType==='PLATFORM'`. **Обязательно** добавить `intervalDays===30` — иначе оффер перепишет первый период 90/180-плана на 60 дней. (Спека 3.5 это предвидела.)

4. **`getOfferDuplicates`-монитор** (`admin-analytics.ts:57-66`) начнёт ложно флагать легитимный апгрейд 30д→90д как «дубль активной PLATFORM». **Рекомендация:** пересмотреть семантику (считать дублем только >1 ACTIVE PLATFORM одного `intervalDays`, или явно допускать разные периоды). **Решение owner:** чинить в этом треке или отдельным пунктом? (норма монитора = 0, ложные срабатывания зашумят).

5. **Легаси COURSE-подписчики на cutover** (2 места с веткой `plan.type==='COURSE'`): `profile/page.tsx:640` (отображение подписки) и `CourseAccessCTA.tsx:51` (доступ). Спека: «оставить рабочим, не показывать мёртвую цену/предложение купить». **Подтвердить:** легаси-COURSE-подписка показывается как активная (read-only), без CTA докупить и без цены 1990.

6. **`admin/promo` создаёт COURSE-промокоды** (операционка, не только копирайт): убрать `'COURSE'` из выбора типа + COURSE-ветку `createPromoCode` (`promo.ts:261-269`). Подтвердить, что owner не создаёт новых COURSE-кодов.

7. **Внешний лендинг (`go.mpstats.academy`)** — вне репо MAAL. Спека 3.8: если есть внешний тарифный блок с ценами курсов — обновить синхронно. **Координация owner'а, не молча.** Единственный оставшийся открытый вопрос спеки (§5.3).

**Мелкие находки карты (вписаны в задачи, не блокеры):**
- CP-хардкод рекуррента ровно в 2 местах: `pricing/page.tsx:315-321`, `billing/page.tsx:198`. Обёртка `lib/cloudpayments/widget.ts:41-58` уже generic — там 0 изменений.
- `DiscountedPrice.tsx:65` хардкодит «/мес» → нужен проп period-label для 3/6-мес.
- `pricing/layout.tsx` SEO-мета/title содержат «1990»/«Подписка на один курс» (спека не листала) — в sweep.
- `courses/page.tsx` — **LIVE-роут** (не прототип), своя COURSE-мини-витрина.
- `/pricing` и `/billing` — две **отдельные несвязанные** реализации COURSE-карточки (обе править).
- Схема: `SubscriptionPlan.type` без `@unique` → миграция не нужна.
- `seed-billing.ts` `seedPlan` хардкодит `intervalDays:30` + апсерт по `findFirst({type,hidden})` → рерайт под 3 строки (композитный ключ/фикс. id).

---

## File Structure (что создаём / трогаем)

**Данные:** `scripts/` новый tsx (2 плана + COURSE isActive=false), `scripts/seed/seed-billing.ts` (рерайт seedPlan).
**Backend (резолв плана + гейты):** `packages/api/src/routers/billing.ts`, `promo.ts`; `apps/web/src/lib/cloudpayments/{subscription-service,decide-recurrent-update,consume-discount,trial-subscription,activation}.ts`; `packages/api/src/services/offer/resolve.ts`; `admin-analytics.ts` (монитор).
**UI общие компоненты (новые):** `apps/web/src/components/pricing/{PlanPeriodCards,PricingFaq,WhatsIncluded,Reviews}.tsx` (вынести из pricing/page.tsx для паритета).
**UI страницы:** `apps/web/src/app/pricing/{page,layout}.tsx`, `apps/web/src/app/(main)/billing/page.tsx`, `apps/web/src/app/page.tsx` (homepage), `apps/web/src/app/courses/{page,ai,analytics,ads,ozon}/*`, `(main)/profile/page.tsx`, `(admin)/admin/promo/page.tsx`, `components/**/DiscountedPrice.tsx`, `CourseAccessCTA.tsx`.

**Порядок волн:** (1) данные+резолв-контракт бэкенда → (2) гейты (оффер/триал/реф/монитор/промо) → (3) общие UI-компоненты → (4) тариф-страницы на новых компонентах → (5) copy-sweep остального → (6) верификация (билд + тест-оплата картой 3/6-мес + sweep-греп=0).

---

### Task 1: Данные — 2 новых PLATFORM-плана + COURSE isActive=false (аддитивный tsx)

**Files:** Create `scripts/migrate-pricing-2.0-plans.ts`; Modify `scripts/seed/seed-billing.ts`.

- [ ] **Step 1: tsx-скрипт (Mgmt API).** Идемпотентно: INSERT 2 PLATFORM-строк (intervalDays 90/price 7990, 180/13990) если ещё нет (по паре type+intervalDays), UPDATE COURSE `isActive=false`. Печатает dry-run (текущие планы) до/после. Форма зеркалит `scripts/migrate-add-partner-entry.ts` (Mgmt API, sbp-токен из `.secrets/`). Точные поля строки `SubscriptionPlan` — из `pricing-billing-map.md` (name, type, price, intervalDays, hidden=false, isActive=true + любые NOT NULL).
- [ ] **Step 2: Запустить** `NODE_OPTIONS=--dns-result-order=ipv4first npx tsx scripts/migrate-pricing-2.0-plans.ts`. Проверить: 3 PLATFORM (30/90/180) active, COURSE isActive=false.
- [ ] **Step 3: Рерайт `seed-billing.ts`** — `seedPlan` больше не апсертит по `findFirst({type,hidden})` (коллизия на 3 PLATFORM). Апсерт по композиту (type+intervalDays) или фикс. id; добавить два PLATFORM seedPlan (90/7990, 180/13990); COURSE seed → isActive=false. Детали текущего seedPlan — `pricing-billing-map.md`.
- [ ] **Step 4:** typecheck. **Commit** `feat(billing): add 3/6-month PLATFORM plans, retire COURSE (isActive=false)`.

> Проверка после Task 1: `getPlans` вернёт 3 PLATFORM по intervalDays, COURSE исчезнет (isActive-фильтр). Историю не трогаем.

---

### Task 2: Резолв плана по периоду + снос COURSE-веток покупки (`billing.ts`, `promo.ts`)

**Files:** Modify `packages/api/src/routers/billing.ts`, `promo.ts`.

- [ ] **Step 1:** `initiatePayment` вход: заменить `planType` на `intervalDays: z.union([z.literal(30),z.literal(90),z.literal(180)])` (или сохранить `planType` + добавить `intervalDays`, PLATFORM-only); резолв `findFirst({type:'PLATFORM', intervalDays, hidden:false, isActive:true})` (billing.ts:153-155). То же в `getApplicableDiscount` (:89-92).
- [ ] **Step 2:** Удалить COURSE-ветки: валидация `courseId` (:137-148), COURSE в `receipt` (:182-193,:281-283), `getApplicableDiscount` enum (:76), `getCourses` дропдаун (:27-33 если только для COURSE-покупки). `firstAmount`/`recurrentAmount` — БЕЗ изменений (уже `plan.price`/`plan.intervalDays`); линчпин «рекуррент по полной цене плана» сохраняется дословно.
- [ ] **Step 3:** `promo.ts` — `createPromoCode` COURSE-ветка (:261-269) + резолв (:140) убрать/пин к PLATFORM.
- [ ] **Step 4:** typecheck + **build**. **Commit** `feat(billing): resolve PLATFORM plan by intervalDays; drop COURSE purchase path`.

---

### Task 3: Гейты и неявные PLATFORM-резолвы (оффер / триал / реф / монитор)

**Files:** Modify `apps/web/src/lib/cloudpayments/{trial-subscription,activation}.ts`, `packages/api/src/services/offer/resolve.ts`, `packages/api/src/routers/admin-analytics.ts`.

- [ ] **Step 1: Оффер — только 30-дневный.** `resolveApplicableOffer` (`offer/resolve.ts:46`) добавить условие `plan.intervalDays===30` (рядом с `planType==='PLATFORM'`). Точная сигнатура (передаётся ли plan/intervalDays в резолвер) — `pricing-billing-map.md`; если нет — прокинуть.
- [ ] **Step 2: Триал + реф → 30-дневный.** `trial-subscription.ts:31-34`, `activation.ts:56-59` — резолв `findFirst({type:'PLATFORM', intervalDays:30, ...})`.
- [ ] **Step 3: Монитор дублей** (`admin-analytics.ts:57-66`) — считать дублем только >1 ACTIVE PLATFORM **одного intervalDays** (или иная договорённость из design-review п.4).
- [ ] **Step 4: Скидка на мультимесяце** — `consume-discount.ts:8-14` `paidAmount < planPrice`: `planPrice` = цена ВЫБРАННОГО периода, не 2990. Проверить источник planPrice (`pricing-billing-map.md`) и что скидка/рекуррент считаются от 7990/13990.
- [ ] **Step 5:** typecheck + build. **Commit** `feat(billing): gate 2-month offer to 30-day plan; pin trial/referral to base plan; fix duplicate monitor`.

---

### Task 4: CP-рекуррент период параметризовать (2 call-site)

**Files:** Modify `apps/web/src/app/pricing/page.tsx:315-321`, `apps/web/src/app/(main)/billing/page.tsx:198`.

- [ ] **Step 1:** В обоих местах `recurrent.{interval,period}` вычислять из выбранного периода: 30д→`{interval:'Month',period:1}`, 90д→`{period:3}`, 180д→`{period:6}`. `amount`=`result.recurrentAmount` (уже цена периода из API), `startDate`=существующий `recurrentStartDate`. Обёртка `widget.ts` не трогается (уже generic). `recurrentReceipt` (54-ФЗ, billing.ts:273-278) — автоматически на `plan.price`+`plan.intervalDays`; проверить сумму/период чека рекуррента = периоду плана.
- [ ] **Step 2:** typecheck + build. **Commit** `feat(billing): parametrize CloudPayments recurrent period by plan intervalDays`.

---

### Task 5: Общие UI-компоненты витрины (`components/pricing/*`)

**Files:** Create `apps/web/src/components/pricing/{PlanPeriodCards,PricingFaq,WhatsIncluded}.tsx`; (Reviews уже общий — `components/billing/offer/ReviewsMarquee`).

- [ ] **Step 1: `PlanPeriodCards`** — 3 карточки по периодам из `getPlans` (1/3/6): цена, «в месяц», выгода (−11%/−22%/«месяц в подарок»), маркер «кому подойдёт», 6-мес акцентный. Проп `onSelect(intervalDays)` + режим оффера на карточке «1 месяц» (`showOfferMode`). Прайс из БД, не хардкод.
- [ ] **Step 2: `PricingFaq`** — вынести `FAQS`+рендер из `pricing/page.tsx` (инлайн, приватные) в компонент; переписать устаревшие FAQ (см. Task 7), добавить FAQ про мультимесяц.
- [ ] **Step 3: `WhatsIncluded`** — вместо `COMPARISON_ROWS` (COURSE vs PLATFORM, устарела): список ценности PLATFORM (все курсы/ассистент/решения/диагностика/обновления). Единый для обеих страниц.
- [ ] **Step 4: `DiscountedPrice.tsx:65`** — проп period-label (не хардкод «/мес») для 3/6-мес.
- [ ] **Step 5:** typecheck + build. **Commit** `feat(pricing): extract shared period-cards/FAQ/whats-included components`.

---

### Task 6: `/pricing` и `/billing` на общих компонентах (паритет)

**Files:** Modify `apps/web/src/app/pricing/page.tsx`, `apps/web/src/app/(main)/billing/page.tsx`.

- [ ] **Step 1: `/pricing`** — заменить COURSE+PLATFORM карточки на `PlanPeriodCards`; удалить COURSE-карточку (:416-497), хардкод 1990 (:429), `COURSE_SHORT_LABEL`/`COURSE_FEATURES` (:36-48), `COMPARISON_ROWS`+таблицу (:59-66,:607-648) → `WhatsIncluded`; FAQ → `PricingFaq`; `handlePayment` шлёт `intervalDays`. `ReviewsMarquee` остаётся.
- [ ] **Step 2: `/billing`** — те же `PlanPeriodCards`/`PricingFaq`/`WhatsIncluded` + **добавить `ReviewsMarquee`** (сейчас нет); удалить COURSE-блок/`COURSE_*` (:23-37); `plans.find(type==='PLATFORM')` (:139-140) → выбор периода; CP-рекуррент уже параметризован (Task 4). Структурно = `/pricing`.
- [ ] **Step 3:** typecheck + build. **Commit** `feat(pricing): three period cards + billing↔pricing parity via shared components`.

---

### Task 7: Copy-sweep остального продукта (homepage / courses / profile / admin / meta)

**Files:** Modify `apps/web/src/app/page.tsx`, `pricing/layout.tsx`, `courses/page.tsx`, `courses/{analytics,ads}/layout.tsx` (+ ai/ozon при наличии), `(main)/profile/page.tsx`, `(admin)/admin/promo/page.tsx`. Полный список — `pricing-copy-sweep.md`.

- [ ] **Step 1: Homepage `page.tsx`** — FAQ «Сколько стоит» (:86-87), собственные тариф-карточки/`plans` (:103-130, COURSE-карточка 1990) под новую структуру (в идеале через `PlanPeriodCards`), кнопка «Выбрать тариф» (:720). Позиционирование «внешний курс 45-90к vs 2990» (:537-561) — оставить (внешний курс), проверить что не намекает на внутренний «курс за 1990».
- [ ] **Step 2: SEO-мета** `pricing/layout.tsx` (title/описание «1990»/«Подписка на один курс»), `courses/{analytics,ads}/layout.tsx` («от 1 990 ₽/мес») → «входит в PLATFORM от 2 990 ₽/мес».
- [ ] **Step 3: `courses/page.tsx`** (LIVE) — pricing-mini 1990 / «Выбрать курс» CTA → ведут на PLATFORM. `CourseAccessCTA.tsx:96` хардкод «2 990» — проверить.
- [ ] **Step 4: Профиль** (:640) — легаси COURSE-подписка отображается рабочей, без мёртвой цены/CTA докупить (design-review п.5).
- [ ] **Step 5: Admin promo** — убрать COURSE из выбора типа (design-review п.6).
- [ ] **Step 6: Sweep-греп = 0.** `grep -rE "1 990|1990|подписк\w* на курс|за курс|один курс" apps/web/src` (исключая `design-*`/тесты) → 0 живых совпадений. **Commit** `feat(pricing): sweep COURSE/1990 copy across homepage, courses, profile, admin, meta`.

---

### Task 8: Финальная верификация (билд + тест-оплата картой + sweep)

- [ ] **Step 1:** `pnpm typecheck` + `pnpm --filter web build`.
- [ ] **Step 2: Тест-оплата КАРТОЙ** на `/admin/billing-test`: 3-мес → первое списание 7990, `recurrent{interval:'Month',period:3,amount:7990}`, `recurrentStartDate` +90д; чек+чек-рекуррента 54-ФЗ на 7990/90д. 6-мес → 13990/period:6/180д.
- [ ] **Step 3: Акция** — триал на 1-мес работает (60 дн); на 3/6-мес НЕ применяется. **Скидочный промокод** на 3-мес → первый платёж скидочный от 7990, рекуррент 7990.
- [ ] **Step 4: Аналитика выручки** — исторические COURSE-платежи видны (`revenue-metrics.ts` planSplit сохранён).
- [ ] **Step 5:** `/pricing`↔`/billing` структурно идентичны (3 карточки, отзывы+FAQ+«что входит» на обеих, COURSE/1990 нет). Sweep-греп=0.
- [ ] **Step 6:** Owner-координация внешнего лендинга (design-review п.7) — surface, не молча.

---

## Self-Review (против спеки)

- §1-2 (снос COURSE, 3 PLATFORM по intervalDays, Option A, акция параллельно) → Tasks 1,2,3. ✅
- §3.1 (данные+seed) → Task 1. §3.2 (резолв+initiatePayment) → Task 2. §3.3 (CP-рекуррент) → Task 4. §3.4/3.4a (3 карточки+паритет+общие компоненты) → Tasks 5,6. §3.5 (оффер 30-only) → Task 3. §3.6 (скидка мультимесяц) → Task 3. §3.7 (что сохранить/убрать) → Tasks 2,7 + Global Constraints. §3.8 (внешний лендинг) → Task 8 Step 6 (координация). §3.9 (sweep) → Task 7. ✅
- §4 гочи → Global Constraints. §6 план проверки → Task 8. ✅
- **Сверх спеки (design-review):** контракт выбора плана (1), trial/referral пины (2), монитор дублей (4), admin-promo операционка (6), pricing/layout SEO (Task 7.2), DiscountedPrice /мес (Task 5.4), courses/page.tsx LIVE (Task 7.3). Все внесены.

**Открытые для owner (блок design-review):** пункты 1-7 секции «Design-review» выше. Реализацию начинать после явного одобрения.
