---
phase: 58-diagnostic-on-jobs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrations/2026-05-26-collapse-marketplaces.sql
  - scripts/migrations/README.md
autonomous: false
requirements: []
tags: [phase-58, migration, data-backfill, prod-database]

must_haves:
  truths:
    - "After backfill, every UserProfile.marketplaces is a subset of {'WB','OZON'} (D-14)"
    - "Users whose marketplaces[] contained only legacy values (YANDEX/ALIEXPRESS/MEGAMARKET/OWN_SHOP/OTHER) are migrated to ['WB','OZON'] so they see all jobs (status quo) — NOT to [] (which would also work via the D-15 fallback, but [WB,OZON] is the explicit decision per CONTEXT.md)"
    - "Backfill is idempotent — running it twice produces the same final state (D-14)"
    - "Backfill runs in production BEFORE the new restrictive z.enum from Plan 58-01 is deployed (deploy ordering — see task)"
  artifacts:
    - path: "scripts/migrations/2026-05-26-collapse-marketplaces.sql"
      provides: "One-time SQL backfill aligned with D-14"
      contains: "UPDATE \"UserProfile\""
  key_links:
    - from: "scripts/migrations/2026-05-26-collapse-marketplaces.sql"
      to: "UserProfile.marketplaces String[] column"
      via: "Supabase Management API query endpoint OR psql against MAAL Supabase staging then prod"
      pattern: "INTERSECT SELECT unnest"
---

<objective>
One-time SQL backfill of `UserProfile.marketplaces` for the ~200 existing MAAL users so every row contains only WB and/or OZON. Implements D-14 verbatim. Must execute against MAAL Supabase project `saecuecevicwjkpmaoot` AHEAD of the Plan 58-01 code deploy, otherwise existing users with legacy marketplaces[] values would be locked out of /profile edit flows that use the new restrictive zod whitelist.

Purpose: Prevent data drift between the new 2-option wizard and existing user records. Without this, the marketplace filter in Plan 58-03 (D-15) would still work (legacy values fall through to fallback [WB,OZON]), BUT any future code path that re-validates marketplaces via the new z.enum would error.

Output: Committed SQL file + executed against staging then prod with verification queries.

**This plan is `autonomous: false` because it requires human approval and execution against the production database per `MAAL/CLAUDE.md` 🚨 PROD DATABASE SAFETY rules.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-diagnostic-on-jobs/58-CONTEXT.md
@./CLAUDE.md

<interfaces>
Target column (from packages/db/prisma/schema.prisma line 36):

```prisma
model UserProfile {
  marketplaces String[] @default([])  // Phase 56: WB|OZON|YANDEX|ALIEXPRESS|MEGAMARKET|OWN_SHOP|OTHER
  …
}
```

Execution surface — Supabase Management API query endpoint (per `.claude/memory/reference_supabase_mgmt.md`):
```
POST https://api.supabase.com/v1/projects/saecuecevicwjkpmaoot/database/query
Body: {"query": "<SQL>"}
```
This same endpoint was used for Phase 56 additive migration ([56-01] decision in STATE.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author idempotent backfill SQL file + README entry</name>
  <files>scripts/migrations/2026-05-26-collapse-marketplaces.sql, scripts/migrations/README.md</files>
  <read_first>
    .planning/phases/58-diagnostic-on-jobs/58-CONTEXT.md (D-14 — exact SQL block must be honored verbatim)
    scripts/migrations/README.md if it exists (otherwise create with a short header)
  </read_first>
  <action>Create scripts/migrations/2026-05-26-collapse-marketplaces.sql containing the exact SQL block from D-14 (CONTEXT.md lines 53-60): a single UPDATE statement using CASE + ARRAY INTERSECT to keep only 'WB'/'OZON' values, or fall back to ARRAY['WB','OZON'] when the intersection is empty. Add a header comment block above the SQL explaining: phase, decision id (D-14), idempotency note (running twice produces identical state because intersecting an already-cleaned array with {WB,OZON} yields itself), and an explicit pre-flight check query as a comment: `SELECT COUNT(*) FROM "UserProfile" WHERE marketplaces && ARRAY['YANDEX','ALIEXPRESS','MEGAMARKET','OWN_SHOP','OTHER']::text[];` — operator must confirm this returns 0 after backfill. Append a section to scripts/migrations/README.md (create if missing) documenting the file with execution order: (1) run on staging Supabase project first if a staging mirror exists, (2) verify counts via the pre-flight comment query, (3) run on prod via Supabase Management API query endpoint, (4) re-run pre-flight on prod to confirm 0. Do NOT execute the SQL in this task — execution is Task 2 (gated checkpoint).</action>
  <verify>
    <automated>test -f scripts/migrations/2026-05-26-collapse-marketplaces.sql && grep -c "INTERSECT" scripts/migrations/2026-05-26-collapse-marketplaces.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/migrations/2026-05-26-collapse-marketplaces.sql` (test -f returns 0)
    - Source assertion: `grep -c "UPDATE \"UserProfile\"" scripts/migrations/2026-05-26-collapse-marketplaces.sql` ≥ 1
    - Source assertion: `grep -c "INTERSECT" scripts/migrations/2026-05-26-collapse-marketplaces.sql` ≥ 2 (one per CASE branch)
    - Source assertion: `grep -E "'WB'.*'OZON'|ARRAY\['WB','OZON'\]" scripts/migrations/2026-05-26-collapse-marketplaces.sql` matches at least once
    - Source assertion: file header comment contains the strings "D-14", "idempotent", "Phase 58"
    - Source assertion: pre-flight check query containing `marketplaces && ARRAY['YANDEX'` is present as a comment in the file
  </acceptance_criteria>
  <done>SQL file + README updated; ready for human-gated execution in Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Execute backfill against MAAL Supabase prod (gated)</name>
  <what-built>
    A reviewed SQL file at `scripts/migrations/2026-05-26-collapse-marketplaces.sql` per D-14. This task DOES NOT auto-execute — owner approves and runs the SQL.
  </what-built>
  <how-to-verify>
    Before approving, owner must:
    1. Read `scripts/migrations/2026-05-26-collapse-marketplaces.sql` and confirm the UPDATE statement matches D-14 verbatim.
    2. Confirm a fresh Supabase PITR backup is available (per `MAAL/CLAUDE.md` pre-flight checklist item 4) — Dashboard → Project saecuecevicwjkpmaoot → Database → Backups.
    3. Run the pre-flight COUNT query from the file's header comment to capture the "before" number of users with legacy marketplace values:
       ```
       SELECT COUNT(*) FROM "UserProfile" WHERE marketplaces && ARRAY['YANDEX','ALIEXPRESS','MEGAMARKET','OWN_SHOP','OTHER']::text[];
       ```
       (Expected: some positive number ~50-100 based on ~200 user base; record the number.)
    4. Execute the UPDATE via Supabase Management API query endpoint (same pattern as Phase 56 additive migration per STATE.md [56-01]) OR via SQL Editor in Supabase Dashboard. The endpoint:
       `POST https://api.supabase.com/v1/projects/saecuecevicwjkpmaoot/database/query` with body `{"query": "<contents-of-sql-file>"}`.
    5. Re-run the pre-flight COUNT query. **Expected: 0** (no rows with legacy values remain).
    6. Spot-check one user: `SELECT id, marketplaces FROM "UserProfile" WHERE id = '<known test user>' LIMIT 1;` — confirm result is a subset of `{WB, OZON}`.
    7. Confirm Plan 58-01 deploy can now proceed (the new restrictive z.enum will no longer reject any existing row).
  </how-to-verify>
  <resume-signal>
    Type "backfill executed, post-count = 0" to proceed.
    If post-count != 0: DO NOT proceed. Investigate which rows escaped (likely a NULL marketplaces[] column — verify CASE branch behavior on NULL — and add a `WHERE marketplaces IS NOT NULL` clause if needed). Open new task.
  </resume-signal>
  <files>scripts/migrations/2026-05-26-collapse-marketplaces.sql (executed against MAAL Supabase prod via Management API)</files>
  <action>Human-gated execution of the SQL file from Task 1 against the production MAAL Supabase project. See <how-to-verify> for the 7-step procedure (verify PITR backup, capture pre-flight COUNT, execute UPDATE, re-run COUNT, spot-check one user, then resume).</action>
  <verify>
    <automated>echo "manual checkpoint — operator runs SELECT COUNT(*) FROM \"UserProfile\" WHERE marketplaces && ARRAY['YANDEX','ALIEXPRESS','MEGAMARKET','OWN_SHOP','OTHER']::text[]; expected = 0"</automated>
  </verify>
  <done>Operator confirms post-execution COUNT == 0 and provides resume-signal. Plan 58-01 z.enum tightening is now safe to deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → MAAL Supabase prod | DDL/DML executed by deploy user crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-T | Tampering | one-time SQL backfill | mitigate | SQL is hardcoded (no user input interpolation) — no injection surface. File reviewed by owner before execution (Task 2 checkpoint). Idempotent — re-running produces same final state. |
| T-58-02-D | Denial of service / data loss | UserProfile.marketplaces column | mitigate | (a) Supabase PITR backup verified before execution per CLAUDE.md pre-flight checklist. (b) Pre-flight COUNT query captures "before" state so operator can detect anomalies. (c) Post-flight COUNT must be 0 to confirm success. (d) No DROP/TRUNCATE — UPDATE only. (e) Recovery path documented in CLAUDE.md incident 2026-05-12 section. |
| T-58-02-R | Repudiation | execution audit | mitigate | SQL file committed to git with header comment naming D-14; execution is via Supabase Management API which logs queries server-side. |
| T-58-02-SC | Tampering (supply chain) | npm install | n/a | No package installs in this plan. |
</threat_model>

<verification>
- File exists: `scripts/migrations/2026-05-26-collapse-marketplaces.sql`
- Header comment contains D-14, idempotency note, and pre-flight check query
- Post-execution prod query: `SELECT COUNT(*) FROM "UserProfile" WHERE marketplaces && ARRAY['YANDEX','ALIEXPRESS','MEGAMARKET','OWN_SHOP','OTHER']::text[];` returns 0
- Manual checkpoint approved by owner before Plan 58-01 reaches prod
</verification>

<success_criteria>
1. SQL file committed and reviewed
2. Backfill executed against prod with post-count == 0
3. No user record has marketplaces[] containing any legacy value
4. Plan 58-01 z.enum tightening is safe to deploy
</success_criteria>

<output>
Create `.planning/phases/58-diagnostic-on-jobs/58-02-SUMMARY.md` recording: (a) SQL committed (commit hash), (b) prod execution timestamp, (c) pre/post counts, (d) operator approval signal.
</output>
