---
phase: 58-diagnostic-on-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/welcome/options.ts
  - apps/web/src/components/welcome/StepMarketplaces.tsx
  - packages/api/src/routers/onboarding.ts
  - apps/web/src/components/welcome/__tests__/StepMarketplaces.test.tsx
autonomous: true
requirements: []
tags: [phase-58, onboarding, marketplaces]

must_haves:
  truths:
    - "Wizard step 2 (StepMarketplaces) shows exactly 2 cards: Wildberries and Ozon (D-12)"
    - "onboarding.complete rejects any marketplace value outside ['WB', 'OZON'] with z.enum validation (D-12)"
    - "Existing users (whose marketplaces[] may contain YANDEX/ALIEXPRESS/MEGAMARKET/OWN_SHOP/OTHER) are NOT shown the wizard again — backfill (Plan 58-02) handles them (D-14)"
  artifacts:
    - path: "apps/web/src/components/welcome/options.ts"
      provides: "MARKETPLACE_OPTIONS array of length 2"
      contains: "MARKETPLACE_OPTIONS"
    - path: "packages/api/src/routers/onboarding.ts"
      provides: "MARKETPLACES tuple of length 2"
      contains: "const MARKETPLACES = ['WB', 'OZON'] as const"
  key_links:
    - from: "StepMarketplaces.tsx"
      to: "options.ts"
      via: "import { MARKETPLACE_OPTIONS }"
      pattern: "MARKETPLACE_OPTIONS"
    - from: "onboarding.ts complete mutation"
      to: "z.enum(MARKETPLACES) input validation"
      via: "marketplaces: z.array(z.enum(MARKETPLACES))"
      pattern: "z\\.enum\\(MARKETPLACES\\)"
---

<objective>
Collapse the wizard marketplace step from 7 options to 2 (WB + Ozon) and tighten the z.enum whitelist in the onboarding tRPC router to match. This is the UI/API half of D-12/D-13. The SQL backfill for existing users (D-14) is Plan 58-02 and MUST be deployed before this plan reaches production (otherwise existing users with legacy marketplace values still pass through getState/profile reads correctly — but onboarding.complete called via /profile edit form would reject saving the legacy mix).

Purpose: Stop falsely promising support for marketplaces (Yandex/AliExpress/Megamarket/OwnShop/Other) where the platform has zero job content (all 29 Phase 57 jobs are WB/OZON/BOTH). Foundation for Plan 58-03 marketplace-aware filter.

Output: 2-card wizard step + restrictive zod whitelist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-diagnostic-on-jobs/58-CONTEXT.md

<interfaces>
From apps/web/src/components/welcome/options.ts (current state — to be reduced):

```typescript
export interface MarketplaceOption {
  key: 'WB' | 'OZON' | 'YANDEX' | 'ALIEXPRESS' | 'MEGAMARKET' | 'OWN_SHOP' | 'OTHER';
  label: string;
  icon: LucideIcon;
}

export const MARKETPLACE_OPTIONS: MarketplaceOption[] = [
  { key: 'WB', label: 'Wildberries', icon: ShoppingBag },
  { key: 'OZON', label: 'Ozon', icon: Package },
  // … 5 more options to be REMOVED
];
```

From packages/api/src/routers/onboarding.ts (current state):

```typescript
const MARKETPLACES = ['WB', 'OZON', 'YANDEX', 'ALIEXPRESS', 'MEGAMARKET', 'OWN_SHOP', 'OTHER'] as const;
```

Target state for both:
- `MarketplaceOption.key` type literal becomes `'WB' | 'OZON'`
- `MARKETPLACE_OPTIONS` array length = 2
- Onboarding router `MARKETPLACES` tuple = `['WB', 'OZON'] as const`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce MARKETPLACE_OPTIONS + tighten StepMarketplaces grid</name>
  <files>apps/web/src/components/welcome/options.ts, apps/web/src/components/welcome/StepMarketplaces.tsx</files>
  <read_first>
    apps/web/src/components/welcome/options.ts (full)
    apps/web/src/components/welcome/StepMarketplaces.tsx (full — note current grid classes and how MARKETPLACE_OPTIONS is consumed)
  </read_first>
  <behavior>
    - MARKETPLACE_OPTIONS exported array has length === 2 with keys 'WB' and 'OZON' in that order
    - MarketplaceOption.key type literal is exactly `'WB' | 'OZON'` (no other literals)
    - Unused icon imports (Store, Globe, Boxes, MoreHorizontal) removed from options.ts
    - StepMarketplaces.tsx grid classes contain `grid-cols-2` (drop any `sm:grid-cols-3` per D-13)
    - StepMarketplaces still allows selecting 0, 1, or 2 marketplaces but requires at least one for proceeding (preserve existing selection logic — confirm by reading current file)
  </behavior>
  <action>Edit options.ts: shrink MarketplaceOption.key literal type to `'WB' | 'OZON'`; reduce MARKETPLACE_OPTIONS array to two entries (WB→Wildberries→ShoppingBag, OZON→Ozon→Package); remove now-unused lucide-react imports (Store, Globe, Boxes, MoreHorizontal — preserve ShoppingBag and Package). Edit StepMarketplaces.tsx: change the marketplace grid container className from `grid-cols-2 sm:grid-cols-3` (or whatever the current value is — read it first) to a flat `grid-cols-2`. Do NOT modify selection state logic, validation, or proceed-button behavior — only the visual grid and the options it iterates over.</action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm --filter @mpstats/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "key: '" apps/web/src/components/welcome/options.ts | grep -v ^# | awk '$1==2'` returns one match (exactly 2 marketplace key lines remain)
    - Source assertion: `grep -E "'YANDEX'|'ALIEXPRESS'|'MEGAMARKET'|'OWN_SHOP'|'OTHER'" apps/web/src/components/welcome/options.ts` returns no matches
    - Source assertion: `grep -c "grid-cols-2" apps/web/src/components/welcome/StepMarketplaces.tsx` ≥ 1 AND `grep "sm:grid-cols-3" apps/web/src/components/welcome/StepMarketplaces.tsx` returns no matches
    - Build: `pnpm --filter @mpstats/web typecheck` exits 0
  </acceptance_criteria>
  <done>options.ts exports 2-element MARKETPLACE_OPTIONS, StepMarketplaces renders a 2-column grid, typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten onboarding.complete z.enum whitelist + add regression test</name>
  <files>packages/api/src/routers/onboarding.ts, apps/web/src/components/welcome/__tests__/StepMarketplaces.test.tsx</files>
  <read_first>
    packages/api/src/routers/onboarding.ts (full — note MARKETPLACES tuple position and how it threads into z.enum)
    apps/web/src/components/welcome/options.ts (post-Task 1 state)
    apps/web/src/components/welcome/StepMarketplaces.tsx (full — for test setup pattern)
  </read_first>
  <behavior>
    - onboarding.ts MARKETPLACES tuple === ['WB', 'OZON']
    - onboarding.complete rejects payload `{ marketplaces: ['YANDEX'] }` with a zod parse error (per D-12: tampered keys must fail before DB write)
    - onboarding.complete accepts `{ marketplaces: ['WB'] }`, `{ marketplaces: ['OZON'] }`, `{ marketplaces: ['WB', 'OZON'] }`, and `{ marketplaces: [] }`
    - StepMarketplaces.test.tsx renders the component and asserts exactly 2 marketplace cards present in DOM (regression guard against future re-expansion)
  </behavior>
  <action>In onboarding.ts, narrow the `MARKETPLACES` tuple constant to `['WB', 'OZON'] as const`. Leave the rest of the file (GOALS, EXPERIENCE, both procedures, CQ mirroring) unchanged — D-13 only specifies marketplaces. Create StepMarketplaces.test.tsx under apps/web/src/components/welcome/__tests__/ following the existing welcome-page.test.tsx pattern (look it up): render StepMarketplaces with required props (read the component signature first), assert `screen.getAllByRole('button', { name: /Wildberries|Ozon/ }).length === 2` (or the closest accessible-name query — adjust to match the component's actual rendered structure). Do NOT modify the CQ mirror logic in the complete mutation.</action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm --filter @mpstats/api typecheck && pnpm --filter @mpstats/web test -- StepMarketplaces</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E "const MARKETPLACES = \[" packages/api/src/routers/onboarding.ts` matches exactly one line containing `'WB'` and `'OZON'` and NOT containing `'YANDEX'`
    - Test command: `pnpm --filter @mpstats/web test -- StepMarketplaces` exits 0 with the new test reporting "2 marketplace cards" assertion green
    - Build: `pnpm --filter @mpstats/api typecheck` exits 0
  </acceptance_criteria>
  <done>z.enum whitelist is 2-element; regression test exists and passes; api typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/trpc/onboarding.complete | User-controlled `marketplaces[]` array crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-T | Tampering | onboarding.complete input | mitigate | z.enum(['WB','OZON']) rejects tampered keys before DB write (Task 2). Pre-existing pattern from Phase 56 (decision [56-02]) preserved — only the whitelist contents shrink. |
| T-58-01-S | Spoofing | userId in mutation | accept | Pre-existing mitigation (ctx.user.id from session), no change in this plan |
| T-58-01-I | Information disclosure | marketplace selection | accept | No PII, low-value target — same disposition as Phase 56 |
</threat_model>

<verification>
- TypeScript: `pnpm --filter @mpstats/web typecheck` AND `pnpm --filter @mpstats/api typecheck` both exit 0
- Unit test: `pnpm --filter @mpstats/web test -- StepMarketplaces` exits 0
- Source assertion: no `'YANDEX'|'ALIEXPRESS'|'MEGAMARKET'|'OWN_SHOP'|'OTHER'` literals remain in options.ts or onboarding.ts
</verification>

<success_criteria>
1. MARKETPLACE_OPTIONS has length 2
2. MARKETPLACES tuple in onboarding.ts has length 2
3. StepMarketplaces grid renders as 2 columns
4. Regression test guards against future re-expansion
5. All typechecks and tests pass
</success_criteria>

<output>
Create `.planning/phases/58-diagnostic-on-jobs/58-01-SUMMARY.md` when done.
</output>
