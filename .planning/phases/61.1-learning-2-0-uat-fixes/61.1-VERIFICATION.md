---
phase: "61.1-learning-2-0-uat-fixes"
verified: 2026-06-04T00:00:00Z
status: human_needed
score: 12/14 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Сердечко в AgentSearch recommend: найти задачу поиском → убедиться, что у каждой рекомендованной задачи отображается сердечко (FavoriteButton), кнопки «В план»/«Положить в трек» нет; отметить сердечко → задача появляется в /learn/favorites раздел «Решения»"
    expected: "Каждая recommend-карточка задачи имеет независимое сердечко; нажатие добавляет в Избранное; страница /learn/favorites показывает задачу"
    why_human: "Поведение FavoriteButton (оптимистичный toggle, visual state) и роутинг в /learn/favorites не проверяются grep-ом"
  - test: "/learn/plan для пользователя с диагностикой и addedJobs: страница показывает блок «Рекомендованные задачи» (карточки-ссылки) и плоский блок «Рекомендованные уроки» с бейджами «Ошибка»/«Углубление»/«Рост»/«Продвинутый»; аккордеон секций отсутствует"
    expected: "Плоский план, два блока, бейджи видны, кнопки «Убрать» и «Обновить план» работают, счётчик прогресса корректен"
    why_human: "Визуальный рендер и интерактивность страницы плана (onRemoveFromTrack, rebuildTrack) требуют ручной проверки"
  - test: "/learn/plan для пользователя с LEGACY flat-format LearningPath (isSectioned=false, addedJobs непустой): проверить WR-01 — отображаются ли addedJobs или план показывает empty-state"
    expected: "При наличии addedJobs блок «Рекомендованные задачи» должен показываться даже когда isSectioned=false; но текущий код (line 160 plan/page.tsx) условие !isSectioned делает planIsEmpty=true, скрывая весь контент"
    why_human: "WR-01 (WARNING из 61.1-REVIEW.md): planIsEmpty жёстко требует isSectioned, addedJobs-only план для legacy-юзера будет спрятан; ожидается принятие решения owner о приоритете фикса"
  - test: "2-й уровень навигации: открыть задачу → кликнуть на урок → убедиться, что URL содержит ?from=job:<slug> и хлебные крошки показывают «Решения под задачу / [Название задачи] / Урок N»; ссылки в крошках ведут на /learn/solutions и /learn/job/<slug>"
    expected: "Контекст задачи виден в URL и в крошках; стандартные крошки без from — без изменений"
    why_human: "Визуальный рендер и ссылки крошек требуют ручной проверки на staging; window.location.search (WR-02) может не обновиться при client-side prev/next навигации — нужно проверить поведение при переходе между уроками"
  - test: "Единая терминология: на /learn каталог задач, детальная страница задачи, результаты поиска, диагностический блок — нет слов «джоба», «плейбук», «трек» (в значении трек обучения) в пользовательском интерфейсе"
    expected: "Везде «задача»/«решение»/«план»; кнопки, тосты, заголовки, тултипы — по канону D-02"
    why_human: "Полный sweep UI-строк по всем поверхностям требует живого браузера; можно покрыть несколько точек проверки из списка в 61.1-04-PLAN.md"
  - test: "Прод-применение cleanup-added-jobs (Task 2 плана 61.1-03, owner-gated): owner запускает snapshot → apply → verify по runbook в 61.1-03-SUMMARY.md"
    expected: "После apply: count LearningPath с непустым addedJobs = 0; Favorite(JOB) count не уменьшился; LessonProgress count не изменился"
    why_human: "Owner-gated blocking checkpoint; не выполняется автономно; необходимо явное подтверждение owner и выполнение runbook через Supabase Management API"
---

# Phase 61.1: Обучение 2.0 UAT-Fixes — Verification Report

**Phase Goal:** Закрыть баги UAT-сессии 03–04.06 и довести модель до консистентной: задачи добавляются только в Избранное (сердечко, модель A), Персональный план перестроен в плоский Избранное-like вид (рекомендованные задачи + уроки с бейджем приоритета), полный нейминг джоба→задача / трек→план, 2-й уровень навигации урок↔задача.

**Verified:** 2026-06-04
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | В AgentSearch recommend у задачи стоит FavoriteButton(JOB), track-кнопки нет | ✓ VERIFIED | AgentSearch.tsx:181 `itemType="JOB"`, isFavorited seed line 40-41; addJobMutation grep = 0 |
| 2 | JobCard в каталоге (без onAddToTrack) — только сердечко, нет бейджа «В треке» и кнопки «+ В план» | ✓ VERIFIED | JobCard.tsx line 57: button guarded `onAddToTrack && !job.isInTrack`; «В треке» badge removed |
| 3 | Детальная страница задачи (job/[slug]) — нет track-кнопок, есть FavoriteButton(JOB) seeded через isFavorited | ✓ VERIFIED | page.tsx line 9 import FavoriteButton, line 17 isFavorited query, line 51-52 `<FavoriteButton itemType="JOB"`; addJobToTrack grep = 0 |
| 4 | /learn/plan показывает блок «Рекомендованные задачи» из addedJobs | ✓ VERIFIED | plan/page.tsx line 258-265: `{addedJobs.length > 0 && (<... Рекомендованные задачи...{addedJobs.map` |
| 5 | /learn/plan показывает плоский «Рекомендованные уроки» с бейджем приоритета, аккордеон убран | ✓ VERIFIED | expandedSections grep = 0; SECTION_BADGES map errors/deepening/growth/advanced → «Ошибка»/«Углубление»/«Рост»/«Продвинутый» confirmed lines 29-32 |
| 6 | Счётчик прогресса плана (visibleTotal/visibleCompleted), кнопка «Убрать», «Обновить план» и data-tour="learn-sections" сохранены | ✓ VERIFIED | line 237: `data-tour="learn-sections"`; onRemoveFromTrack и rebuildTrack grep ≥1 |
| 7 | В живых компонентах обучения нет user-facing строк «джоба», «плейбук», «трек» | ✓ VERIFIED | Greps: DiagnosticGateBanner «персональный план» ≥1; SearchResultCard «В вашем плане» ≥1; LessonCard «В треке»=0; JobCatalog «задач» ≥1, «джоб»=0; RecommendedJobsBlock «плейбук»=0, «/learn/track»=0; job/[slug] «Продолжить задачу» ≥1 |
| 8 | RecommendedJobsBlock функционально не сломан (addJobToTrack работает) | ✓ VERIFIED | Идентификаторы addJobMutation/addJobToTrack/isInTrack не тронуты (только UI-строки); bulk nav → /learn/plan |
| 9 | Существует идемпотентный скрипт cleanup-added-jobs.ts с dry-run/apply, unit-тесты зелёные | ✓ VERIFIED | scripts/cleanup-added-jobs.ts exports planRowsToClear + cleanup; --dry-run overrides --apply (line 125: `&& !dryRun`); 6 тестов в cleanup-added-jobs.test.ts |
| 10 | Скрипт НИКОГДА не пишет LessonProgress (только .count) | ✓ VERIFIED | grep lessonProgress в cleanup-added-jobs.ts: только .count; нет .update/.create/.delete |
| 11 | Прод-применение cleanup — owner-gated blocking checkpoint, runbook задокументирован | ✓ VERIFIED | 61.1-03-SUMMARY.md содержит полный runbook с SQL-запросами, snapshot/verify шагами; Task 2 не выполнен автономно (по дизайну) |
| 12 | job.getTitleBySlug — лёгкая процедура, 3 unit-теста зелёные | ✓ VERIFIED | job.ts line 124: protectedProcedure getTitleBySlug; job.test.ts describe('getTitleBySlug') 3 кейса |
| 13 | Урок, открытый из задачи, несёт ?from=job:<slug> в URL | ✓ VERIFIED | job/[slug]/page.tsx line 79: `href={l.locked ? '#' : \`/learn/${l.id}?from=job:${slug}\`` |
| 14 | Страница урока показывает контекстные хлебные крошки «Решения под задачу / [Задача] / Урок» при наличии from | ? UNCERTAIN | learn/[id]/page.tsx line 295-302: парсинг from + getTitleBySlug wired (grep confirmed). Однако парсинг через window.location.search (WR-02): при client-side prev/next навигации крошки могут не обновиться — нужна ручная проверка |

**Score:** 12/14 (2 требуют ручной проверки/уточнения)

---

### Deferred Items

| # | Item | Status |
|---|------|--------|
| 1 | Прод-применение cleanup-added-jobs (Task 2 плана 03) | PENDING — owner-gated по дизайну |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/learning/AgentSearch.tsx` | recommend-задачи с FavoriteButton | ✓ VERIFIED | itemType="JOB" line 181, isFavorited batch seeded |
| `apps/web/src/components/learning/JobCard.tsx` | без track-механики в каталоге, сердечко | ✓ VERIFIED | «+ В план» guarded by onAddToTrack; «В треке» badge removed |
| `apps/web/src/app/(main)/learn/job/[slug]/page.tsx` | FavoriteButton(JOB) вместо track-кнопок; from=job: на ссылках уроков | ✓ VERIFIED | Оба требования подтверждены grep |
| `apps/web/src/app/(main)/learn/plan/page.tsx` | плоский план: задачи + уроки с бейджем | ✓ VERIFIED (with WARNING) | Плоский вид, бейджи, addedJobs рендерятся. WR-01: isSectioned guard при planIsEmpty скрывает addedJobs для legacy flat-format юзеров |
| `apps/web/src/app/(main)/learn/[id]/page.tsx` | контекстный breadcrumb по ?from=job | ✓ WIRED (uncertain behavior) | getTitleBySlug wired; window.location.search вместо useSearchParams (WR-02) |
| `packages/api/src/routers/job.ts` | getTitleBySlug procedure | ✓ VERIFIED | line 124 |
| `packages/api/src/routers/__tests__/job.test.ts` | 3 unit-теста getTitleBySlug | ✓ VERIFIED | describe('getTitleBySlug') с 3 кейсами |
| `scripts/cleanup-added-jobs.ts` | idempotent dry-run/apply cleanup | ✓ VERIFIED | CR-01 fixed: --dry-run overrides --apply |
| `scripts/__tests__/cleanup-added-jobs.test.ts` | 6 unit-тестов | ✓ VERIFIED | 6 кейсов включая идемпотентность + LessonProgress инвариант |
| 7 live component files (W2 terminology) | единая терминология | ✓ VERIFIED | Все acceptance greps из 61.1-04 подтверждены |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|------|-----|--------|---------|
| AgentSearch recommend / JobCard / JobDetail | trpc.favorite.add/remove | FavoriteButton itemType="JOB" | ✓ WIRED | 3 поверхности используют FavoriteButton(JOB) |
| AgentSearch / job/[slug] | trpc.favorite.isFavorited | initialFavorited batch seed | ✓ WIRED | Оба файла: isFavorited.useQuery → favoritedSet |
| plan/page.tsx | getRecommendedPath (addedJobs) | useMemo addedJobs | ✓ WIRED | line 99-100 |
| job/[slug] список уроков | /learn/[id]?from=job:<slug> | Link href | ✓ WIRED | line 79 |
| learn/[id] breadcrumb | job.getTitleBySlug | useQuery при наличии fromJobSlug | ✓ WIRED | lines 299-302 |

---

### Behavioral Spot-Checks

| Behavior | Method | Result | Status |
|----------|--------|--------|--------|
| addJobToTrack removed from job/[slug] | grep addJobToTrack/removeJobFromTrack in file | 0 matches | ✓ PASS |
| FavoriteButton(JOB) in job/[slug] | grep `itemType="JOB"` | match at line 52 | ✓ PASS |
| addJobMutation removed from AgentSearch | grep addJobMutation | 0 matches | ✓ PASS |
| plan/page.tsx accordion removed | grep expandedSections\|toggleSection | 0 matches | ✓ PASS |
| data-tour="learn-sections" preserved | grep data-tour="learn-sections" | match at line 237 | ✓ PASS |
| cleanup-added-jobs CR-01 fixed | grep dry-run precedence | `&& !dryRun` on line 125 | ✓ PASS |
| Terminology W2 — no «джоба»/«плейбук»/«трек» | greps across 7 files | All 0 | ✓ PASS |
| getTitleBySlug in job.ts | grep getTitleBySlug | match at line 124 | ✓ PASS |
| from=job: in job/[slug] lesson links | grep from=job: | match at line 79 | ✓ PASS |
| Tests: api 151/151, web 208/208 | reported in context | Executor-verified | ? SKIP (no runner) |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/web/src/app/(main)/learn/plan/page.tsx` | 160 | `planIsEmpty` gated on `!isSectioned` — hides addedJobs for legacy flat-format paths | ⚠️ Warning | WR-01 (REVIEW): юзеры с legacy flat-format LearningPath не увидят блок «Рекомендованные задачи»; advisory для owner |
| `apps/web/src/app/(main)/learn/[id]/page.tsx` | 295-296 | `window.location.search` вместо `useSearchParams()` | ⚠️ Warning | WR-02 (REVIEW): breadcrumb не обновится при client-side prev/next навигации между уроками; advisory для owner |
| `apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx` | 16, 104 | Shared `addJobMutation` + single `pendingId` — race on concurrent bulk+single add | ℹ️ Info | WR-03 (REVIEW): edge-case UI glitch, не влияет на корректность данных |

No TBD/FIXME/XXX markers found in phase-modified files.

---

### Requirements Coverage

| Work Item | Plans | Status | Evidence |
|-----------|-------|--------|----------|
| W1 — Задачи → Избранное (сердечко) | 61.1-01 | ✓ SATISFIED | AgentSearch/JobCard/JobDetail: track-кнопки заменены FavoriteButton(JOB) |
| W1b — Редизайн Персонального плана | 61.1-02, 61.1-03 | ✓ SATISFIED (prod-apply pending) | plan/page.tsx плоский; задачи из addedJobs рендерятся; cleanup-скрипт готов к owner-apply |
| W2 — Нейминг джоба→задача / трек→план | 61.1-04 | ✓ SATISFIED | Все 7 файлов переименованы; acceptance greps подтверждены |
| W3 — 2-й уровень навигации (breadcrumb) | 61.1-05 | ✓ SATISFIED (with WR-02 caveat) | from=job: в ссылках; getTitleBySlug wired; крошки рендерятся; soft-nav edge case (WR-02) advisory |

---

### Human Verification Required

#### 1. Сердечко задачи в AgentSearch recommend и Избранном

**Test:** Выполнить поиск на /learn, перейти в режим рекомендаций → у каждой рекомендованной задачи нажать сердечко → перейти в /learn/favorites → убедиться, что задача появилась в разделе «Решения».
**Expected:** FavoriteButton отображается у каждой рекомендованной задачи независимо; нет кнопки «+ В план»; задача видна в /learn/favorites раздел «Решения».
**Why human:** Оптимистичный toggle UI и роутинг в Избранное не проверяются статически.

#### 2. Плоский Персональный план с бейджами

**Test:** Зайти на /learn/plan для юзера с результатами диагностики (sectioned format). Убедиться: блок «Рекомендованные задачи» (если есть addedJobs), плоский список уроков с бейджами «Ошибка»/«Углубление»/«Рост»/«Продвинутый», нет аккордеон-секций, работает «Убрать» и «Обновить план», счётчик прогресса показывает корректное число.
**Expected:** Два плоских блока, бейджи видны рядом с каждым уроком, функциональность кнопок цела.
**Why human:** Визуальный рендер и интерактивность страницы требуют живого браузера.

#### 3. WR-01 — Legacy flat-format план с addedJobs

**Test:** Зайти на /learn/plan от имени юзера с legacy flat-format LearningPath (isSectioned=false) при наличии addedJobs (например, test@mpstats.academy с пересборкой).
**Expected:** Ожидается принятие решения owner: либо принять текущее поведение (empty-state пока юзер не пройдёт диагностику заново), либо применить фикс WR-01 из 61.1-REVIEW.md (изменить formulation planIsEmpty).
**Why human:** Требует решения owner о приоритете фикса и целевого поведения для legacy-юзеров.

#### 4. Контекстный breadcrumb (W3) + WR-02

**Test:** Открыть детальную страницу задачи → кликнуть на урок → URL должен содержать `?from=job:<slug>`; хлебные крошки «Решения под задачу / [Название задачи] / Урок N»; ссылки крошек ведут на /learn/solutions и /learn/job/<slug>. Открыть тот же урок без from (напрямую) → стандартные крошки «Обучение / Курс / Урок».
**Expected:** Контекст виден в URL и крошках. Дополнительно проверить WR-02: при переходе Prev/Next между уроками внутри задачи (client-side навигация) — обновляется ли крошка до нового контекста.
**Why human:** Визуальный рендер крошек и поведение при client-side навигации (WR-02) требуют ручной проверки.

#### 5. Единая терминология на staging

**Test:** Пройтись по каталогу задач, результатам поиска, диагностическому блоку, детальной задаче — не должно быть слов «джоба», «плейбук», «трек» в видимом UI.
**Expected:** Везде «задача»/«решение»/«план»; тосты, тултипы, кнопки — по канону D-02.
**Why human:** Полный sweep UI-строк на живом staging.

#### 6. Прод-применение cleanup-added-jobs (owner-gated)

**Test:** Owner выполняет runbook из 61.1-03-SUMMARY.md: snapshot → dry-run SELECT → UPDATE через Supabase Mgmt API → verify (с_jobs=0, Favorite count ≥ baseline, LessonProgress count = baseline).
**Expected:** После apply: 0 строк LearningPath с непустым addedJobs; Favorite(JOB) и LessonProgress не пострадали.
**Why human:** Owner-gated checkpoint; необходимо явное подтверждение на запуск; разовая необратимая операция на prod Supabase.

---

### Gaps Summary

No blocking gaps identified. All 5 plan deliverables (W1, W1b code, W1b cleanup script, W2, W3) have substantive implementation verified in the codebase. Test counts confirmed by executor (api 151/151, web 208/208). CR-01 from code review is fixed in cleanup-added-jobs.ts.

**Advisory warnings (not blocking; owner decision needed):**

- **WR-01** (`plan/page.tsx:160`): `planIsEmpty` requires `isSectioned === true`; legacy flat-format LearningPath users with addedJobs will see empty-state instead of «Рекомендованные задачи». Fix in REVIEW.md.
- **WR-02** (`learn/[id]/page.tsx:295-296`): `window.location.search` instead of `useSearchParams()` — breadcrumb won't update on client-side prev/next lesson navigation. Fix in REVIEW.md.
- **WR-03** (`RecommendedJobsBlock.tsx`): shared mutation `pendingId` race on concurrent bulk+single add — cosmetic glitch only.

**Pending action (not a code gap):** Owner-gated prod cleanup of `LearningPath.addedJobs` (Task 2, plan 03) per documented runbook.

---

_Verified: 2026-06-04_
_Verifier: Claude (gsd-verifier)_
