---
phase: "61.1"
plan: "02"
type: execute
wave: 2
depends_on: ["61.1-01"]
autonomous: true
requirements: [W1b]
files_modified:
  - apps/web/src/app/(main)/learn/plan/page.tsx

must_haves:
  truths:
    - "Страница /learn/plan показывает блок «Рекомендованные задачи» — карточки задач из getRecommendedPath.addedJobs"
    - "Страница /learn/plan показывает блок «Рекомендованные уроки» — плоский список карточек, отсортированный по приоритету errors→deepening→growth→advanced"
    - "У каждого урока в плане виден бейдж приоритета («Ошибка»/«Углубление»/«Рост»/«Продвинутый») вместо аккордеон-секций"
    - "Аккордеон секций errors/deepening/growth/advanced убран — план плоский, как Избранное"
    - "План остаётся без сердечек: кнопки «Убрать» (урок) и «Обновить план» сохранены; счётчик прогресса по видимым диагностическим урокам не сломан"
  artifacts:
    - path: "apps/web/src/app/(main)/learn/plan/page.tsx"
      provides: "плоский redesign плана: задачи (карточки) + уроки (плоский список с бейджем приоритета)"
      min_lines: 150
  key_links:
    - from: "plan/page.tsx"
      to: "trpc.learning.getRecommendedPath (sections + addedJobs)"
      via: "useQuery"
      pattern: "getRecommendedPath"
    - from: "plan/page.tsx «Рекомендованные задачи»"
      to: "JobCard"
      via: "рендер addedJobs"
      pattern: "addedJobs"
---

<objective>
W1b — редизайн страницы Персонального плана из аккордеона диагностических секций в плоский вид «как Избранное»: блок «Рекомендованные задачи» (карточки задач из addedJobs, которые бэкенд уже отдаёт, но страница не рендерит) + блок «Рекомендованные уроки» (плоский список карточек, сорт по приоритету, приоритет = бейдж на карточке). После Phase 58 диагностика рекомендует преимущественно задачи — теперь они видны в Плане.

Purpose: Закрыть UAT-замечание owner (04.06): план должен показывать и рекомендованные задачи, и уроки единым плоским видом, визуально как Избранное; различие только в источнике (план = авто по диагностике; избранное = руками).

Output: plan/page.tsx с двумя плоскими блоками, без аккордеона, с бейджем приоритета на карточках уроков. План без сердечек (кнопки «Убрать»/«Обновить план» сохранены).

ВАЖНО: счётчик прогресса должен считать ТОЛЬКО видимые диагностические уроки (баг 6 уже починен в текущей версии — visibleTotal/visibleCompleted; сохранить эту логику). Источник уроков — секции errors/deepening/growth/advanced (allowlist DIAGNOSTIC_SECTION_IDS); секцию custom НЕ выводить (ручное живёт в Избранном). data-tour="learn-sections" сохранить на контейнере плана (тур getSteps детектит этот якорь).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61.1-learning-2-0-uat-fixes/61.1-CONTEXT.md

<interfaces>
getRecommendedPath (packages/api/src/routers/learning.ts @274) возвращает (sectioned, version 2):
  {
    generatedAt, isSectioned: true,
    sections: Array<{ id: 'errors'|'deepening'|'growth'|'advanced'|'custom', title, lessons: LessonData[] , hints? }>,
    lessons: LessonData[]   // плоский для backward compat
    totalLessons, completedLessons, hasPlatformSubscription,
    addedJobs: Array<{ id, slug, title, marketplace, lessons: LessonData[] }>
  }
  LessonData: { id, courseId, courseName, title, description, videoUrl, videoId, duration, order, skillCategory, skillLevel, status, watchedPercent, locked }
  Когда диагностики нет / формат старый → возвращает null или плоский (см. ветки @412+).

LessonCard (apps/web/src/components/learning/LessonCard.tsx):
  props: { lesson: LessonWithProgress, showCourse?, courseName?, isRecommended?, locked?, onRemoveFromTrack?, favorite? }
  — уже есть кнопка «Убрать» через onRemoveFromTrack; «сердечко» через favorite (в Плане НЕ передавать favorite).

JobCard (apps/web/src/components/learning/JobCard.tsx после 61.1-01): { job: JobSummary, initialFavorited? } в каталожном режиме рендерит сердечко.
  ВАЖНО: addedJobs из getRecommendedPath НЕ содержат полей JobSummary (нет description/totalDurationMin/isRecommended/isInTrack/completedLessons в нужном виде — есть только id/slug/title/marketplace/lessons[]). Для блока «Рекомендованные задачи» либо собрать минимальную карточку-ссылку на /learn/job/${slug} (заголовок + N уроков + прогресс по lessons[]), либо адаптировать в форму JobSummary. Выбрать вариант с минимальной карточкой-ссылкой (как FavoriteRow в favorites/page.tsx) чтобы не тянуть лишний запрос — НЕ показывать сердечко в Плане.

learning.removeFromTrack.useMutation({lessonId}) и learning.rebuildTrack.useMutation() — уже используются на странице, сохранить.

Бейджи приоритета (соответствие section.id → лейбл):
  errors → «Ошибка», deepening → «Углубление», growth → «Рост», advanced → «Продвинутый».
  Цвета взять из существующих SECTION_STYLES (errors=red, deepening=mp-blue, growth=mp-green, advanced=yellow).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Плоский список «Рекомендованные уроки» с бейджем приоритета</name>
  <files>apps/web/src/app/(main)/learn/plan/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/learn/plan/page.tsx (вся текущая страница: SECTION_STYLES @41-73, diagnosticSections @124-127, visibleTotal/visibleCompleted @135-146, аккордеон-рендер @282-357, removeFromTrackMutation @87-107, rebuildTrackMutation @109-115)
    - apps/web/src/components/learning/LessonCard.tsx (props, onRemoveFromTrack, isRecommended)
  </read_first>
  <action>
    Заменить аккордеон-блок (@282-357: `.filter(...).map(section => <Card>...accordion...)`) на единый плоский список. Логика:
    1. Сохранить diagnosticSections (allowlist errors/deepening/growth/advanced; custom исключён) и visibleTotal/visibleCompleted/прогресс-бар — НЕ менять счётчик (баг 6).
    2. Флэттенить уроки всех диагностических секций в один массив в порядке приоритета секций: errors → deepening → growth → advanced. Для каждого урока сохранить его section.id, чтобы вывести бейдж.
    3. Рендерить плоский список LessonCard (grid как в Избранном: `grid gap-3 sm:grid-cols-2 lg:grid-cols-3` или одноколоночный список — выбрать визуально-как-Избранное), с заголовком блока «Рекомендованные уроки».
    4. Приоритет показать как бейдж: добавить маленький лейбл над/в карточке («Ошибка»/«Углубление»/«Рост»/«Продвинутый») с цветом из SECTION_STYLES[section.id].badgeColor. Поскольку LessonCard не имеет слота для произвольного бейджа приоритета, обернуть каждую карточку в div с бейджем сверху (absolute или inline над карточкой), либо передать через существующий isRecommended только для errors (как было) и добавить отдельный бейдж-обёртку для остальных. Выбрать обёртку-бейдж над карточкой — единообразно для всех 4 приоритетов.
    5. onRemoveFromTrack оставить (мутация removeFromTrack по lessonId) — «Убрать» работает в плоском виде. НЕ передавать `favorite` проп (план без сердечек).
    6. Удалить state expandedSections/toggleSection и SECTION_DESCRIPTIONS, если они больше не используются после удаления аккордеона (проверить — SECTION_STYLES остаётся для цветов бейджей).
    7. Сохранить «Re-diagnostic CTA» (errors fully completed) и empty-state как есть.
    8. Контейнер плоского списка должен остаться с data-tour="learn-sections" (тур getSteps).
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `expandedSections\|toggleSection` apps/web/src/app/(main)/learn/plan/page.tsx → 0 совпадений (аккордеон убран)
    - grep `data-tour="learn-sections"` apps/web/src/app/(main)/learn/plan/page.tsx → ≥1 (тур не сломан)
    - В коде присутствуют лейблы бейджей: grep `Ошибка\|Углубление` → ≥1, grep `Продвинутый\|Рост` → ≥1
    - grep `onRemoveFromTrack` plan/page.tsx → ≥1 (кнопка «Убрать» сохранена); grep `rebuildTrack` → ≥1 («Обновить план» сохранён)
    - В plan/page.tsx НЕТ `favorite=` проп на LessonCard (план без сердечек)
    - visibleTotal/visibleCompleted сохранены (счётчик по видимым диагностическим урокам)
    - `npx tsc --noEmit` зелёный
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Блок «Рекомендованные задачи» из addedJobs</name>
  <files>apps/web/src/app/(main)/learn/plan/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/learn/plan/page.tsx (после Task 1)
    - apps/web/src/app/(main)/learn/favorites/page.tsx (паттерн FavoriteRow — минимальная карточка-ссылка с иконкой/заголовком)
    - getRecommendedPath addedJobs shape (см. interfaces: { id, slug, title, marketplace, lessons[] })
  </read_first>
  <behavior>
    - Когда addedJobs непустой → над блоком «Рекомендованные уроки» рендерится «Рекомендованные задачи» с карточками-ссылками /learn/job/${slug}.
    - Каждая карточка задачи показывает title, число уроков (lessons.length) и прогресс (completed = lessons.filter(status==='COMPLETED').length).
    - Когда addedJobs пуст → блок «Рекомендованные задачи» не рендерится (нет пустого заголовка).
    - В Плане у карточек задач НЕТ сердечка (отличие от Избранного).
    - planIsEmpty (нет диагностических уроков И нет addedJobs) → текущий empty-state.
  </behavior>
  <action>
    Добавить блок «Рекомендованные задачи» перед блоком «Рекомендованные уроки». Источник — recommendedPath.addedJobs. Для каждой задачи рендерить минимальную карточку-ссылку (паттерн FavoriteRow из favorites/page.tsx, но БЕЗ FavoriteButton): Link на `/learn/job/${job.slug}`, иконка (Wrench из lucide), заголовок job.title, подпись «N уроков · прогресс X/N» где N=job.lessons.length, X=число завершённых. Grid как у уроков. Скорректировать planIsEmpty: план НЕ пустой если есть диагностические уроки ИЛИ есть addedJobs (сейчас planIsEmpty завязан только на hasDiagnosticLessons — расширить на addedJobs.length). Заголовки action-кнопок «Обновить план»/«Продолжить» оставить как есть.
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `Рекомендованные задачи` plan/page.tsx → ≥1
    - grep `addedJobs` plan/page.tsx → ≥1 (рендерится)
    - grep `/learn/job/` plan/page.tsx → ≥1 (карточка-ссылка на задачу)
    - В блоке задач НЕТ FavoriteButton (план без сердечек): рядом с рендером addedJobs нет `<FavoriteButton`
    - planIsEmpty учитывает addedJobs.length (план не пуст при наличии задач без уроков)
    - `npx tsc --noEmit` зелёный
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` зелёный.
- Ручная проверка на staging для юзера с диагностикой: /learn/plan показывает «Рекомендованные задачи» (если есть addedJobs) + плоский «Рекомендованные уроки» с бейджами приоритета; аккордеона нет; «Убрать» и «Обновить план» работают; счётчик прогресса корректен.
- ВАЖНО: визуальный смысл блока «Рекомендованные задачи» на проде зависит от плана 61.1-03 (clean-up addedJobs). До clean-up addedJobs может содержать старые ручные добавления (дубль с Избранным). Это ожидаемо; код плана корректен в обоих состояниях. Порядок выкатки задан в 61.1-03 runbook.
</verification>

<success_criteria>
- План плоский (без аккордеона), показывает задачи + уроки.
- Приоритет уроков виден бейджем.
- Счётчик/кнопки/empty-state не сломаны; тур-якорь сохранён.
- typecheck зелёный.
</success_criteria>

<output>
Create `.planning/phases/61.1-learning-2-0-uat-fixes/61.1-02-SUMMARY.md` when done
</output>
