---
phase: "61.1"
plan: "03"
type: execute
wave: 2
depends_on: ["61.1-01"]
autonomous: false
requirements: [W1b]
files_modified:
  - scripts/cleanup-added-jobs.ts
  - scripts/__tests__/cleanup-added-jobs.test.ts

must_haves:
  truths:
    - "Существует идемпотентный скрипт, очищающий ручные (не-диагностические) записи из LearningPath.addedJobs на проде"
    - "Скрипт имеет --dry-run (только отчёт) и --apply (запись) режимы; повторный --apply не меняет состояние (идемпотентность)"
    - "Скрипт НИКОГДА не пишет LessonProgress (read-only count для before/after инварианта)"
    - "Применение на проде — за owner-gated blocking checkpoint, через Supabase Management API (additive/safe SQL), со снапшотом до и верификацией после"
    - "Чистая функция выбора записей к очистке покрыта unit-тестом"
  artifacts:
    - path: "scripts/cleanup-added-jobs.ts"
      provides: "pure-функция + CLI clean-up addedJobs (dry-run/apply, idempotent, LessonProgress read-only)"
    - path: "scripts/__tests__/cleanup-added-jobs.test.ts"
      provides: "unit-тесты чистой функции (идемпотентность, выбор записей)"
  key_links:
    - from: "cleanup-added-jobs.ts"
      to: "LearningPath.addedJobs (prod Supabase saecuecevicwjkpmaoot)"
      via: "Supabase Management API query endpoint (owner-gated)"
      pattern: "addedJobs"
---

<objective>
Разовый идемпотентный clean-up `LearningPath.addedJobs` на проде. Миграция migrate-track-to-favorites уже скопировала старые `addedJobs` в Favorite(JOB), но НЕ очистила сам `addedJobs` → задвоение (одна и та же задача и в Плане как «рекомендованная», и в Избранном). Базовое решение owner (модель A): очистить из `addedJobs` все ручные старые записи, оставив план наполняться только новыми диагностическими добавлениями через RecommendedJobsBlock.

Purpose: Убрать задвоение «задача в Плане + в Избранном» после редизайна Плана (61.1-02). Без clean-up блок «Рекомендованные задачи» на /learn/plan покажет старые ручные задачи, которые уже лежат в Избранном.

Output: безопасный скрипт scripts/cleanup-added-jobs.ts (dry-run/apply, идемпотентный, LessonProgress нетронут) + unit-тест + runbook применения на проде за owner-gated checkpoint.

⚠ КРИТИЧНО — PROD DATABASE SAFETY (CLAUDE.md): целевая БД — shared prod Supabase `saecuecevicwjkpmaoot` (158 платящих, 124 подписки). Никакого `prisma db push`, `--accept-data-loss`, DROP/ALTER/TRUNCATE. Только UPDATE конкретной колонки addedJobs конкретных строк, через Supabase Management API query endpoint, ПОСЛЕ снапшота и за blocking-human checkpoint с явным approve owner.

РЕШЕНИЕ ПО СТРАТЕГИИ ОЧИСТКИ (зафиксировать при исполнении, по умолчанию — вариант базовый owner): по умолчанию `addedJobs` очищается ПОЛНОСТЬЮ (`addedJobs = []`) для всех строк — всё старое уже в Favorite, дальше план наполняется заново через диагностику/RecommendedJobsBlock. Это самый простой и проверяемый инвариант. Если owner на checkpoint попросит сохранить диагностические задачи — реализовать фильтр, но базовый план = полная очистка.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61.1-learning-2-0-uat-fixes/61.1-CONTEXT.md
@scripts/migrate-track-to-favorites.ts
@~/.claude/projects/D--GpT-docs-MPSTATS-ACADEMY-ADAPTIVE-LEARNING-MAAL/memory/reference_supabase_migration_via_mgmt_api.md

<interfaces>
migrate-track-to-favorites.ts (аналог-паттерн): pure collectFavoriteRows() + migrate(prisma,{apply}); PrismaClient лениво require внутри main() (тест импортирует pure-функции под api vitest harness без @prisma/client в module graph); createMany({skipDuplicates}); lessonProgress.count() только для before/after инварианта.

LearningPath: { userId, lessons: Json, addedJobs: Json (string[] jobId) }.
addedJobs пишется addJobToTrack (ручное добавление задачи) И RecommendedJobsBlock (диагностические рекомендации тоже идут через addJobToTrack). Поэтому различить «ручное» vs «диагностическое» по самому addedJobs нельзя → базовое решение owner: очистить всё (всё старое уже в Favorite).

Supabase Management API (reference_supabase_migration_via_mgmt_api.md):
  POST https://api.supabase.com/v1/projects/{ref}/database/query  body {"query":"..."} — raw SQL, rows как JSON. Использовался в Phase 56/60 для additive prod-операций (VPS без pnpm/prisma).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Скрипт clean-up + unit-тест (dry-run/apply, идемпотентность)</name>
  <files>scripts/cleanup-added-jobs.ts, scripts/__tests__/cleanup-added-jobs.test.ts</files>
  <read_first>
    - scripts/migrate-track-to-favorites.ts (паттерн: pure-функция + migrate(prisma,{apply}) + ленивый require PrismaClient + lessonProgress.count инвариант + CLI с --dry-run/--apply)
    - packages/api vitest include (по 61-00: расширен на `../../scripts/__tests__/**/*.test.ts` — тест бежит под node-харнессом api)
  </read_first>
  <behavior>
    - planRowsToClear(rows): чистая функция, принимает строки [{userId, addedJobs}], возвращает список userId, у которых addedJobs непустой (кандидаты на очистку до []). Идемпотентна: повторный прогон над уже очищёнными ([]) строками возвращает пустой список.
    - cleanup(prisma,{apply}): считает кандидатов; при apply=false — только отчёт; при apply=true — обновляет addedJobs=[] для кандидатов; читает lessonProgress.count() до и после (должны совпасть; никогда не пишет LessonProgress).
    - Повторный cleanup --apply после первого → 0 обновлённых строк (идемпотентность).
  </behavior>
  <action>
    Создать scripts/cleanup-added-jobs.ts по образу migrate-track-to-favorites.ts:
    - export interface CleanupPrisma c минимальной поверхностью: learningPath.findMany({select:{userId,addedJobs}}), learningPath.update / updateMany по userId с data {addedJobs: []}, lessonProgress.count().
    - export function planRowsToClear(rows): возвращает userId[] где Array.isArray(addedJobs) && addedJobs.length > 0. Идемпотентна (пустые addedJobs не попадают).
    - export async function cleanup(prisma,{apply?}): lessonProgressBefore=count(); rows=findMany; ids=planRowsToClear(rows); если apply && ids.length → updateMany({where:{userId:{in:ids}}, data:{addedJobs:[]}}) (или per-row update если updateMany по json недоступен — выбрать updateMany); lessonProgressAfter=count(); вернуть {users, candidates: ids.length, updated, lessonProgressBefore, lessonProgressAfter}. Бросить ошибку если before!==after.
    - CLI main() с --dry-run/--apply, ленивый require('@prisma/client'), $disconnect в finally, абортить если before!==after. Печатать отчёт (кандидаты / обновлено / LessonProgress before=after).
    Создать scripts/__tests__/cleanup-added-jobs.test.ts (vitest): импортировать pure-функции и cleanup со stub-prisma. Тесты: (1) planRowsToClear возвращает только userId с непустым addedJobs; (2) идемпотентность — после «очистки» (stub возвращает []) planRowsToClear → []; (3) cleanup с apply:false не вызывает update; (4) cleanup с apply:true вызывает updateMany ровно по кандидатам; (5) lessonProgress.count вызывается дважды и не пишется. Stub НЕ должен импортировать @prisma/client.
  </action>
  <verify>
    <automated>cd "packages/api" && pnpm test cleanup-added-jobs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - scripts/cleanup-added-jobs.ts существует, экспортирует planRowsToClear и cleanup
    - grep `lessonProgress` scripts/cleanup-added-jobs.ts → присутствует только .count (нет .update/.create/.delete на lessonProgress)
    - grep `accept-data-loss\|db push\|DROP\|TRUNCATE\|ALTER` scripts/cleanup-added-jobs.ts → 0 совпадений
    - тест scripts/__tests__/cleanup-added-jobs.test.ts зелёный (включая идемпотентность)
    - ленивый require('@prisma/client') внутри main(), не top-level
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Применение clean-up на проде (owner-gated, Mgmt API)</name>
  <what-built>
    Скрипт scripts/cleanup-added-jobs.ts (dry-run/apply, идемпотентный, LessonProgress read-only) + unit-тесты зелёные. Эквивалентный SQL для применения через Supabase Management API:
      SELECT count(*) FROM "LearningPath" WHERE jsonb_array_length("addedJobs") > 0;   -- снапшот числа кандидатов
      UPDATE "LearningPath" SET "addedJobs" = '[]'::jsonb WHERE jsonb_array_length("addedJobs") > 0;  -- очистка
  </what-built>
  <how-to-verify>
    Процедура (выполняет owner или агент по approve owner; цель — shared prod Supabase `saecuecevicwjkpmaoot`):
    1. СНАПШОТ ДО: через Supabase Mgmt API query endpoint выполнить
       `SELECT count(*) AS total, count(*) FILTER (WHERE jsonb_array_length("addedJobs") > 0) AS with_jobs FROM "LearningPath";`
       Записать числа. Дополнительно зафиксировать count Favorite(JOB): `SELECT count(*) FROM "Favorite" WHERE "itemType"='JOB';` (должен НЕ уменьшиться после clean-up — Favorite не трогаем).
    2. DRY-RUN: либо `npx tsx scripts/cleanup-added-jobs.ts --dry-run` против prod DATABASE_URL (если запускается локально с прод-строкой), либо просто SELECT-проверка п.1. Сверить число кандидатов.
    3. APPLY: `UPDATE "LearningPath" SET "addedJobs" = '[]'::jsonb WHERE jsonb_array_length("addedJobs") > 0;` через Mgmt API (или `npx tsx scripts/cleanup-added-jobs.ts --apply`).
    4. ВЕРИФИКАЦИЯ ПОСЛЕ:
       - `SELECT count(*) FILTER (WHERE jsonb_array_length("addedJobs") > 0) FROM "LearningPath";` → должно быть 0.
       - `SELECT count(*) FROM "Favorite" WHERE "itemType"='JOB';` → не меньше, чем до (Favorite не пострадал).
       - `SELECT count(*) FROM "LessonProgress";` → равно значению до (инвариант D-03/D-07).
    5. ИДЕМПОТЕНТНОСТЬ: повторный UPDATE → 0 затронутых строк.
    Безопасность: только UPDATE одной json-колонки addedJobs по фильтру; никаких DROP/ALTER/TRUNCATE/accept-data-loss. Favorite и LessonProgress не пишутся.
  </how-to-verify>
  <resume-signal>Owner подтверждает approve на запуск и сообщает «cleaned» с числами before/after (with_jobs до → 0, LessonProgress unchanged, Favorite(JOB) unchanged)</resume-signal>
</task>

</tasks>

<verification>
- unit-тесты cleanup-added-jobs зелёные (включая идемпотентность и LessonProgress-инвариант).
- На проде после apply: 0 строк LearningPath с непустым addedJobs; LessonProgress count неизменен; Favorite(JOB) count неизменен.
- Порядок выкатки (runbook): код 61.1-01/02/04 уезжает на прод в одном релизе → ПОТОМ clean-up addedJobs на проде. Если clean-up сделать ДО деплоя кода 61.1-02 — план временно покажет пустой блок задач, что безопасно. Окончательно: clean-up можно применять в любой момент после того, как код Плана (61.1-02) перестал зависеть от старого addedJobs-смысла; рекомендуется сразу после prod-деплоя кода фазы.
</verification>

<success_criteria>
- Идемпотентный безопасный скрипт + тесты.
- Прод-применение за owner-gated checkpoint со снапшотом/верификацией.
- addedJobs очищен, Favorite и LessonProgress не пострадали.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → shared prod Supabase | разовая запись в prod БД, общую с 158 платящими юзерами |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-03-01 | Tampering | UPDATE addedJobs на prod | mitigate | только UPDATE одной json-колонки по фильтру; снапшот до + верификация после; blocking-human checkpoint с approve owner |
| T-61.1-03-02 | Denial of Service | случайный DROP/TRUNCATE/accept-data-loss | mitigate | grep-гейт запрещает эти токены в скрипте; запуск только через Mgmt API query endpoint конкретным UPDATE; PROD DATABASE SAFETY правила CLAUDE.md |
| T-61.1-03-03 | Tampering | побочная запись в LessonProgress/Favorite | mitigate | LessonProgress только .count (инвариант before==after); Favorite не упоминается в скрипте; верификация count Favorite(JOB) после |
</threat_model>

<output>
Create `.planning/phases/61.1-learning-2-0-uat-fixes/61.1-03-SUMMARY.md` when done
</output>
