---
phase: "61.1"
plan: "04"
type: execute
wave: 3
depends_on: ["61.1-01", "61.1-02"]
autonomous: true
requirements: [W2]
files_modified:
  - apps/web/src/components/learning/JobCatalog.tsx
  - apps/web/src/components/learning/JobCard.tsx
  - apps/web/src/components/learning/SearchResultCard.tsx
  - apps/web/src/components/learning/LessonCard.tsx
  - apps/web/src/components/learning/DiagnosticGateBanner.tsx
  - apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx
  - apps/web/src/app/(main)/learn/job/[slug]/page.tsx

must_haves:
  truths:
    - "В живых компонентах обучения нет пользовательских строк «джоб(а/у/ы)», «плейбук», «трек» в значении «джоба/трек обучения» — заменены на «задача»/«решение»/«план»"
    - "RecommendedJobsBlock (диагностика) говорит «план», а не «трек»; функционально не сломан (addJobToTrack остаётся)"
    - "JobCatalog показывает «N задач» вместо «N джоб», пустое состояние про задачи"
    - "Прототипы design-* и юридические страницы НЕ затронуты"
  artifacts:
    - path: "apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx"
      provides: "диагностический блок с терминологией задача/план"
  key_links:
    - from: "живые learn-компоненты"
      to: "пользователь (UI-строки)"
      via: "канон терминологии D-02 (джоба→задача, трек→план)"
      pattern: "задач"
---

<objective>
W2 — добивка нейминга джоба→задача / трек→план по живым компонентам обучения. Большая часть строк «В трек»/«В треке» исчезает вместе с 61.1-01 (track-кнопки задач удалены) и 61.1-02 (аккордеон плана заменён). Этот план зачищает остаток: счётчики/заголовки/тосты/тултипы в каталоге, диагностическом блоке, карточках поиска и LessonCard.

Purpose: Единая терминология по канону D-02 (задача, решение, план) во всём живом разделе обучения; убрать остаточные «джоба/плейбук/трек».

Output: согласованные UI-строки в JobCatalog, JobCard, SearchResultCard, LessonCard, DiagnosticGateBanner, RecommendedJobsBlock, job/[slug].

Граница: НЕ трогать прототипы `apps/web/src/app/**/design-*` и юр-страницы (оферта/политики). НЕ трогать имена идентификаторов кода (addJobToTrack, addedJobs, isInTrack, onRemoveFromTrack, data-tour="learn-add-to-track" и т.п.) — только пользовательские строки и комментарии где уместно. RecommendedJobsBlock переименовать слова, но оставить addJobMutation/addJobToTrack рабочими.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61.1-learning-2-0-uat-fixes/61.1-CONTEXT.md

<interfaces>
Канон D-02 (из 61-02): джоба→задача, плейбук→решение, трек→план, «Все курсы»→«База знаний».
Конкретные цели (по grep живых компонентов на момент планирования; часть уйдёт в 61.1-01/02 — перепроверить):
  JobCatalog.tsx:48 «Под фильтр джоб не нашлось.» → «Под фильтр задач не нашлось.»
  JobCatalog.tsx:58 «{N} джоб» → «{N} задач»
  DiagnosticGateBanner.tsx:38 «соберём персональный трек» → «…персональный план»
  RecommendedJobsBlock.tsx:41 «Плейбук в треке» → «Решение в плане»
  RecommendedJobsBlock.tsx:60 «Добавлено в трек: N» → «Добавлено в план: N»
  RecommendedJobsBlock.tsx:73 «Рекомендованные плейбуки» → «Рекомендованные задачи»
  RecommendedJobsBlock.tsx:75 «Начни с плейбука №1 …» → «Начни с задачи №1 …»
  RecommendedJobsBlock.tsx:85 «Добавить все N в трек» → «Добавить все N в план»
  RecommendedJobsBlock.tsx:62 router.push('/learn/track') → '/learn/plan' (старый роут редиректит, но лучше прямой)
  SearchResultCard.tsx:39 «В вашем треке» → «В вашем плане»
  LessonCard.tsx:189/196/203/218 тултипы и лейблы «трек» → «план» (onToggleTrack-ветка): «Уже в твоём треке»→«Уже в твоём плане», «Добавить в твой трек обучения»→«…план обучения», «В треке»→«В плане», «В трек»→«В план», «Убрать из трека»→«Убрать из плана»
  job/[slug]/page.tsx (после 61.1-01 остаются): «Продолжить джобу →» → «Продолжить задачу →», «Уроки джобы — по порядку» → «Уроки задачи — по порядку»

JobCard.tsx:40/71 («В треке» бейдж, «+ В трек» кнопка) — если 61.1-01 уже убрал каталожный показ, остаётся лейбл кнопки в onAddToTrack-ветке (диагностика): «+ В трек» → «+ В план».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Терминология в каталоге, поиске, LessonCard, DiagnosticGateBanner, JobCard</name>
  <files>apps/web/src/components/learning/JobCatalog.tsx, apps/web/src/components/learning/SearchResultCard.tsx, apps/web/src/components/learning/LessonCard.tsx, apps/web/src/components/learning/DiagnosticGateBanner.tsx, apps/web/src/components/learning/JobCard.tsx</files>
  <read_first>
    - apps/web/src/components/learning/JobCatalog.tsx (@48 пустое состояние, @58 счётчик «N джоб»)
    - apps/web/src/components/learning/SearchResultCard.tsx (@39 «В вашем треке»)
    - apps/web/src/components/learning/LessonCard.tsx (@189/196/203/218 onToggleTrack-ветка, тултипы/лейблы)
    - apps/web/src/components/learning/DiagnosticGateBanner.tsx (@38 «персональный трек»)
    - apps/web/src/components/learning/JobCard.tsx (после 61.1-01: лейбл кнопки в onAddToTrack-ветке)
  </read_first>
  <action>
    Заменить пользовательские строки по канону (см. interfaces). НЕ менять идентификаторы кода/props/data-tour. В LessonCard трогать только видимые строки в onToggleTrack-ветке (трек→план). В JobCard заменить лейбл кнопки «+ В трек» → «+ В план» (кнопка осталась только в onAddToTrack/диагностическом использовании после 61.1-01) и, если остался бейдж «В треке», его текст → «В плане» (или убедиться, что 61.1-01 его удалил). Сверить актуальное состояние grep'ом — часть строк могла уйти в 61.1-01/02.
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^\s*//\|^\s*\*' для пользовательских строк: в JobCatalog/SearchResultCard/DiagnosticGateBanner нет «джоб», «трек» (в значении трек обучения), «плейбук»
    - grep `N задач\|задач не нашлось\|} задач` JobCatalog.tsx → ≥1
    - grep `персональный план` DiagnosticGateBanner.tsx → ≥1; grep `персональный трек` → 0
    - grep `В вашем плане` SearchResultCard.tsx → ≥1
    - LessonCard onToggleTrack-ветка: видимые лейблы «В план»/«В плане», тултипы про «план» (grep `В треке\|в твоём треке\|Убрать из трека` → 0)
    - `npx tsc --noEmit` зелёный
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Терминология в RecommendedJobsBlock и job/[slug]</name>
  <files>apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx, apps/web/src/app/(main)/learn/job/[slug]/page.tsx</files>
  <read_first>
    - apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx (@41/60/73/75/85 строки, @62 router.push('/learn/track'))
    - apps/web/src/app/(main)/learn/job/[slug]/page.tsx (после 61.1-01: остаются «Продолжить джобу» @~59, «Уроки джобы» @~98)
  </read_first>
  <behavior>
    - RecommendedJobsBlock: все видимые строки говорят «задача»/«решение»/«план»; функционально add-в-план через addJobMutation работает; bulk-навигация ведёт на /learn/plan.
    - job/[slug]: «Продолжить задачу», «Уроки задачи — по порядку».
  </behavior>
  <action>
    В RecommendedJobsBlock.tsx заменить: «Плейбук в треке»→«Решение в плане»; «Добавлено в трек: N»→«Добавлено в план: N»; «Рекомендованные плейбуки»→«Рекомендованные задачи»; «Начни с плейбука №1 …»→«Начни с задачи №1 …»; «Добавить все N в трек»→«Добавить все N в план»; router.push('/learn/track')→router.push('/learn/plan'). Идентификаторы (addJobMutation, addJobToTrack, isInTrack, notInTrack) НЕ трогать.
    В job/[slug]/page.tsx: «Продолжить джобу →»→«Продолжить задачу →»; «Уроки джобы — по порядку»→«Уроки задачи — по порядку».
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `плейбук\|Плейбук` RecommendedJobsBlock.tsx → 0; grep `в треке\|в трек:` (строки) → 0
    - grep `Рекомендованные задачи` RecommendedJobsBlock.tsx → ≥1
    - grep `/learn/track` RecommendedJobsBlock.tsx → 0 (навигация на /learn/plan)
    - grep `джобу\|джобы` job/[slug]/page.tsx → 0; grep `Продолжить задачу\|Уроки задачи` → ≥1
    - `npx tsc --noEmit` зелёный
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Греп по живым learn-компонентам (исключая design-* и юр-страницы): нет пользовательских «джоб»/«плейбук»/«трек(обучения)».
- `cd apps/web && npx tsc --noEmit` зелёный.
- Прототипы design-* и оферта/политики не в diff.
</verification>

<success_criteria>
- Единая терминология (задача/решение/план) в живом разделе обучения.
- Диагностический блок и детальная задача переименованы; функциональность цела.
- typecheck зелёный.
</success_criteria>

<output>
Create `.planning/phases/61.1-learning-2-0-uat-fixes/61.1-04-SUMMARY.md` when done
</output>
