---
phase: "61.1"
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [W1]
files_modified:
  - apps/web/src/components/learning/AgentSearch.tsx
  - apps/web/src/components/learning/JobCard.tsx
  - apps/web/src/app/(main)/learn/job/[slug]/page.tsx

must_haves:
  truths:
    - "В блоке «Решения» (AgentSearch recommend) у задачи стоит сердечко (FavoriteButton JOB), кнопки «Положить в трек»/«В план» нет"
    - "На карточке задачи (JobCard) в каталоге нет бейджа «В треке» и кнопки «+ В трек» — только сердечко"
    - "На детальной странице задачи нет кнопок «+ В трек»/«В треке ✓» — есть сердечко; track-мутации (addJobToTrack/removeJobFromTrack) со страницы не вызываются"
    - "Отмеченная сердечком задача появляется в разделе «Задачи»/«Решения» на странице Избранного"
    - "RecommendedJobsBlock (диагностический флоу) функционально не сломан — задачи по-прежнему уходят в план через addJobToTrack"
  artifacts:
    - path: "apps/web/src/components/learning/AgentSearch.tsx"
      provides: "recommend-результаты задач с FavoriteButton вместо track-кнопки"
    - path: "apps/web/src/components/learning/JobCard.tsx"
      provides: "карточка задачи без ручной track-механики, только сердечко"
    - path: "apps/web/src/app/(main)/learn/job/[slug]/page.tsx"
      provides: "детальная задача с сердечком вместо track-кнопок"
  key_links:
    - from: "AgentSearch recommend / JobCard / JobDetail"
      to: "trpc.favorite.add/remove (через FavoriteButton)"
      via: "FavoriteButton itemType=JOB"
      pattern: "FavoriteButton[^>]*itemType=[\"']JOB[\"']"
    - from: "JobCard / JobDetail / AgentSearch"
      to: "trpc.favorite.isFavorited"
      via: "seed initialFavorited"
      pattern: "favorite\\.isFavorited"
---

<objective>
W1 — заменить ручную «трек»-механику у задач на сердечко (Избранное) в трёх живых поверхностях: recommend-результаты AgentSearch («Решения»), карточка задачи JobCard, детальная страница задачи. Ручное добавление задачи теперь всегда → Избранное (модель A). Это попутно закрывает баг 2а (общий isPending на 3 кнопки) и 2.1 (цвет) — конфликтная кнопка уходит вообще.

Purpose: Зафиксировать решение owner: персональный план строится только на диагностике, любое ручное добавление задачи управляется сердечком и попадает в Избранное.

Output: AgentSearch / JobCard / JobDetail без ручных track-кнопок; везде FavoriteButton(JOB) с seed-состоянием через favorite.isFavorited.

ВАЖНО (граница скоупа): RecommendedJobsBlock (компонент диагностического результата) НЕ трогаем функционально в этом плане — диагностические задачи остаются в Плане через addJobToTrack. Переименование слова «трек→план» в RecommendedJobsBlock делается в плане 61.1-04 (W2). НЕ удаляйте addJobToTrack/removeJobFromTrack из роутера learning.ts — их продолжает использовать RecommendedJobsBlock и rebuild-флоу.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61.1-learning-2-0-uat-fixes/61.1-CONTEXT.md
@.planning/phases/61-learning-2-0/61.1-UAT-FIXES-SCOPE.md

<interfaces>
FavoriteButton (apps/web/src/components/learning/FavoriteButton.tsx) — уже существует:
  props: { itemType: 'LESSON'|'JOB'|'MATERIAL'; itemId: string; initialFavorited?: boolean; className?: string }
  — оптимистичный toggle favorite.add/remove, внутри Link делает preventDefault/stopPropagation, mp-pink-500 filled = saved.

favorite.isFavorited (packages/api/src/routers/favorite.ts):
  input: { items: Array<{ itemType, itemId }> }  (max 500)
  output: { favorited: string[] }  где элемент = `${itemType}:${itemId}` (например `JOB:clx123`)

JobSummary (packages/shared): { id, slug, title, description, marketplace, lessonCount, completedLessons, totalDurationMin, isRecommended, isInTrack }
IntentResult recommend.jobs[]: { jobId, slug, title, reason, lessonCount, actions: [{label}] }

JobCatalog (apps/web/src/components/learning/JobCatalog.tsx) уже batch-сидит initialFavorited через favorite.isFavorited и передаёт в JobCard — НЕ трогаем JobCatalog в этом плане, только JobCard убирает track-UI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JobCard и JobDetail — убрать track-механику, оставить сердечко</name>
  <files>apps/web/src/components/learning/JobCard.tsx, apps/web/src/app/(main)/learn/job/[slug]/page.tsx</files>
  <read_first>
    - apps/web/src/components/learning/JobCard.tsx (текущая разметка: бейдж «В треке» @39-44, FavoriteButton уже смонтирован @48, кнопка «+ В трек» @60-73)
    - apps/web/src/app/(main)/learn/job/[slug]/page.tsx (addToTrack/removeFromTrack мутации @16-31, кнопки «+ В трек»/«В треке ✓» @62-80)
    - apps/web/src/components/learning/FavoriteButton.tsx (контракт props)
    - packages/api/src/routers/favorite.ts (isFavorited output shape)
    - apps/web/src/components/diagnostic/RecommendedJobsBlock.tsx (caller JobCard через onAddToTrack — НЕ должен сломаться по типам)
  </read_first>
  <behavior>
    - JobCard, вызванный без onAddToTrack (каталог): не рендерит кнопку «+ В трек», не рендерит бейдж «В треке»; рендерит сердечко.
    - JobCard, вызванный с onAddToTrack (RecommendedJobsBlock диагностики): кнопка добавления остаётся (диагностический флоу не ломается). Бейдж «В треке» при этом не показывается (он относился к ручному треку).
    - JobDetail: при любом состоянии задачи нет track-кнопок; есть сердечко, seed-состояние из job.isInTrack заменено на favorite.isFavorited(JOB, job.id).
    - JobDetail: addJobToTrack/removeJobFromTrack больше не импортируются и не вызываются со страницы.
  </behavior>
  <action>
    В JobCard.tsx: удалить блок бейджа «В треке» (оставить бейджи WB+Ozon и «Рекомендовано диагностикой» как есть — последний относится к диагностике). FavoriteButton оставить на месте (@48). Кнопку «+ В трек» (`onAddToTrack && !job.isInTrack`) НЕ удалять полностью — она нужна RecommendedJobsBlock; оставить рендер кнопки только когда передан onAddToTrack. Главное требование плана: в каталожном использовании (JobCatalog передаёт JobCard БЕЗ onAddToTrack) кнопка и бейдж «В треке» не появляются. Лейбл кнопки оставить «+ В трек» — переименование слова делается в 61.1-04.

    В job/[slug]/page.tsx: удалить useMutation addToTrack и removeFromTrack (@16-31) и весь блок track-кнопок (@62-80). Вместо них в правой колонке action-блока (`flex flex-col gap-2 shrink-0`) под кнопкой «Продолжить» смонтировать FavoriteButton itemType="JOB" itemId={job.id} initialFavorited={favorited}. Состояние favorited получить через trpc.favorite.isFavorited.useQuery({ items: [{ itemType: 'JOB', itemId: job.id }] }, { enabled: !!job }) и проверить вхождение `JOB:${job.id}` в data.favorited. ВСЕ useQuery/useMutation/useUtils держать ВЫШЕ любого early return (`if (isLoading)` / `notFound()`) — Rules of Hooks (CLAUDE.md gotcha, инцидент 72facc9). Импорт addJobToTrack/removeJobFromTrack убрать. Тексты «Продолжить джобу»/«Уроки джобы» НЕ трогать здесь — это 61.1-04.
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `В треке` apps/web/src/app/(main)/learn/job/[slug]/page.tsx → 0 совпадений
    - grep `addJobToTrack\|removeJobFromTrack` apps/web/src/app/(main)/learn/job/[slug]/page.tsx → 0 совпадений
    - grep `FavoriteButton[^>]*itemType=["']JOB["']` apps/web/src/app/(main)/learn/job/[slug]/page.tsx → ≥1 совпадение
    - grep `favorite.isFavorited` apps/web/src/app/(main)/learn/job/[slug]/page.tsx → ≥1 совпадение
    - JobCard: при отсутствии onAddToTrack в JSX нет кнопки «+ В трек» и нет бейджа «В треке» (визуально/в коде guard на onAddToTrack)
    - `npx tsc --noEmit` в apps/web проходит без новых ошибок
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AgentSearch recommend — сердечко вместо track-кнопки</name>
  <files>apps/web/src/components/learning/AgentSearch.tsx</files>
  <read_first>
    - apps/web/src/components/learning/AgentSearch.tsx (addJobMutation @40-49, trackedJobIds @34-37, recommend-рендер с кнопкой «В план» @170-195, общий isPending @108)
    - apps/web/src/components/learning/FavoriteButton.tsx
    - packages/api/src/routers/favorite.ts (isFavorited)
  </read_first>
  <action>
    В AgentSearch.tsx удалить addJobMutation (@40-49) и производный trackedJobIds/recommendedPath-query, если они больше нигде не используются после правки (проверить: recommendedPath.getRecommendedPath нужен только для track-состояния recommend — после замены на сердечко он не нужен; убрать query и useMemo trackedJobIds). В recommend-ветке (`result?.mode === 'recommend'`) заменить кнопку добавления (`addJobMutation.mutate`) на FavoriteButton itemType="JOB" itemId={j.jobId}. Seed initialFavorited: после получения recommend-результата собрать jobIds и одним batch-запросом trpc.favorite.isFavorited.useQuery по этим id (enabled при наличии recommend-jobs), передать в каждую карточку через `favoritedSet.has(\`JOB:${j.jobId}\`)`. Карточка остаётся ссылкой `/learn/job/${j.slug}` + reason + lessonCount; сердечко в правом верхнем углу карточки (className `-mt-2 -mr-2 shrink-0`, паттерн как в JobCard). Баг 2а (общий isPending) закрывается тем, что у каждой карточки теперь свой независимый FavoriteButton, а не общая мутация. Слово в тексте answer не трогаем — это 61.1-04.
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `addJobMutation\|addJobToTrack` apps/web/src/components/learning/AgentSearch.tsx → 0 совпадений
    - grep `FavoriteButton[^>]*itemType=["']JOB["']` apps/web/src/components/learning/AgentSearch.tsx → ≥1 совпадение
    - grep `favorite.isFavorited` apps/web/src/components/learning/AgentSearch.tsx → ≥1 совпадение
    - В recommend-ветке нет ни одной кнопки с `disabled={isAdded || ...}` (старая track-кнопка удалена)
    - `npx tsc --noEmit` в apps/web проходит без новых ошибок
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` — без новых ошибок типов.
- `cd packages/api && pnpm test` — favorite/learning тесты зелёные (мы не меняли роутеры, регрессии быть не должно).
- Ручная проверка на staging: каталог задач (/learn/library lens «По задачам» или /learn/solutions) — у карточек задач только сердечко; детальная задача — только сердечко; «Решения» поиск recommend — у каждой задачи своё сердечко; отметка сердечком → задача в /learn/favorites раздел «Решения».
</verification>

<success_criteria>
- Ни одна ручная (не-диагностическая) поверхность задач не показывает «трек»-кнопку.
- FavoriteButton(JOB) смонтирован в AgentSearch recommend, JobCard (каталог), JobDetail.
- RecommendedJobsBlock компилируется и работает (диагностический add не сломан).
- typecheck зелёный.
</success_criteria>

<output>
Create `.planning/phases/61.1-learning-2-0-uat-fixes/61.1-01-SUMMARY.md` when done
</output>
