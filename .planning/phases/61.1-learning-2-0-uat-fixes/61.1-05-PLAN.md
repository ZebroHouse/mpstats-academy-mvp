---
phase: "61.1"
plan: "05"
type: execute
wave: 4
depends_on: ["61.1-01", "61.1-04"]
autonomous: true
requirements: [W3]
files_modified:
  - packages/api/src/routers/job.ts
  - packages/api/src/routers/__tests__/job.test.ts
  - apps/web/src/app/(main)/learn/job/[slug]/page.tsx
  - apps/web/src/app/(main)/learn/[id]/page.tsx

must_haves:
  truths:
    - "Урок, открытый из детальной страницы задачи, получает URL /learn/[id]?from=job:<slug>"
    - "Страница урока читает ?from=job:<slug> и показывает хлебные крошки «Решения под задачу / [Название задачи] / Урок» вместо стандартных «Обучение / Курс / Урок»"
    - "Название задачи в крошках резолвится по slug (лёгкий запрос), при отсутствии from — стандартные крошки"
    - "Существует лёгкая tRPC-процедура резолва названия задачи по slug (без тяжёлого getJob)"
  artifacts:
    - path: "packages/api/src/routers/job.ts"
      provides: "job.getTitleBySlug — лёгкий резолв {slug,title} для крошек"
      contains: "getTitleBySlug"
    - path: "packages/api/src/routers/__tests__/job.test.ts"
      provides: "unit-тесты getTitleBySlug (published → title, unpublished → null, not found → null)"
    - path: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      provides: "контекст-зависимые хлебные крошки по ?from=job"
  key_links:
    - from: "job/[slug] список уроков задачи"
      to: "/learn/[id]?from=job:<slug>"
      via: "Link href с query"
      pattern: "from=job:"
    - from: "learn/[id] breadcrumb"
      to: "job.getTitleBySlug"
      via: "useQuery при наличии from"
      pattern: "getTitleBySlug"
---

<objective>
W3 (баг 4) — 2-й уровень навигации: урок, открытый из задачи, сохраняет контекст задачи. Сейчас урок из задачи теряет контекст и показывает крошки «Обучение / Курс / Урок». Цель: ссылки на уроки из детальной задачи несут `?from=job:<slug>`; страница урока читает этот параметр и показывает крошки «Решения под задачу / [Задача] / Урок».

Purpose: Пользователь, идущий по решению (задаче), не выпадает из контекста — крошки и возврат ведут обратно к задаче, а не в общий каталог курса.

Output: lite-резолвер названия задачи (job.getTitleBySlug), unit-тесты резолвера, ссылки уроков задачи с from-параметром, контекст-зависимый breadcrumb на странице урока.

Граница: prev/next внутри задачи — опционально и НЕ в скоупе этого плана (только крошки + ссылки). Стандартное поведение урока (без from) не меняется.

Зависимость по файлам: job/[slug]/page.tsx правят также 61.1-01 (убирает track-кнопки) и 61.1-04 (нейминг). Этот план идёт ПОСЛЕ них (Wave 4) — добавляет только from-параметр к ссылкам уроков, поверх уже переименованных строк. Не пересекаться с их правками.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61.1-learning-2-0-uat-fixes/61.1-CONTEXT.md

<interfaces>
job/[slug]/page.tsx список уроков задачи (@99-113 на исходной версии; после 61.1-01/04 строки/кнопки изменены, список уроков сохранён): Link href={l.locked ? '#' : `/learn/${l.id}`}. Нужно добавить ?from=job:${slug} для незаблокированных.

learn/[id]/page.tsx breadcrumb (@612-623): сейчас «Обучение / course.title / Урок N». useParams для id; window.location.search уже читается для ?t=. Хук-правила: все useQuery держать выше early returns (страница уже так устроена — getLesson/hasDiagnostic/recommendedPath/watchProgress в начале компонента).

job.ts роутер: getJob тяжёлый (subs/billing/lessons). Нужен лёгкий getTitleBySlug: protectedProcedure input {slug}, select {slug,title,isPublished}, возврат {slug,title}|null.

Формат from: значение `job:<slug>`. Парсинг на странице урока: searchParams.get('from'), если startsWith('job:') → slug = substring после 'job:'.

Паттерн тестов: packages/api/src/routers/__tests__/favorite.test.ts (или material.test.ts) — stub prisma через vi.mock, тест protectedProcedure через createCaller.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: job.getTitleBySlug — лёгкий резолвер названия задачи + unit-тесты</name>
  <files>packages/api/src/routers/job.ts, packages/api/src/routers/__tests__/job.test.ts</files>
  <read_first>
    - packages/api/src/routers/job.ts (структура роутера, getJob @123 как образец input/handleDatabaseError)
    - packages/api/src/routers/__tests__/favorite.test.ts (или ближайший существующий тест роутера — паттерн vi.mock prisma + createCaller)
  </read_first>
  <behavior>
    - getTitleBySlug({slug}) → { slug, title } для опубликованной задачи; null если не найдена/не опубликована.
    - Не тянет lessons/subs/billing (лёгкий select).
    - Тест 1: published job (isPublished=true) → возвращает { slug, title }.
    - Тест 2: unpublished job (isPublished=false) → возвращает null.
    - Тест 3: slug не найден (findUnique → null) → возвращает null.
  </behavior>
  <action>
    Добавить в jobRouter процедуру getTitleBySlug: protectedProcedure.input(z.object({ slug: z.string() })).query → ctx.prisma.job.findUnique({ where: { slug }, select: { slug: true, title: true, isPublished: true } }); если !job || !job.isPublished → return null; иначе return { slug: job.slug, title: job.title }. Обернуть в try/catch handleDatabaseError как в getJob.

    Создать packages/api/src/routers/__tests__/job.test.ts со stub prisma (vi.mock) и тремя тест-кейсами для getTitleBySlug (published → title, unpublished → null, not found → null). Использовать паттерн createCaller из соседних тест-файлов роутеров.
  </action>
  <verify>
    <automated>cd "packages/api" && pnpm test -- --reporter=verbose job 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `getTitleBySlug` packages/api/src/routers/job.ts → ≥1
    - процедура использует select без lessons (grep блока: нет include lessons рядом с getTitleBySlug)
    - packages/api/src/routers/__tests__/job.test.ts существует, содержит ≥3 тест-кейса для getTitleBySlug
    - Все 3 тест-кейса (published, unpublished, not-found) проходят зелёными
    - `cd packages/api && npx tsc --noEmit` зелёный
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: from-параметр в ссылках уроков задачи + контекстный breadcrumb на уроке</name>
  <files>apps/web/src/app/(main)/learn/job/[slug]/page.tsx, apps/web/src/app/(main)/learn/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/learn/job/[slug]/page.tsx (текущее состояние после 61.1-01/04: список уроков, Link href на /learn/${l.id})
    - apps/web/src/app/(main)/learn/[id]/page.tsx (breadcrumb @612-623; useParams/useRouter @274-276; чтение window.location.search @289-291 как образец)
  </read_first>
  <behavior>
    - В job/[slug]: каждая незаблокированная ссылка урока ведёт на /learn/${l.id}?from=job:${slug}. Заблокированные (#) — без изменений.
    - В learn/[id]: если ?from=job:<slug> присутствует и job.getTitleBySlug вернул title → крошки «Решения под задачу (ссылка /learn/solutions) / [Название задачи] (ссылка /learn/job/<slug>) / Урок N». Иначе — текущие крошки «Обучение / Курс / Урок N».
    - Отсутствие from или нерезолвленный slug → стандартные крошки (никаких пустых/битых крошек).
  </behavior>
  <action>
    В job/[slug]/page.tsx: в map уроков заменить href незаблокированных уроков на `/learn/${l.id}?from=job:${slug}` (slug берётся из useParams, уже доступен). Заблокированные оставить '#'.

    В learn/[id]/page.tsx:
    1. Распарсить from один раз: const fromParam = typeof window !== 'undefined' ? new URLSearchParams(window.location.search).get('from') : null; const fromJobSlug = fromParam?.startsWith('job:') ? fromParam.slice(4) : null. (Можно useMemo; держать ВЫШЕ early returns.)
    2. Добавить useQuery: trpc.job.getTitleBySlug.useQuery({ slug: fromJobSlug! }, { enabled: !!fromJobSlug }) — тоже выше early returns (Rules of Hooks).
    3. В JSX breadcrumb (@612-623) сделать условный рендер: если fromJobSlug && jobTitleData?.title → крошки «Решения под задачу» (Link /learn/solutions) / job title (Link /learn/job/${fromJobSlug}) / «Урок {currentLessonNumber}». Иначе — текущая разметка крошек без изменений.
    Не менять остальную страницу. Все хуки — выше `if (isLoading)`/`if (lessonError)`/`if (!data?.lesson)`.
  </action>
  <verify>
    <automated>cd "apps/web" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep `from=job:` apps/web/src/app/(main)/learn/job/[slug]/page.tsx → ≥1
    - grep `getTitleBySlug` apps/web/src/app/(main)/learn/[id]/page.tsx → ≥1
    - grep `Решения под задачу` apps/web/src/app/(main)/learn/[id]/page.tsx → ≥1
    - В learn/[id]: getTitleBySlug.useQuery и парсинг from — выше первого early return (`if (isLoading)`)
    - `npx tsc --noEmit` в apps/web и packages/api зелёный
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` и `cd packages/api && npx tsc --noEmit` зелёные.
- `cd packages/api && pnpm test -- job` — все тест-кейсы getTitleBySlug зелёные.
- Ручная проверка на staging: открыть детальную задачу → кликнуть урок → URL содержит ?from=job:<slug>, крошки «Решения под задачу / [Задача] / Урок N», ссылки крошек ведут на /learn/solutions и /learn/job/<slug>. Открыть тот же урок напрямую из плана/каталога (без from) → стандартные крошки «Обучение / Курс / Урок».
</verification>

<success_criteria>
- Уроки из задачи несут from-контекст.
- Breadcrumb контекст-зависим, резолвит название задачи, gracefully падает в стандартный вид без from.
- Лёгкий резолвер не тянет тяжёлый getJob.
- Unit-тесты для getTitleBySlug зелёные (3 кейса).
- typecheck зелёный, Rules of Hooks соблюдены.
</success_criteria>

<output>
Create `.planning/phases/61.1-learning-2-0-uat-fixes/61.1-05-SUMMARY.md` when done
</output>
