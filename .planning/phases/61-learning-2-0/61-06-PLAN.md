---
phase: 61-learning-2-0
plan: 06
type: execute
wave: 7
depends_on: ["61-04"]
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/20260603000000_add_favorite/migration.sql
  - packages/api/src/routers/favorite.ts
  - packages/api/src/root.ts
  - packages/api/src/routers/__tests__/favorite.test.ts
autonomous: false
requirements: [D-06, D-07, D-10]

must_haves:
  truths:
    - "Favorite model + FavoriteItemType enum exist in schema.prisma; UserProfile has back-relation favorites Favorite[]"
    - "Additive migration.sql uses CREATE TYPE (new enum) + CREATE TABLE + unique/index + FK onDelete Cascade, zero destructive statements"
    - "Migration is APPLIED to the live prod DB via Supabase Management API and recorded in _prisma_migrations"
    - "favorite.{add,remove,list,isFavorited} are protectedProcedure, scoped ONLY by ctx.user.id"
    - "favorite.list resolves itemId entities with isHidden:false and skips dangling polymorphic refs"
  artifacts:
    - path: "packages/db/prisma/migrations/20260603000000_add_favorite/migration.sql"
      provides: "additive Favorite DDL"
      contains: "CREATE TYPE \"FavoriteItemType\""
    - path: "packages/api/src/routers/favorite.ts"
      provides: "favorite CRUD router"
      contains: "ctx.user.id"
    - path: "packages/api/src/root.ts"
      provides: "favorite router mounted"
      contains: "favorite"
  key_links:
    - from: "favorite.ts"
      to: "prisma.favorite"
      via: "ctx.user.id scoped queries"
      pattern: "ctx\\.user\\.id"
    - from: "schema.prisma UserProfile"
      to: "Favorite"
      via: "favorites Favorite[] back-relation"
      pattern: "favorites\\s+Favorite"
---

<objective>
Land the `Favorite` polymorphic model (D-06), apply the additive migration to live prod via the Supabase Management API (D-07), and build the IDOR-safe `favorite.{add,remove,list,isFavorited}` router. This is the backend foundation; the heart UI + data-migration are in 61-07.

Purpose: D-06 model + D-07 migration + favorite CRUD backend.
Output: schema + migration (applied to prod) + favorite router (mounted) + green favorite tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-CONTEXT.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md

<interfaces>
<!-- D-06 schema:
enum FavoriteItemType { LESSON JOB MATERIAL }
model Favorite { id String @id @default(cuid()); userId String; itemType FavoriteItemType; itemId String;
  createdAt DateTime @default(now()); user UserProfile @relation(fields:[userId], references:[id], onDelete: Cascade);
  @@unique([userId,itemType,itemId]); @@index([userId,itemType]) } -->
<!-- back-relation: add `favorites Favorite[]` to UserProfile relations block (schema.prisma:46-59, next to learningPath :48) -->
<!-- router mount: packages/api/src/root.ts — add `favorite: favoriteRouter` (alongside material/referral) -->
<!-- Mgmt API: reference_supabase_migration_via_mgmt_api.md — sha256(migration.sql) → POST /v1/projects/saecuecevicwjkpmaoot/database/query → INSERT _prisma_migrations row -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `favorite.*` tRPC | authenticated user manages their own favorites |
| migration apply → PROD DB | additive DDL on shared live DB (158 paying users) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | Elevation (IDOR) | favorite.* using userId from input | mitigate | `userId` is ALWAYS `ctx.user.id`, never read from input; zod input only carries `{itemType,itemId}` |
| T-61-06-02 | Tampering | dangling polymorphic itemId (no FK) | mitigate | `favorite.list` resolves itemId→entity with `isHidden:false`; skip non-existent/hidden refs (app-level integrity) |
| T-61-06-03 | Information Disclosure | favorite.list leaks hidden lesson/material | mitigate | `isHidden:false` (+ `course.isHidden:false` for lessons) in resolution query |
| T-61-06-04 | Tampering/DoS | destructive migration on shared prod | mitigate | Pre-flight grep `DROP|TRUNCATE|ALTER COLUMN.*TYPE` = 0; additive only; PITR backup confirmed before apply; [BLOCKING] human checkpoint |
| T-61-06-05 | Tampering | invalid itemType enum | mitigate | `z.nativeEnum(FavoriteItemType)` rejects tampered values |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Favorite schema + migration.sql + favorite router (mounted) + tests</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/20260603000000_add_favorite/migration.sql, packages/api/src/routers/favorite.ts, packages/api/src/root.ts, packages/api/src/routers/__tests__/favorite.test.ts</files>
  <behavior>
    - favorite.add upserts/creates a row with userId = ctx.user.id (never from input)
    - favorite.remove deleteMany where {userId: ctx.user.id, itemType, itemId}
    - favorite.list returns rows for ctx.user.id, optional itemType filter; resolves entities with isHidden:false, skips dangling refs
    - favorite.isFavorited({items}) returns a map/Set of favorited (itemType,itemId)
    - adding the same favorite twice is a no-op (idempotent via @@unique)
  </behavior>
  <read_first>
    - `packages/db/prisma/schema.prisma:46-59` (UserProfile relations block, `learningPath LearningPath?` :48, referral added `ambassadorCodesCreated ReferralCode[]` :59 at this site)
    - `packages/db/prisma/migrations/20260528000000_add_referral_code_table/migration.sql` (additive structural template; note it used ALTER TYPE for an existing enum — here use CREATE TYPE for the new enum)
    - `packages/api/src/routers/material.ts:15-21,109-112` (router skeleton + imports) + `learning.ts:1121-1155` (addJobToTrack: ctx.user.id scoping, upsert, `handleDatabaseError` + TRPCError re-throw)
    - `packages/api/src/root.ts` (mount pattern) + the 61-00 `favorite.test.ts` stub
    - `61-PATTERNS.md` § favorite.ts + § migration + § isHidden filter; `61-RESEARCH.md` Pattern 4
  </read_first>
  <action>
    schema.prisma: add the `FavoriteItemType` enum + `Favorite` model exactly per interfaces, and add `favorites Favorite[]` to the `UserProfile` relations block (next to `learningPath` :48). Run `pnpm db:generate` so the Prisma client types exist.
    migration.sql: create `packages/db/prisma/migrations/20260603000000_add_favorite/migration.sql` with `CREATE TYPE "FavoriteItemType" AS ENUM ('LESSON','JOB','MATERIAL')`, `CREATE TABLE "Favorite"(...)`, the unique index `Favorite_userId_itemType_itemId_key`, the index `Favorite_userId_itemType_idx`, and the FK to `UserProfile` `ON DELETE CASCADE` — structurally mirroring the referral migration. NO DROP/TRUNCATE/ALTER COLUMN.
    favorite.ts: build `favoriteRouter` with 4 `protectedProcedure`s. `add` → `prisma.favorite.upsert` (or create catching unique) with `userId: ctx.user.id`, zod input `{ itemType: z.nativeEnum(FavoriteItemType), itemId: z.string().min(1) }`. `remove` → deleteMany. `list` → findMany scoped to `ctx.user.id`, optional `itemType`; resolve each itemId to its entity with `isHidden:false` (+ `course.isHidden:false` for lessons), SKIP dangling/hidden. `isFavorited` → batch input `{ items: {itemType,itemId}[] }` → return favorited set. Wrap each in `try{}catch(e){ if(e instanceof TRPCError) throw e; handleDatabaseError(e) }`.
    root.ts: import + mount `favorite: favoriteRouter`.
    Fill the 61-00 `favorite.test.ts` assertions and un-skip them.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test favorite --run 2>&1 | grep -Ev '^#' | grep -Eqi 'pass|✓'</automated>
  </verify>
  <acceptance_criteria>
    - `schema.prisma` contains `enum FavoriteItemType`, `model Favorite`, and `favorites Favorite[]` on UserProfile.
    - `migration.sql` contains `CREATE TYPE "FavoriteItemType"` and `onDelete`-Cascade FK; `grep -E 'DROP|TRUNCATE|ALTER COLUMN.*TYPE' migration.sql` returns 0.
    - `favorite.ts` uses `ctx.user.id` and never reads `userId` from input; `grep input.*userId favorite.ts` = 0.
    - `favorite.list` resolution filters `isHidden`.
    - `root.ts` mounts `favorite: favoriteRouter`.
    - `pnpm test favorite` green (assertions un-skipped).
  </acceptance_criteria>
  <done>Favorite model, migration file, and IDOR-safe router exist; favorite tests green; client generated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply Favorite migration to PROD via Supabase Management API</name>
  <what-built>
    Additive `Favorite` migration SQL (`20260603000000_add_favorite/migration.sql`) ready to apply to the LIVE prod DB `saecuecevicwjkpmaoot` (158 paying users). VPS/local has no prisma-against-prod path (D-07) → apply via Supabase Management API per `reference_supabase_migration_via_mgmt_api.md` (Phase 56/60 precedent). The Favorite feature (61-07) CANNOT verify without this table existing; build/typecheck pass without it (types come from schema.prisma, not the live DB) → false positive guard.
  </what-built>
  <how-to-verify>
    1. PRE-FLIGHT: confirm PITR backup is enabled on project `saecuecevicwjkpmaoot` (Supabase dashboard → Database → Backups).
    2. PRE-FLIGHT: `grep -E 'DROP|TRUNCATE|ALTER COLUMN.*TYPE' packages/db/prisma/migrations/20260603000000_add_favorite/migration.sql` MUST return nothing (additive-only gate).
    3. Compute `sha256` of `migration.sql`.
    4. POST the DDL to `https://api.supabase.com/v1/projects/saecuecevicwjkpmaoot/database/query` (Management API token — owner/env). Apply `CREATE TYPE`, `CREATE TABLE`, the two indexes, and the FK.
    5. INSERT a row into `_prisma_migrations` with `migration_name = '20260603000000_add_favorite'` and the sha256 checksum (so `prisma migrate status` stays clean) — per the reference doc.
    6. VERIFY table exists: `SELECT to_regclass('public."Favorite"');` returns non-null and `SELECT 1 FROM "Favorite" LIMIT 1;` runs without «relation does not exist».
    This task touches PROD — requires the Management API token / owner action.
  </how-to-verify>
  <resume-signal>Type "applied" once the Favorite table exists in prod and the _prisma_migrations row is inserted, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test favorite && pnpm typecheck` green.
- Prod: `Favorite` table exists (`to_regclass` non-null); `_prisma_migrations` has the row.
- Security gate: no destructive SQL; `userId` only from `ctx.user.id`.
</verification>

<success_criteria>
Favorite model + IDOR-safe router shipped and the additive migration applied to live prod via Management API, unblocking 61-07.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-06-SUMMARY.md` when done
</output>
