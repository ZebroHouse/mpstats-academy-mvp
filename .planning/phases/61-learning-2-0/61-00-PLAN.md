---
phase: 61-learning-2-0
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routers/__tests__/favorite.test.ts
  - scripts/__tests__/migrate-track-to-favorites.test.ts
  - apps/web/src/components/learning/__tests__/AgentSearch.test.tsx
  - apps/web/tests/e2e/learn-redirect.spec.ts
  - packages/api/src/routers/__tests__/material.test.ts
autonomous: true
requirements: [NYQUIST-W0]
nyquist_compliant: true

must_haves:
  truths:
    - "Every Wave A–E behavior with an automated verify has a test file that exists and currently fails (RED) or is marked skip-pending-impl"
    - "favorite CRUD + IDOR scope test scaffolded before favorite.ts exists"
    - "data-migration idempotency + LessonProgress-untouched test scaffolded before script exists"
    - "AgentSearch scope-routing test scaffolded before scope prop exists"
    - "learn redirect e2e scaffolded before route split exists"
  artifacts:
    - path: "packages/api/src/routers/__tests__/favorite.test.ts"
      provides: "Wave 0 stub: favorite CRUD + isFavorited batch + IDOR-scope assertions"
      contains: "describe"
    - path: "scripts/__tests__/migrate-track-to-favorites.test.ts"
      provides: "Wave 0 stub: idempotency + LessonProgress snapshot"
      contains: "skipDuplicates"
    - path: "apps/web/src/components/learning/__tests__/AgentSearch.test.tsx"
      provides: "Wave 0 stub: scope routing solutions vs library"
      contains: "scope"
    - path: "apps/web/tests/e2e/learn-redirect.spec.ts"
      provides: "Wave 0 stub: /learn default + /learn/track redirect"
      contains: "/learn/track"
    - path: "packages/api/src/routers/__tests__/material.test.ts"
      provides: "extended: listForUser isHidden + type filter + standalone"
      contains: "listForUser"
  key_links: []
---

<objective>
Create the Wave 0 test scaffolds required by 61-VALIDATION.md so every downstream behavior with an `<automated>` verify has a test target that exists. These tests are written FIRST (RED state) — they reference identifiers (`favoriteRouter`, `scope` prop, `migrate-track-to-favorites`, `material.listForUser`, redirect routes) that later waves implement. Tests should fail or be `it.skip`/`describe.skip` pending implementation, but the files and assertion shells MUST exist.

Purpose: Satisfy Nyquist Dimension 8 — no implementation task in Waves A–E can claim `<automated>MISSING`; it points here.
Output: 5 test files (4 new + 1 extension of an existing pattern) committed before any feature code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-VALIDATION.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md

<interfaces>
<!-- Identifiers the stubs reference; implemented in later waves -->
- favorite router: `favorite.{add,remove,list,isFavorited}`, `protectedProcedure`, zod `{ itemType: FavoriteItemType, itemId: string }`, scoped by `ctx.user.id`
- enum `FavoriteItemType { LESSON JOB MATERIAL }` from `@mpstats/db`
- script `scripts/migrate-track-to-favorites.ts` — `--dry-run`/`--apply`, idempotent via `createMany({ skipDuplicates: true })`
- `AgentSearch` prop `scope: 'solutions' | 'library'` — solutions→`intent.resolve`, library→`ai.searchLessons` + `material.listForUser`
- `material.listForUser` protectedProcedure: zod `{ type?: MaterialType, search?: string, limit? }`, forces `where.isHidden = false`
- routes `/learn/plan`, `/learn/solutions`, `/learn/library`, `/learn/favorites`; `/learn`→server redirect; `/learn/track`→`/learn/plan`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend test stubs — favorite CRUD, data-migration idempotency, material.listForUser</name>
  <files>packages/api/src/routers/__tests__/favorite.test.ts, scripts/__tests__/migrate-track-to-favorites.test.ts, packages/api/src/routers/__tests__/material.test.ts</files>
  <read_first>
    - `packages/api/src/routers/material.ts:109-160` (existing list read shape — analog for listForUser test) + any existing router test in `packages/api/src/routers/__tests__/` (vitest + prisma ctx stub pattern, per State decision 49-02 / 56-02)
    - `61-PATTERNS.md` § favorite.ts (ctx.user.id IDOR-safe pattern) + § migrate-track-to-favorites (idempotency, `@@unique`, LessonProgress untouched)
    - `61-RESEARCH.md` Code Examples (idempotent data migration; custom section `id:'custom'` detection at learning.ts:897)
    - `61-VALIDATION.md` Per-Task Verification Map rows (favorite, migration, material)
  </read_first>
  <action>
    Create three vitest files using the existing api test harness (ctx prisma stub discriminated by select-arg, as in 56-02 onboarding test).
    (1) `favorite.test.ts` — `describe('favorite router')` with `it` blocks (may be `.skip` until 61-06 lands): `add` writes a row scoped to `ctx.user.id` (assert userId is `ctx.user.id`, NEVER taken from input — IDOR guard); `remove` deleteMany by `{userId,itemType,itemId}`; `list` filters by `ctx.user.id` and resolves itemId entities with `isHidden:false` skipping dangling refs; `isFavorited` batch returns a map/Set for `{items:{itemType,itemId}[]}`. Reference `FavoriteItemType` from `@mpstats/db`.
    (2) `migrate-track-to-favorites.test.ts` — `describe` with: idempotency assertion (run twice → row count unchanged thanks to `skipDuplicates`); custom-section lessons → `Favorite(LESSON)` and `addedJobs[]` → `Favorite(JOB)`; **LessonProgress snapshot count before == after** (hard rule D-03). Use a stubbed prisma whose `favorite.createMany` records calls.
    (3) Extend `material.test.ts` (create if absent) — `listForUser` forces `where.isHidden = false` (no includeHidden escape), honors optional `type` filter and `title contains` search, includes `isStandalone:true` rows; assert `getSignedUrl` ACL is NOT referenced/weakened by listForUser.
    Mark behavioral bodies `it.skip(... , 'pending 61-06/61-04')` where the implementation does not yet exist, so the suite stays green but the assertions are authored.
  </action>
  <verify>
    <!-- Gate must FAIL on a collection error, not merely confirm the module name is printed.
         Assert: no "Failed to collect" / "Cannot find" and a non-error vitest exit. -->
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && OUT=$(pnpm test favorite migrate-track material --run 2>&1); echo "$OUT" | grep -Eqi 'Failed to collect|Cannot find|SyntaxError|Error: Transform' && { echo "COLLECTION ERROR"; exit 1; }; echo "$OUT" | grep -Eq 'favorite|migrate-track|material' || { echo "no suite ran"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under their stated paths.
    - `favorite.test.ts` contains `describe` and asserts `ctx.user.id` is the userId source (string present: `ctx.user.id`).
    - `migrate-track-to-favorites.test.ts` contains `skipDuplicates` and a LessonProgress before/after count assertion.
    - `material.test.ts` references `listForUser` and `isHidden`.
    - The three suites COLLECT without error: runner output contains no `Failed to collect` / `Cannot find` / `SyntaxError` (skips allowed, RED behavioral failures allowed; collection/import errors are a hard fail).
  </acceptance_criteria>
  <done>Three backend test files authored; suite collects them without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend test stubs — AgentSearch scope routing + learn redirect e2e</name>
  <files>apps/web/src/components/learning/__tests__/AgentSearch.test.tsx, apps/web/tests/e2e/learn-redirect.spec.ts</files>
  <read_first>
    - `apps/web/src/components/learning/AgentSearch.tsx:15-35` (current `surface:'learn'` hardcode + trackedJobIds subscription — the change site)
    - `apps/web/tests/unit/welcome-page.test.tsx` (regression-test analog for redirect / router-cache loop, cited in RESEARCH Pitfall 1) + any existing playwright spec under `apps/web/tests/e2e/`
    - `61-UI-SPEC.md` § Interaction & State Contracts (redirect rows; `/learn`→default, `/learn/track`→`/learn/plan`)
    - `61-PATTERNS.md` § AgentSearch change site + § pages/redirects
  </read_first>
  <action>
    (1) `AgentSearch.test.tsx` — vitest + @testing-library/react. `describe('AgentSearch scope')` with `it.skip(...'pending 61-03')`: with `scope='solutions'` submitting a query calls `intent.resolve` (mock the trpc mutation, assert it's invoked); with `scope='library'` it calls `ai.searchLessons` + `material.listForUser` and renders grouped «Уроки»/«Материалы». Author the assertions even though the prop lands in 61-03.
    (2) `learn-redirect.spec.ts` — playwright. `test.describe('learn redirects')`: visiting `/learn/track` lands on `/learn/plan` (URL assertion); visiting `/learn` lands on `/learn/plan` OR `/learn/library` (default-by-plan). Mark tests `test.skip` (or `test.fixme`) pending 61-01 route split; the spec file + URL assertions must exist.
    Do NOT introduce `router.push`-based redirect expectations — assert final URL only (server redirect).
  </action>
  <verify>
    <!-- Fail on collection error, not just module-name presence. -->
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && OUT=$(pnpm test AgentSearch --run 2>&1); echo "$OUT" | grep -Eqi 'Failed to collect|Cannot find|SyntaxError|Error: Transform' && { echo "COLLECTION ERROR"; exit 1; }; echo "$OUT" | grep -q 'AgentSearch' || { echo "suite did not run"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at stated paths.
    - `AgentSearch.test.tsx` references `scope`, `solutions`, `library`, `intent.resolve`, `ai.searchLessons`.
    - `learn-redirect.spec.ts` references `/learn/track` and `/learn/plan` and asserts on final URL (no `router.push`).
    - `AgentSearch.test.tsx` COLLECTS without error: output contains no `Failed to collect` / `Cannot find` / `SyntaxError`.
  </acceptance_criteria>
  <done>Two frontend test scaffolds authored; web vitest collects AgentSearch stub without error.</done>
</task>

</tasks>

<verification>
- `pnpm test --run` collects all 5 files without import/syntax errors (skips and RED failures permitted).
- Every VALIDATION.md "File Exists ❌ W0" row now has a corresponding file.
</verification>

<success_criteria>
All 5 Wave 0 test files exist and are collected by the test runner. Downstream waves can reference these as their `<automated>` target instead of declaring MISSING.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-00-SUMMARY.md` when done
</output>
