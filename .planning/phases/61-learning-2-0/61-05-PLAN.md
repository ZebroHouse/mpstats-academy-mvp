---
phase: 61-learning-2-0
plan: 05
type: execute
wave: 6
depends_on: ["61-04"]
files_modified:
  - apps/web/src/components/learning/LearningHero.tsx
  - apps/web/src/app/(main)/learn/solutions/page.tsx
  - apps/web/src/app/(main)/learn/library/page.tsx
  - apps/web/src/app/(main)/dashboard/page.tsx
autonomous: true
requirements: [D-08, D-09]

must_haves:
  truths:
    - "Hero block (accent gradient bg) sits at the top of /learn/solutions and /learn/library with display headline + large search input + filter chips"
    - "Dashboard shows 3 accent entry cards above condensed stats, each routing to its section"
    - "Dashboard 4 stat tiles condensed into a compact strip; all-zero strip hidden / replaced with a hint for new users"
    - "data-tour learn-search anchor points at the hero input"
  artifacts:
    - path: "apps/web/src/components/learning/LearningHero.tsx"
      provides: "Hero search wrapper (gradient bg, display headline, AgentSearch, chips)"
      contains: "bg-mp-hero-gradient"
    - path: "apps/web/src/app/(main)/dashboard/page.tsx"
      provides: "3 entry cards + condensed stats"
      contains: "/learn/plan"
  key_links:
    - from: "dashboard entry cards"
      to: "/learn/{plan,library,solutions}"
      via: "card links"
      pattern: "/learn/(plan|library|solutions)"
    - from: "LearningHero"
      to: "AgentSearch"
      via: "wraps scoped search"
      pattern: "AgentSearch"
---

<objective>
Surface search and section entry points prominently: a hero search block (D-09, slide 6) at the top of «Решений» and «Базы знаний», and 3 large accent entry cards on the dashboard (D-08, slide 17) with condensed stats. Pure UI/layout reusing brand tokens — no new search logic (AgentSearch already scoped in 61-04).

Purpose: D-08 dashboard 3 entries + D-09 hero search.
Output: `LearningHero` wrapper, hero-mounted search on 2 pages, dashboard entry cards + condensed stats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-UI-SPEC.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md

<interfaces>
<!-- Hero (UI-SPEC §2): bg-mp-hero-gradient, rounded-xl, px-6 py-8; headline text-display-sm; input h-14 rounded-xl white bg shadow-mp-card; submit pill bg-mp-blue-500; chip row below -->
<!-- Hero copy: Solutions headline + placeholder «Опишите задачу — подберём решение»; Library «Найдите урок или материал по теме» -->
<!-- Dashboard entries (UI-SPEC §3, D-08):
  1 «Продолжить мой план» / «К месту, где вы остановились» → /learn/plan (soft-blue)
  2 «Найти быстрый ответ» / «Поиск по урокам и материалам» → /learn/library (soft-green)
  3 «Решить задачу» / «Инструкции под конкретную задачу» → /learn/solutions (gradient) -->
<!-- Card variants: card.tsx:13-17 soft-blue/soft-green/gradient + interactive (hover:-translate-y-0.5) -->
<!-- Dashboard current: 4 stats grid :138-174; 2 CTA cards :180-218 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LearningHero wrapper + mount on solutions & library pages</name>
  <files>apps/web/src/components/learning/LearningHero.tsx, apps/web/src/app/(main)/learn/solutions/page.tsx, apps/web/src/app/(main)/learn/library/page.tsx</files>
  <read_first>
    - `apps/web/src/components/ui/card.tsx:13-17` (variants soft-blue/soft-green/gradient) + `tailwind.config.ts` (`mp-hero-gradient`, `text-display-sm`, `shadow-mp-card` tokens)
    - the 61-04 `solutions/page.tsx` + `library/page.tsx` (where `<AgentSearch scope=…>` currently sits — wrap it in the hero)
    - `61-UI-SPEC.md` §2 (Hero block contract: container, headline, input h-14, chips) + Copywriting (hero copy per scope)
    - `61-PATTERNS.md` § Card-accents Don't-Hand-Roll
  </read_first>
  <action>
    Create `LearningHero.tsx` (`'use client'`): a full-width block, `bg-mp-hero-gradient rounded-xl px-6 py-8` (mobile `px-4 py-6`), containing a `text-display-sm` headline (per `scope` prop) + optional `text-body` sub-line, and `{children}` (the scoped `AgentSearch` + chip row). Props: `scope: 'solutions'|'library'`, `headline`, optional `subline`. The hero owns the visual chrome; `AgentSearch` (passed as child) gets the larger `h-14` treatment via a `size`/`variant` prop or wrapper class (do NOT fork AgentSearch — pass a class or boolean). Add `data-tour="learn-search"` on the hero input wrapper (D-10).
    In solutions/page.tsx and library/page.tsx: replace the bare `<AgentSearch scope=…>` mount with `<LearningHero scope=… headline=…>` wrapping the same `<AgentSearch>` + the existing filter chips moved under the input. Keep all 61-04 query wiring intact.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -qi 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `LearningHero.tsx` exists, contains `bg-mp-hero-gradient` and `text-display-sm`, and renders `data-tour="learn-search"`.
    - solutions & library pages wrap their `AgentSearch` in `<LearningHero`.
    - No raw `text-[Npx]` introduced (semantic tokens only, UI-SPEC Typography).
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Hero search block renders at the top of Solutions and Library with the brand gradient and large input.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard 3 entry cards + condensed stats strip</name>
  <files>apps/web/src/app/(main)/dashboard/page.tsx</files>
  <read_first>
    - `apps/web/src/app/(main)/dashboard/page.tsx:138-174` (4 stat tiles grid) + `:180-218` (2 CTA cards `Card variant="soft-blue"/"soft-green"` + `hover:-translate-y-1`)
    - `apps/web/src/components/ui/card.tsx:13-17` (variants + `interactive`)
    - `61-UI-SPEC.md` §3 (3 entry cards: copy, variant mapping plan→soft-blue / library→soft-green / solutions→gradient; condensed stats; all-zero hide/hint)
  </read_first>
  <action>
    Add a 3-card entry row at the VERY top of the dashboard (above stats): `grid grid-cols-1 md:grid-cols-3 gap-6`, each `Card interactive p-6` linking to its route, using the copy + variant mapping from interfaces (plan→soft-blue, library→soft-green, solutions→gradient). Each card: lucide icon, `text-heading-lg` title, `text-body-sm text-mp-gray-600` sub-line.
    Condense the existing 4 stat tiles (`:138-174`) into a single compact horizontal strip (`flex gap-6` inline `label: value` pairs, `text-body-sm`) placed BELOW the entry cards. If all four stats are zero (new user), hide the strip OR show a single «Начните с диагностики» hint instead of zeros. Radar, «Продолжить урок», activity feed remain unchanged below.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -qi 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/page.tsx` contains links `/learn/plan`, `/learn/library`, `/learn/solutions` in 3 entry cards with the canon titles «Продолжить мой план», «Найти быстрый ответ», «Решить задачу».
    - Entry cards use `Card` `interactive` + the soft-blue/soft-green/gradient variants.
    - Stat tiles condensed; an all-zero guard (hide or hint) is present.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Dashboard leads with 3 accent entries; stats condensed; new-user view shows no dead-zeros.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` green; `pnpm test` full suite still green.
- Manual (staging): hero visible on Solutions/Library per slide 6; dashboard matches slide 17 (3 entries + condensed stats).
</verification>

<success_criteria>
Hero search (D-09) + dashboard 3 entries with condensed stats (D-08) shipped, reusing brand tokens.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-05-SUMMARY.md` when done
</output>
