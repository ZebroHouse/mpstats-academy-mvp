---
phase: 61-learning-2-0
plan: 02
type: execute
wave: 3
depends_on: ["61-01"]
files_modified:
  - apps/web/src/app/(main)/learn/page.tsx
  - apps/web/src/app/(main)/learn/plan/page.tsx
  - apps/web/src/app/(main)/learn/solutions/page.tsx
  - apps/web/src/app/(main)/learn/library/page.tsx
  - apps/web/src/app/(main)/learn/favorites/page.tsx
  - apps/web/src/lib/tours/definitions.ts
autonomous: true
requirements: [D-01, D-02, D-10]

must_haves:
  truths:
    - "/learn is a Server Component that server-redirects to /learn/plan (non-empty plan) else /learn/library"
    - "/learn/solutions renders the former lens==='jobs' UI (MarketplaceSwitch + progress filter + JobCatalog)"
    - "/learn/library renders the former lens==='courses' UI (courses accordion + filters)"
    - "/learn/plan renders diagnostic sections + План empty-state; /learn/favorites renders a placeholder/empty-state (filled in Wave D)"
    - "No user-facing «трек»/«плейбук»/«джоба»/«lens»/«каталог» strings remain in these surfaces"
    - "data-tour anchors re-homed so the onboarding tour does not break"
  artifacts:
    - path: "apps/web/src/app/(main)/learn/page.tsx"
      provides: "Server-redirect entry (default by plan)"
      contains: "redirect("
    - path: "apps/web/src/app/(main)/learn/solutions/page.tsx"
      provides: "Solutions (Job catalog) page"
      contains: "JobCatalog"
    - path: "apps/web/src/app/(main)/learn/library/page.tsx"
      provides: "Library (courses) page"
      contains: "Курс"
    - path: "apps/web/src/app/(main)/learn/plan/page.tsx"
      provides: "Персональный план page (diagnostic sections)"
      contains: "getRecommendedPath"
    - path: "apps/web/src/app/(main)/learn/favorites/page.tsx"
      provides: "Избранное page (placeholder/empty-state, wired in Wave D)"
      contains: "Избранное"
  key_links:
    - from: "apps/web/src/app/(main)/learn/page.tsx"
      to: "prisma.learningPath"
      via: "server read for default redirect"
      pattern: "learningPath"
---

<objective>
Split the single `/learn` page (lens jobs/courses toggle) into 4 routed pages and convert `/learn` itself into a Server Component that redirects to the correct default. Apply the D-02 terminology canon to all surfaced strings and re-home the `data-tour` anchors so the onboarding tour survives (D-10). На этом этапе `/learn/plan` показывает ТОЛЬКО диагностические секции (Избранное split происходит в Wave D — здесь читаем `getRecommendedPath` как есть, но не выводим ручные добавления как «план»).

Purpose: D-01 route split + D-02 rename + D-10 tour anchors.
Output: 4 sub-pages, server-redirect `/learn`, canon strings, intact tour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-UI-SPEC.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md

<interfaces>
<!-- Canon (UI-SPEC Copywriting): трек→Персональный план; плейбук/джоба→Решение под задачу/Задача; все курсы/каталог→База знаний; +В трек→В избранное -->
<!-- Default-redirect rule (UI-SPEC §Interaction): /learn → /learn/plan if non-empty plan else /learn/library -->
<!-- LearningTabs (from 61-01) — mount at top of each /learn/* page on mobile -->
<!-- Empty states (UI-SPEC §6):
  План no path: heading «Плана пока нет», body «Пройдите диагностику…», CTA «Пройти диагностику»→/diagnostic
  Избранное empty: heading «В избранном пусто», body «Нажимайте на сердечко…», CTA «Перейти в Базу знаний»→/learn/library -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lens blocks into /learn/solutions + /learn/library; create /learn/plan and /learn/favorites pages</name>
  <files>apps/web/src/app/(main)/learn/solutions/page.tsx, apps/web/src/app/(main)/learn/library/page.tsx, apps/web/src/app/(main)/learn/plan/page.tsx, apps/web/src/app/(main)/learn/favorites/page.tsx</files>
  <read_first>
    - `apps/web/src/app/(main)/learn/page.tsx` FULL (lens state :71, toggle :274-289, jobs block :335-358, courses block :359-495, loading skeleton :218-228, error card :231-256, empty state :317-327)
    - `apps/web/src/app/(main)/learn/track/page.tsx` (current diagnostic sections render — the body for /learn/plan, BEFORE the 61-01 redirect replacement; read the pre-redirect git content if needed) — diagnostic-section consumer of `learning.getRecommendedPath`
    - `61-PATTERNS.md` § pages (client page structure, loading/error/empty analogs) + § learn/page.tsx change site
    - `61-UI-SPEC.md` §2 (hero placement — leave a slot; hero itself lands in 61-05), §6 (empty states), Copywriting canon
  </read_first>
  <action>
    Create 4 `'use client'` pages under `app/(main)/learn/`, each mounting `<LearningTabs />` (from 61-01) at the top (mobile sub-nav):
    - `solutions/page.tsx` = the former `lens==='jobs'` block (`learn/page.tsx:335-358`): `MarketplaceSwitch` + progress filter + `<JobCatalog>`. H1 «Решения под задачу». Keep the existing `trpc.job.getCatalog` query + loading skeleton (:218-228) + error card (:231-256).
    - `library/page.tsx` = the former `lens==='courses'` block (`learn/page.tsx:359-495`): courses accordion + filters. H1 «База знаний». Reuse the same skeleton/error patterns and `toggleCourseExpanded`.
    - `plan/page.tsx` = diagnostic-sections view from the current track page: read `trpc.learning.getRecommendedPath`, render ONLY the diagnostic sections (do not surface `addedJobs`/custom here as «план» — Wave D removes them entirely; for now render diagnostic sections only). Empty state per interfaces (heading «Плана пока нет», CTA «Пройти диагностику»→`/diagnostic`). H1 «Персональный план».
    - `favorites/page.tsx` = placeholder page: H1 «Избранное» + the empty-state block (heading «В избранном пусто», body «Нажимайте на сердечко…», CTA «Перейти в Базу знаний»→`/learn/library`). Real `favorite.list` wiring is 61-07; this page must exist now so the nav route resolves.
    Apply D-02 canon to every visible string (no «трек»/«плейбук»/«джоба»/«lens»/«каталог»). Leave a clearly-marked slot/comment at the top of solutions + library for the hero block (61-05 fills it). Do NOT add `FavoriteButton` yet (61-07).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -qi 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 page files exist; each imports and renders `LearningTabs`.
    - `solutions/page.tsx` contains `JobCatalog` and H1 string «Решения под задачу».
    - `library/page.tsx` contains H1 «База знаний».
    - `plan/page.tsx` references `getRecommendedPath` and the empty-state copy «Плана пока нет».
    - `favorites/page.tsx` contains «В избранном пусто».
    - `grep -RInE 'трек|плейбук|джоб|\blens\b|каталог' apps/web/src/app/(main)/learn/{plan,solutions,library,favorites}` filtered for user-visible JSX text returns 0 (model-name/internal code comments excluded).
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>4 sub-pages render the split content with canon copy; favorites is a working empty-state placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: /learn → Server Component default redirect + re-home data-tour anchors</name>
  <files>apps/web/src/app/(main)/learn/page.tsx, apps/web/src/lib/tours/definitions.ts</files>
  <read_first>
    - `apps/web/src/app/(main)/layout.tsx:34,49,64` (Server Component reading profile via prisma + `redirect()` — analog for reading learningPath + redirecting)
    - `apps/web/src/lib/tours/definitions.ts` (`learn-search`/`learn-view-toggle`/`learn-add-to-track` anchors, `getSteps` DOM-state branch :178)
    - `61-RESEARCH.md` Pitfall 1 (router-cache loop — server redirect ONLY) + Code Example
    - `61-UI-SPEC.md` §Interaction (`/learn` redirect row; new anchors `learn-search`→hero input, `learn-submenu`→nav group)
  </read_first>
  <action>
    Replace `/learn/page.tsx` ENTIRELY with a Server Component: read the current user's `learningPath` via prisma (mirror how `layout.tsx` reads profile — direct prisma, not tRPC server caller per State 34-01). If the plan has non-empty diagnostic sections → `redirect('/learn/plan')`, else → `redirect('/learn/library')`. Use `redirect` from `next/navigation`; NO `'use client'`, NO `useEffect`, NO `router.push` (router-cache loop). All former lens UI now lives in solutions/library (Task 1).
    Update `definitions.ts`: re-point the onboarding tour anchors to the new structure (D-10) — `learn-view-toggle` is gone (no lens toggle), so either remove that step or repoint to `learn-submenu`; `learn-search`→the hero/search input on `/learn/library` or `/learn/solutions`; `learn-add-to-track` copy/anchor→the new «В план»/«В избранное» affordance. Ensure `getSteps` no longer references a removed DOM anchor (otherwise driver.js step targets a missing element and the tour breaks).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test:e2e learn-redirect 2>&1 | grep -Ev '^#' | grep -Eqi 'learn.*default|/learn '</automated>
  </verify>
  <acceptance_criteria>
    - `/learn/page.tsx` is a Server Component (no `'use client'`), imports `redirect` from `next/navigation`, reads `learningPath`, and branches `redirect('/learn/plan')` / `redirect('/learn/library')`.
    - No `router.push` / `useEffect` redirect anywhere in `/learn/page.tsx`.
    - `definitions.ts` contains no anchor id that lacks a DOM target under the new structure (grep the removed `learn-view-toggle` is repointed or removed).
    - `learn-redirect` e2e `/learn` default case passes; `/learn/track` case still passes.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>/learn redirects server-side to the right default; onboarding tour anchors resolve to existing elements.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` green; `pnpm test:e2e learn-redirect` both cases green.
- Manual (staging): tour runs without «element not found»; canon strings everywhere.
</verification>

<success_criteria>
4 routed learning pages live with canon copy, `/learn` server-redirects to the right default, onboarding tour intact (D-10).
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-02-SUMMARY.md` when done
</output>
