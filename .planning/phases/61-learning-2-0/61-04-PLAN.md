---
phase: 61-learning-2-0
plan: 04
type: execute
wave: 5
depends_on: ["61-03"]
files_modified:
  - apps/web/src/components/learning/AgentSearch.tsx
  - apps/web/src/components/learning/__tests__/AgentSearch.test.tsx
  - apps/web/src/components/learning/LessonResultCard.tsx
  - apps/web/src/app/(main)/learn/solutions/page.tsx
  - apps/web/src/app/(main)/learn/library/page.tsx
autonomous: true
requirements: [D-04, D-10]

must_haves:
  truths:
    - "AgentSearch accepts scope: 'solutions' | 'library'"
    - "scope='solutions' resolves via intent.resolve (job result cards — unchanged)"
    - "scope='library' resolves via ai.searchLessons (lesson cards) AND material.listForUser (material cards), rendered grouped «Уроки»/«Материалы»"
    - "Library material catalog + type filter visible even without a search query"
    - "All new lesson/material queries keep the isHidden filter"
  artifacts:
    - path: "apps/web/src/components/learning/AgentSearch.tsx"
      provides: "scope-aware search"
      contains: "scope"
    - path: "apps/web/src/components/learning/LessonResultCard.tsx"
      provides: "lesson search-result card"
      contains: "/learn/"
    - path: "apps/web/src/app/(main)/learn/library/page.tsx"
      provides: "library page wired to scope='library' search + material catalog"
      contains: "listForUser"
  key_links:
    - from: "AgentSearch (scope='library')"
      to: "ai.searchLessons + material.listForUser"
      via: "parallel trpc queries"
      pattern: "searchLessons|listForUser"
    - from: "AgentSearch (scope='solutions')"
      to: "intent.resolve"
      via: "mutation"
      pattern: "intent\\.resolve|resolveMutation"
---

<objective>
Make search context-scoped (D-04). `AgentSearch` gains a `scope` prop: «Решения под задачу» searches playbooks (existing `intent.resolve`), «База знаний» searches lessons (`ai.searchLessons`, already built at ai.ts:213) + materials (`material.listForUser` title search from 61-03). Mount the scoped search on the solutions and library pages, and add a material catalog + type filter to the library page.

Purpose: D-04 contextual search + D-05 material catalog UI.
Output: scope-aware AgentSearch, lesson result card, library page with materials catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-UI-SPEC.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md

<interfaces>
<!-- intent.resolve input (intent.ts:16): surface: z.enum(['learn','welcome','diagnostic']) — keep 'learn' for solutions -->
<!-- ai.searchLessons (ai.ts:213): input {query: string.min(1).max(500)}; output {query, results: SearchLessonResult[], totalChunks}
     SearchLessonResult = {lesson:{id,courseId,title,duration,...}, course:{id,title,isHidden}, snippets[], watchedPercent, status, locked, inRecommendedPath} — already filters isHidden -->
<!-- material.listForUser (61-03): {type?, search?, limit?} → visible materials, isHidden forced -->
<!-- AgentSearch current hardcode (AgentSearch.tsx:35): surface:'learn'; trackedJobIds subscription :15-19 -->
<!-- copy (UI-SPEC): Solutions placeholder «Опишите задачу — подберём решение»; Library placeholder «Найдите урок или материал по теме»; submit «Найти»/«Ищем…»; no-results «Ничего не нашли» -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `ai.searchLessons` / `material.listForUser` | authenticated user search query crosses into retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Information Disclosure | search surfaces hidden lessons/materials | mitigate | `ai.searchLessons` already filters `isHidden:false` (ai.ts:237); `material.listForUser` forces it (61-03). No new query bypasses the filter |
| T-61-04-02 | Tampering | over-long / malformed query | mitigate | `z.string().min(1).max(500)` on searchLessons (existing); material search trims |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add scope prop to AgentSearch + grouped library results; fill scope test</name>
  <files>apps/web/src/components/learning/AgentSearch.tsx, apps/web/src/components/learning/LessonResultCard.tsx, apps/web/src/components/learning/__tests__/AgentSearch.test.tsx</files>
  <read_first>
    - `apps/web/src/components/learning/AgentSearch.tsx` FULL (surface hardcode :35, trackedJobIds :15-19, job result render :93-138)
    - `packages/api/src/routers/ai.ts:213-379` (searchLessons input/output shape) + `apps/web/src/components/learning/JobCard.tsx` (card layout language to mirror for LessonResultCard)
    - `apps/web/src/components/learning/__tests__/AgentSearch.test.tsx` (61-00 stub to fill)
    - `61-PATTERNS.md` § AgentSearch change site + § pages; `61-RESEARCH.md` Pattern 3 + Pitfall 3
    - `61-UI-SPEC.md` §2 (scope behavior, grouped «Уроки»/«Материалы» render) + §4 (Lesson result card elements)
  </read_first>
  <action>
    AgentSearch: add required prop `scope: 'solutions' | 'library'`.
    - `scope='solutions'` → keep current `intent.resolve` mutation (replace the literal `surface:'learn'` use so it's reached only in this branch); render job cards as today (:93-138). Use Solutions placeholder copy.
    - `scope='library'` → on submit run TWO parallel queries: `trpc.ai.searchLessons.useQuery({query})` (or mutation per existing batching) and `trpc.material.listForUser.useQuery({search:query})`. Render grouped: a «Уроки» section of `LessonResultCard` then a «Материалы» section of `MaterialCard` (standalone-capable from 61-03). Empty → «Ничего не нашли» copy. Use Library placeholder copy.
    - Keep `trackedJobIds` subscription but be aware of the Wave-D split (Pitfall 3) — do not change its semantics here; 61-07 updates consumers together.
    Create `LessonResultCard.tsx`: mirror `JobCard` layout (UI-SPEC §4) — title → `/learn/[lesson.id]`, course name, snippet, progress %, `locked` lock icon if gated. Reserve a slot for `FavoriteButton` (added 61-07), do not add the heart now.
    Fill the 61-00 `AgentSearch.test.tsx`: assert solutions→`intent.resolve` invoked, library→`ai.searchLessons` + `material.listForUser` invoked + grouped sections rendered.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test AgentSearch --run 2>&1 | grep -Ev '^#' | grep -Eqi 'pass|✓'</automated>
  </verify>
  <acceptance_criteria>
    - `AgentSearch.tsx` declares `scope: 'solutions' | 'library'` and branches on it.
    - solutions branch calls `intent.resolve`; library branch calls both `ai.searchLessons` and `material.listForUser`.
    - `LessonResultCard.tsx` exists, links to `/learn/${lesson.id}`.
    - `AgentSearch.test.tsx` assertions un-skipped and green.
    - No `isHidden` bypass introduced (library queries reuse the filtered endpoints).
  </acceptance_criteria>
  <done>Search returns playbooks on Solutions and lessons+materials on Library, with the scope test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scoped search into solutions + library pages; add material catalog + type filter</name>
  <files>apps/web/src/app/(main)/learn/solutions/page.tsx, apps/web/src/app/(main)/learn/library/page.tsx</files>
  <read_first>
    - the 61-02 `solutions/page.tsx` + `library/page.tsx` (the hero/search slots left by 61-02)
    - `apps/web/src/components/learning/MaterialCard.tsx` (now optional-lessonId, 61-03)
    - `61-UI-SPEC.md` §2 (filter chips: Library material-type chips «Презентация»/«Таблица расчётов»/«Внешний сервис»/«Чек-лист»/«Памятка»/«Уроки») + §4 (Material card)
  </read_first>
  <action>
    solutions/page.tsx: mount `<AgentSearch scope="solutions" />` in the search slot (above JobCatalog). Marketplace WB/Ozon + progress chips remain.
    library/page.tsx: mount `<AgentSearch scope="library" />` in the search slot. Below it, add a material catalog: `trpc.material.listForUser.useQuery({ type })` rendering `MaterialCard`s, with a single-select type-filter chip row (the 5 `MaterialType` values + «Уроки» as a toggle back to the courses accordion view). When a type filter yields nothing → empty copy «Материалов этого типа пока нет» / «Снимите фильтр…» (UI-SPEC §6). Courses accordion (from 61-02) stays as the default/«Уроки» view. Keep `isHidden`-filtered data only.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -qi 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `solutions/page.tsx` mounts `<AgentSearch scope="solutions"`.
    - `library/page.tsx` mounts `<AgentSearch scope="library"` and calls `material.listForUser`.
    - Library has a material-type chip row covering the 5 `MaterialType` values.
    - Empty-type copy «Материалов этого типа пока нет» present.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Solutions and Library pages have scoped search; Library shows the material catalog with type filtering.</done>
</task>

</tasks>

<verification>
- `pnpm test AgentSearch && pnpm typecheck` green.
- Manual (staging): Solutions search returns решения; Library search returns уроки + материалы; type filter works.
</verification>

<success_criteria>
Context-scoped search live (D-04); Library shows materials with type filter (D-05 UI); no hidden-content leak.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-04-SUMMARY.md` when done
</output>
