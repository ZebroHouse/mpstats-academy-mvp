---
phase: 61-learning-2-0
plan: 03
type: execute
wave: 4
depends_on: ["61-02"]
files_modified:
  - packages/api/src/routers/material.ts
  - packages/api/src/routers/__tests__/material.test.ts
  - apps/web/src/components/learning/MaterialCard.tsx
autonomous: true
requirements: [D-05, D-10]

must_haves:
  truths:
    - "material.listForUser is a protectedProcedure returning visible materials (isHidden=false forced) with optional type + title-search filters"
    - "listForUser includes standalone (isStandalone=true) and lesson-attached materials"
    - "getSignedUrl download ACL is unchanged (not weakened)"
    - "MaterialCard accepts an optional lessonId (standalone cards render without a lessonId)"
  artifacts:
    - path: "packages/api/src/routers/material.ts"
      provides: "material.listForUser user-facing read"
      contains: "listForUser"
    - path: "apps/web/src/components/learning/MaterialCard.tsx"
      provides: "MaterialCard with optional lessonId"
      contains: "lessonId"
  key_links:
    - from: "packages/api/src/routers/material.ts (listForUser)"
      to: "prisma.material"
      via: "findMany with where.isHidden=false"
      pattern: "isHidden"
---

<objective>
Add a user-facing `material.listForUser` read endpoint (D-05) so materials can appear in the Base of Knowledge catalog, and make `MaterialCard.lessonId` optional so standalone materials render. The download ACL (`getSignedUrl`) is frozen — standalone materials are `externalUrl`-only this pass (A8), so the ACL gap never triggers.

Purpose: D-05 materials user-facing + the backend that the library search (61-04) and Избранное (61-07) consume.
Output: `material.listForUser` procedure, extended material test, optional-`lessonId` MaterialCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-CONTEXT.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md

<interfaces>
<!-- list read shape to copy — material.ts:112-160 (adminProcedure → protectedProcedure, force isHidden=false) -->
<!-- MaterialType enum: PRESENTATION | CALCULATION_TABLE | EXTERNAL_SERVICE | CHECKLIST | MEMO, from @mpstats/db -->
<!-- getSignedUrl ACL (DO NOT TOUCH): material.ts:398-492 — standalone (no attached lesson) currently FORBIDDEN -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC `material.listForUser` | authenticated user requests material list with filters |
| client → tRPC `material.getSignedUrl` | authenticated user requests a download URL (UNCHANGED) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Information Disclosure | `material.listForUser` returns hidden content | mitigate | Force `where.isHidden = false` (no `includeHidden` escape hatch in the public input); copy `material.ts:127` pattern |
| T-61-03-02 | Elevation of Privilege | weakening download ACL for standalone | mitigate | Do NOT modify `getSignedUrl` (material.ts:398-492). Standalone = `externalUrl`-only (A8); list returns metadata only, never `storagePath` to client |
| T-61-03-03 | Tampering | tampered `type` filter value | mitigate | `z.nativeEnum(MaterialType)` validates enum; reject before query |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: material.listForUser (protectedProcedure) + extend material test</name>
  <files>packages/api/src/routers/material.ts, packages/api/src/routers/__tests__/material.test.ts</files>
  <behavior>
    - listForUser forces isHidden=false (passing includeHidden has no effect / not in input)
    - optional type filter applies where.type
    - optional search applies where.title contains, mode insensitive
    - returns standalone (isStandalone=true) AND lesson-attached materials
    - response payload never includes storagePath (hasFile boolean only, per State 49-02)
    - getSignedUrl ACL untouched (test asserts no change in its FORBIDDEN-on-no-lesson behavior)
  </behavior>
  <read_first>
    - `packages/api/src/routers/material.ts:109-160` (`list` adminProcedure read — copy shape) + `:398-492` (`getSignedUrl` ACL — DO NOT WEAKEN) + `:149` (storagePath-never-to-client pattern, State 49-02)
    - `packages/api/src/routers/__tests__/material.test.ts` (the 61-00 stub to fill) + `learning.ts:1152-1155` (`handleDatabaseError` + `TRPCError` re-throw)
    - `61-PATTERNS.md` § material.ts change site + § tRPC protected read + § isHidden filter + § download ACL DO-NOT-WEAKEN
  </read_first>
  <action>
    Add `listForUser: protectedProcedure` to `materialRouter` (after `getById`). Input zod: `{ type: z.nativeEnum(MaterialType).optional(), search: z.string().optional(), limit: z.number().int().min(1).max(100).default(50), cursor: z.string().optional() }`. Build `where` like `list` (`material.ts:125-135`) BUT always set `where.isHidden = false` (no `includeHidden`). Include both standalone and lesson-attached (no `isStandalone` filter → returns all visible). Select metadata only — `hasFile` boolean derived from `storagePath != null`, NEVER return `storagePath` to client (State 49-02). Wrap in `try { } catch (e) { handleDatabaseError(e) }`. Do NOT modify `getSignedUrl`. Register requires no root.ts change (materialRouter already mounted).
    Fill the 61-00 `material.test.ts` skipped assertions: isHidden forced, type filter, search filter, standalone inclusion, storagePath-absent, getSignedUrl unchanged.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test material --run 2>&1 | grep -Ev '^#' | grep -Eqi 'pass|✓'</automated>
  </verify>
  <acceptance_criteria>
    - `material.ts` contains `listForUser: protectedProcedure` and forces `where.isHidden = false`.
    - Input uses `z.nativeEnum(MaterialType)` for `type`.
    - No diff to the `getSignedUrl` body (grep confirms `FORBIDDEN` + `material.lessons.length === 0` branch intact at :437).
    - Response shape exposes `hasFile` (boolean), not `storagePath`.
    - `material.test.ts` assertions un-skipped and green (`pnpm test material`).
  </acceptance_criteria>
  <done>listForUser returns visible materials with filters; ACL frozen; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Make MaterialCard.lessonId optional for standalone</name>
  <files>apps/web/src/components/learning/MaterialCard.tsx</files>
  <read_first>
    - `apps/web/src/components/learning/MaterialCard.tsx:21` (required `lessonId` prop) + `:46` (`reachGoal` uses lessonId) + `:57-62` (`getSignedUrl` flow + «Доступ к материалу ограничен» toast)
    - `61-RESEARCH.md` Pattern 5 (standalone = externalUrl-only; lessonId optional) + Pitfall 2
    - `61-UI-SPEC.md` §4 (Material card row: type chip, title, CTA «Открыть»/«Скачать»)
  </read_first>
  <action>
    Change the `lessonId` prop to optional (`lessonId?: string`). When `lessonId` is absent (standalone card): render the `externalUrl` «Открыть» path and do NOT call `getSignedUrl` (A8 — standalone is externalUrl-only this pass). Guard `reachGoal` (`:46`) for the missing `lessonId`. Keep existing copy «Доступ к материалу ограничен» / «Материал больше недоступен» (UI-SPEC error states). Leave a placeholder prop slot for `FavoriteButton` (mounted in 61-07) — do not add the heart yet.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -qi 'error TS' && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `MaterialCard.tsx` declares `lessonId?:` (optional).
    - When `lessonId` is undefined, `getSignedUrl` is not invoked (externalUrl path only).
    - `reachGoal` does not throw on missing `lessonId`.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>MaterialCard renders for both lesson-attached and standalone (externalUrl) materials.</done>
</task>

</tasks>

<verification>
- `pnpm test material && pnpm typecheck` green.
- `getSignedUrl` unchanged (security gate).
</verification>

<success_criteria>
`material.listForUser` ships with isHidden enforcement + type/search filters; MaterialCard supports standalone; download ACL untouched.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-03-SUMMARY.md` when done
</output>
