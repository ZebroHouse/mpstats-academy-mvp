---
phase: 61-learning-2-0
plan: 01
type: execute
wave: 2
depends_on: ["61-00"]
files_modified:
  - apps/web/src/components/shared/sidebar.tsx
  - apps/web/src/components/shared/mobile-nav.tsx
  - apps/web/src/components/learning/LearningTabs.tsx
  - apps/web/src/app/(main)/learn/track/page.tsx
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "Sidebar shows an expandable «Обучение» group with 4 sub-items, auto-open on /learn/*"
    - "Active sub-item highlights via pathname===href || pathname.startsWith(href+'/')"
    - "Mobile: single «Обучение» bottom-bar item routes to default; horizontal pill-tab strip exposes 4 sub-sections on /learn/* (md:hidden)"
    - "/learn/track unconditionally server-redirects to /learn/plan"
  artifacts:
    - path: "apps/web/src/components/shared/sidebar.tsx"
      provides: "Expandable «Обучение» nav group + 4 sub-links"
      contains: "/learn/plan"
    - path: "apps/web/src/components/learning/LearningTabs.tsx"
      provides: "Mobile horizontal pill-tab strip for /learn/* sub-sections"
      contains: "overflow-x-auto"
    - path: "apps/web/src/app/(main)/learn/track/page.tsx"
      provides: "Server redirect /learn/track → /learn/plan"
      contains: "redirect('/learn/plan')"
  key_links:
    - from: "sidebar.tsx"
      to: "/learn/{plan,solutions,library,favorites}"
      via: "sub-<Link> hrefs"
      pattern: "/learn/(plan|solutions|library|favorites)"
---

<objective>
Build the navigation chassis for Обучение 2.0: convert the flat «Обучение → /learn» sidebar entry into an expandable group with 4 sub-items (D-01), add the mobile pill-tab pattern (A3), and add the unconditional `/learn/track → /learn/plan` server redirect. The 4 sub-routes do not have content pages yet (61-02 builds them) but the nav links + mobile tabs reference them now.

Purpose: D-01 navigation + route aliases — the skeleton every other wave navigates through.
Output: Expandable desktop submenu, mobile bottom-bar + tab strip, `/learn/track` redirect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-CONTEXT.md
@.planning/phases/61-learning-2-0/61-UI-SPEC.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md

<interfaces>
<!-- Active-state idiom (reuse exactly) — sidebar.tsx:118 -->
const isActive = pathname === item.href || pathname.startsWith(item.href + '/');
// active:  'bg-mp-blue-50 text-mp-blue-600 shadow-mp-sm', icon 'text-mp-blue-500'
// inactive:'text-mp-gray-600 hover:bg-mp-gray-100 hover:text-mp-gray-900', icon 'text-mp-gray-400'

Sub-routes + canon labels (UI-SPEC §1):
- Персональный план → /learn/plan
- Решения под задачу → /learn/solutions
- База знаний → /learn/library
- Избранное → /learn/favorites
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandable «Обучение» group in Sidebar + mobile-nav default + LearningTabs strip</name>
  <files>apps/web/src/components/shared/sidebar.tsx, apps/web/src/components/shared/mobile-nav.tsx, apps/web/src/components/learning/LearningTabs.tsx</files>
  <read_first>
    - `apps/web/src/components/shared/sidebar.tsx:16-53` (flat `navItems`, «Обучение → /learn» entry :35-43) + `:116-141` (map render + active-state :118)
    - `apps/web/src/components/shared/mobile-nav.tsx` (bottom-bar pattern, existing `overflow-x-auto`)
    - `apps/web/src/app/(main)/learn/page.tsx:206-216` (`toggleCourseExpanded` Set-toggle + chevron `rotate-180` idiom — replicate for nav group, NO radix Collapsible)
    - `61-UI-SPEC.md` §1 (Expandable nav group: indent `pl-10`, `text-body-sm`, chevron, A3 mobile decision)
    - `61-PATTERNS.md` § Active-nav state + § sidebar change site
  </read_first>
  <action>
    Sidebar: replace the single flat «Обучение → /learn» item (`sidebar.tsx:35-43`) with an expandable group. Introduce a `NavGroup` shape `{ title:'Обучение', icon, children: {title,href}[] }` whose `children` are the 4 sub-routes/labels from interfaces. Render: a header button (book icon + «Обучение» + chevron) using local `useState(open)` defaulting to `pathname.startsWith('/learn')`; when open, render the 4 sub-`<Link>`s indented `pl-10`, `text-body-sm`. Reuse the exact active class strings from `:124-138`. Sub-item active = `pathname === href || pathname.startsWith(href + '/')`; group header lit when `pathname.startsWith('/learn')`. Chevron animates `rotate-180` when open (same idiom as `learn/page.tsx:206`). Keep `data-tour="sidebar-nav"` on the `<nav>`; add `data-tour="learn-submenu"` on the group header (D-10 anchor per UI-SPEC §Interaction).
    mobile-nav: keep ONE bottom-bar «Обучение» item (book icon) pointing to `/learn/plan` (default); its active state stays lit on any `pathname.startsWith('/learn')`.
    Create `LearningTabs.tsx` (`'use client'`): a `md:hidden` horizontal `overflow-x-auto` pill-tab strip rendering the 4 sub-sections as `<Link>` pills; active pill `bg-mp-blue-50 text-mp-blue-600`, inactive `text-mp-gray-600`; active by `pathname.startsWith`. Export for 61-02 to mount at the top of each `/learn/*` page.
    Do NOT touch `/learn/job/[slug]` or `/learn/[id]` routes (D-01).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | grep -Ev '^#' | grep -Eqi 'error TS' && echo TYPECHECK_FAIL || echo TYPECHECK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `sidebar.tsx` contains all four hrefs `/learn/plan`, `/learn/solutions`, `/learn/library`, `/learn/favorites` and the labels «Персональный план», «Решения под задачу», «База знаний», «Избранное».
    - Group header carries `data-tour="learn-submenu"`; chevron uses `rotate-180`.
    - No `@radix-ui` Collapsible import added (hand-rolled toggle).
    - `LearningTabs.tsx` exists, is `'use client'`, contains `overflow-x-auto` and `md:hidden`.
    - `mobile-nav.tsx` «Обучение» item href is `/learn/plan` and stays active on `pathname.startsWith('/learn')`.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Desktop submenu expands/collapses with correct active states; mobile bottom-bar + tab strip wired to the 4 sub-routes.</done>
</task>

<task type="auto">
  <name>Task 2: /learn/track unconditional server redirect to /learn/plan</name>
  <files>apps/web/src/app/(main)/learn/track/page.tsx</files>
  <read_first>
    - `apps/web/src/app/(main)/learn/track/page.tsx` (current client component to REPLACE)
    - `apps/web/src/app/(main)/layout.tsx:34,49,64` (Server Component `redirect()` from `next/navigation` — the analog)
    - `61-RESEARCH.md` Pitfall 1 + Code Example «Server redirect alias»; `61-UI-SPEC.md` §Interaction (`/learn/track` row)
  </read_first>
  <action>
    Replace the ENTIRE `/learn/track/page.tsx` with a Server Component that calls `redirect('/learn/plan')` from `next/navigation` at module/function top. No `'use client'`, no `useEffect`, no `router.push` (router-cache loop — RESEARCH Pitfall 1). This preserves bookmarks/emails/internal links to `/learn/track` (D-01).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test:e2e learn-redirect 2>&1 | grep -Ev '^#' | grep -Eqi 'track.*plan|learn redirect'</automated>
  </verify>
  <acceptance_criteria>
    - File contains `redirect('/learn/plan')` and imports `redirect` from `next/navigation`.
    - File does NOT contain `'use client'`, `useEffect`, or `router.push`.
    - The `learn-redirect` e2e `/learn/track`→`/learn/plan` assertion (un-skipped from 61-00) passes.
  </acceptance_criteria>
  <done>Navigating to /learn/track lands on /learn/plan via server redirect.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` green.
- `pnpm test:e2e learn-redirect` `/learn/track` case green; `/learn` default case may remain skipped until 61-02.
- Manual (staging): desktop submenu expands on /learn/*, active sub-item highlighted; mobile tab strip visible on /learn/* only.
</verification>

<success_criteria>
Navigation chassis live: expandable «Обучение» submenu (desktop) + mobile bottom-bar/tab-strip, `/learn/track` redirect working. UI strings use canon labels (no «трек»/«плейбук»/«джоба»).
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-01-SUMMARY.md` when done
</output>
