---
phase: 61-learning-2-0
plan: 07
type: execute
wave: 8
depends_on: ["61-06"]
files_modified:
  - scripts/migrate-track-to-favorites.ts
  - scripts/__tests__/migrate-track-to-favorites.test.ts
  - apps/web/src/components/learning/FavoriteButton.tsx
  - apps/web/src/components/learning/JobCard.tsx
  - apps/web/src/components/learning/MaterialCard.tsx
  - apps/web/src/components/learning/LessonResultCard.tsx
  - apps/web/src/app/(main)/learn/favorites/page.tsx
  - apps/web/src/app/(main)/learn/plan/page.tsx
autonomous: false
requirements: [D-03, D-06, D-07, D-10]

must_haves:
  truths:
    - "Idempotent script migrates custom-section lessons → Favorite(LESSON) and addedJobs[] → Favorite(JOB); re-run produces no duplicates"
    - "LessonProgress row count is identical before and after the migration"
    - "FavoriteButton toggles favorite.add/remove optimistically with rollback + error toast; pink-filled = saved"
    - "FavoriteButton mounted on Job, Material, and Lesson result cards seeded by favorite.isFavorited"
    - "Персональный план shows ONLY diagnostic sections; manual additions live in Избранное"
    - "Избранное page lists saved items via favorite.list with type filter"
    - "All 3 getRecommendedPath consumers updated together (AgentSearch trackedJobIds, /learn/plan, former track page) — no contradictory in-track/in-favorites state"
  artifacts:
    - path: "scripts/migrate-track-to-favorites.ts"
      provides: "idempotent track→favorites data migration"
      contains: "skipDuplicates"
    - path: "apps/web/src/components/learning/FavoriteButton.tsx"
      provides: "shared optimistic heart toggle"
      contains: "favorite.add"
    - path: "apps/web/src/app/(main)/learn/favorites/page.tsx"
      provides: "Избранное listing via favorite.list"
      contains: "favorite.list"
  key_links:
    - from: "FavoriteButton"
      to: "favorite.add / favorite.remove"
      via: "optimistic mutation"
      pattern: "favorite\\.(add|remove)"
    - from: "favorites/page.tsx"
      to: "favorite.list"
      via: "useQuery"
      pattern: "favorite\\.list"
    - from: "/learn/plan"
      to: "getRecommendedPath (diagnostic sections only)"
      via: "useQuery, no addedJobs/custom"
      pattern: "getRecommendedPath"
---

<objective>
Complete the План/Избранное split (D-03): migrate existing manual track additions into `Favorite` (idempotent, `LessonProgress` untouched), build the shared `FavoriteButton`, mount it on the entity cards, wire the Избранное page, and ensure План shows only diagnostic recommendations. All three `getRecommendedPath` consumers are updated in this one plan to avoid contradictory state (RESEARCH Pitfall 3).

Purpose: D-03 plan/favorites split + D-06 FavoriteButton UX + D-07 idempotent data migration.
Output: migration script (run on prod), FavoriteButton on 3 card types, Избранное listing, diagnostic-only План.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-learning-2-0/61-CONTEXT.md
@.planning/phases/61-learning-2-0/61-PATTERNS.md
@.planning/phases/61-learning-2-0/61-RESEARCH.md
@.planning/phases/61-learning-2-0/61-UI-SPEC.md

<interfaces>
<!-- favorite.{add,remove,list,isFavorited} from 61-06 -->
<!-- custom-section detection (VERIFIED RESEARCH): section.id === 'custom' (learning.ts:897/1043), title «Мои уроки»; rebuild excludes :936; rebuildTrack keys :1214 -->
<!-- addedJobs[]: LearningPath.addedJobs JSON array of jobId (schema.prisma:217, learning.ts:1140) -->
<!-- FavoriteButton (UI-SPEC §5): lucide Heart w-5 h-5 in min-h-11 min-w-11; off=outline text-mp-gray-400; on=filled mp-pink-500 (A5); aria-pressed + aria-label «Добавить в избранное»/«Убрать из избранного»; no confirm dialog -->
<!-- optimistic pattern: learn/page.tsx:115-142 (onMutate/onError rollback/onSettled invalidate); click-inside-card guard JobCard.tsx:55-61 (preventDefault+stopPropagation) -->
<!-- error toast (UI-SPEC): «Не удалось обновить избранное. Попробуйте ещё раз.» -->
<!-- script harness analog: scripts/backfill-referral-codes.ts (--dry-run/--apply, main().catch().finally($disconnect)) -->
<!-- A4: solutions result keeps BOTH «В план» (addJobToTrack) AND FavoriteButton(JOB) -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `favorite.*` | heart toggle scoped by ctx.user.id (61-06) |
| migration script → PROD DB | one-off idempotent re-classification of existing LearningPath data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07-01 | Tampering/data-loss | migration corrupts LearningPath or LessonProgress | mitigate | `LessonProgress` NEVER written (hard rule D-03/D-07); script reads-only LearningPath, writes only Favorite via `createMany({skipDuplicates:true})`; PITR backup + `--dry-run` first |
| T-61-07-02 | Tampering | non-idempotent re-run duplicates favorites | mitigate | `@@unique([userId,itemType,itemId])` + `skipDuplicates`; test asserts re-run stable |
| T-61-07-03 | Information Disclosure | favorites/plan surfaces hidden content | mitigate | `favorite.list` resolution + plan query keep `isHidden:false` (61-06 + getRecommendedPath :323) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotent migrate-track-to-favorites script + tests</name>
  <files>scripts/migrate-track-to-favorites.ts, scripts/__tests__/migrate-track-to-favorites.test.ts</files>
  <behavior>
    - reads every LearningPath, collects custom-section lessonIds → Favorite(LESSON), addedJobs[] → Favorite(JOB)
    - createMany({skipDuplicates:true}); re-run leaves row count unchanged
    - LessonProgress row count identical before/after (never written)
    - --dry-run prints counts without writing; --apply writes
  </behavior>
  <read_first>
    - `scripts/backfill-referral-codes.ts` (harness: argv flags, PrismaClient, main().catch().finally($disconnect))
    - `packages/api/src/routers/learning.ts:897` (custom marker), `:1140` (addedJobs), `:1214` (custom detection in rebuildTrack) + the `parseLearningPath` helper used there
    - `scripts/__tests__/migrate-track-to-favorites.test.ts` (61-00 stub to fill)
    - `61-RESEARCH.md` Code Example «Idempotent data migration» + § custom detection VERIFIED; `61-PATTERNS.md` § migrate-track-to-favorites
  </read_first>
  <action>
    Create `scripts/migrate-track-to-favorites.ts` mirroring `backfill-referral-codes.ts`: `--dry-run`/`--apply` flags. For each `LearningPath`: parse `lessons` (existing `parseLearningPath` helper), find the `custom` section (`section.id === 'custom'`), push its `lessonIds` as `{userId, itemType:'LESSON', itemId}`; push each `addedJobs[]` element as `{userId, itemType:'JOB', itemId}`. On `--apply`: `prisma.favorite.createMany({ data, skipDuplicates: true })`. NEVER touch `LessonProgress`. Print per-user and total counts on `--dry-run`.
    Fill the 61-00 `migrate-track-to-favorites.test.ts`: idempotency (run twice → stable count), custom→LESSON + addedJobs→JOB mapping, LessonProgress count snapshot equal before/after (stub prisma records favorite.createMany; assert no LessonProgress write call).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test migrate-track-to-favorites --run 2>&1 | grep -Ev '^#' | grep -Eqi 'pass|✓'</automated>
  </verify>
  <acceptance_criteria>
    - `migrate-track-to-favorites.ts` exists with `--dry-run`/`--apply` and `createMany({ ... skipDuplicates: true })`.
    - Script keys lesson migration on `section.id === 'custom'`.
    - `grep -i lessonprogress scripts/migrate-track-to-favorites.ts` returns 0 (never written).
    - Test asserts idempotent re-run + LessonProgress untouched; `pnpm test migrate-track-to-favorites` green.
  </acceptance_criteria>
  <done>Idempotent script ready; tests prove re-run safety and LessonProgress isolation.</done>
</task>

<task type="auto">
  <name>Task 2: FavoriteButton + mount on Job/Material/Lesson cards; wire Избранное page; План diagnostic-only (3 consumers)</name>
  <files>apps/web/src/components/learning/FavoriteButton.tsx, apps/web/src/components/learning/JobCard.tsx, apps/web/src/components/learning/MaterialCard.tsx, apps/web/src/components/learning/LessonResultCard.tsx, apps/web/src/app/(main)/learn/favorites/page.tsx, apps/web/src/app/(main)/learn/plan/page.tsx</files>
  <read_first>
    - `apps/web/src/app/(main)/learn/page.tsx:115-142` (optimistic onMutate/onError/onSettled pattern) + `JobCard.tsx:55-61` (click-inside-card guard) + `AgentSearch.tsx:15-19` (trackedJobIds — consumer #1)
    - `apps/web/src/app/(main)/learn/plan/page.tsx` (61-02 — consumer #2, make diagnostic-only) + `packages/api/src/routers/learning.ts:274-333` (getRecommendedPath: diagnostic sections + addedJobs :317-333 + custom — what to stop consuming in План)
    - `apps/web/src/components/learning/MaterialCard.tsx` + `LessonResultCard.tsx` (61-03/61-04 slots reserved for the heart)
    - `61-UI-SPEC.md` §5 (FavoriteButton contract, A5 pink), §6 (Избранное empty state), Copywriting (error toast, «В план» A4)
    - `61-PATTERNS.md` § FavoriteButton + § Optimistic mutation; `61-RESEARCH.md` Pitfall 3 (3 consumers together)
  </read_first>
  <action>
    Create `FavoriteButton.tsx` (`'use client'`): props `{ itemType: FavoriteItemType; itemId: string; initialFavorited?: boolean }`. Hoist ALL `useMutation`/`useUtils` above any early return (Rules of Hooks — CLAUDE.md gotcha). Optimistic toggle: `favorite.add`/`favorite.remove` by current state, `onMutate` snapshot + flip heart, `onError` rollback + `toast.error('Не удалось обновить избранное. Попробуйте ещё раз.')`, `onSettled` invalidate `favorite.isFavorited`/`favorite.list`. lucide `Heart` `w-5 h-5` in `min-h-11 min-w-11`; off outline `text-mp-gray-400`, on filled `mp-pink-500`; `aria-pressed` + aria-label per interfaces. Click guard `e.preventDefault();e.stopPropagation()` when on a `<Link>` card. No confirm dialog.
    Mount `FavoriteButton` on `JobCard` (JOB — alongside «В план», A4), `MaterialCard` (MATERIAL), `LessonResultCard` (LESSON). Seed initial state from `favorite.isFavorited` batch at the list/catalog level (pass `initialFavorited`).
    favorites/page.tsx: replace the 61-02 placeholder with a real listing — `trpc.favorite.list.useQuery({ itemType })` rendering the resolved entities as the matching card type, with a type-filter chip row (Уроки/Решения/Материалы). Keep the empty-state «В избранном пусто» when no items.
    plan/page.tsx: make it diagnostic-only — read `getRecommendedPath` and render ONLY diagnostic sections; remove any consumption of `addedJobs`/custom (those now live in Избранное). 
    Consumer #1 (AgentSearch trackedJobIds): keep «В план» (addJobToTrack) green-badge semantics intact, but ensure it does not conflict with the heart state (in-track green vs in-favorites pink are independent — A4/A5). Verify all 3 consumers read the post-split shape without contradiction (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm test --run 2>&1 | grep -Ev '^#' | grep -Eqi 'fail|✗' && echo HASFAIL || echo ALLPASS</automated>
  </verify>
  <acceptance_criteria>
    - `FavoriteButton.tsx` exists, calls `favorite.add`/`favorite.remove`, uses `mp-pink-500` filled state, `aria-pressed`, and a `min-h-11 min-w-11` tap target.
    - All `useMutation`/`useUtils` are above any early return (no Rules-of-Hooks violation).
    - `FavoriteButton` mounted in `JobCard`, `MaterialCard`, `LessonResultCard`.
    - `favorites/page.tsx` calls `favorite.list` and renders type-filtered cards; empty state «В избранном пусто» retained.
    - `plan/page.tsx` no longer consumes `addedJobs`/`custom` (grep returns 0 for those identifiers in plan page).
    - `pnpm test && pnpm typecheck` green.
  </acceptance_criteria>
  <done>Heart toggle works on all 3 card types; Избранное lists saved items; План is diagnostic-only; 3 consumers consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Run data migration on PROD (backup → dry-run → apply)</name>
  <what-built>
    The idempotent `scripts/migrate-track-to-favorites.ts` re-classifies existing manual track additions (custom-section lessons + addedJobs) into `Favorite` rows on the live prod DB (~170 users). Requires the Favorite table from 61-06 to already exist. `LessonProgress` is never written (hard rule).
  </what-built>
  <how-to-verify>
    1. Confirm PITR backup enabled on `saecuecevicwjkpmaoot` (Database → Backups).
    2. Snapshot counts: `SELECT count(*) FROM "LessonProgress";` and `SELECT count(*) FROM "Favorite";` — record both.
    3. Run `--dry-run` against prod DATABASE_URL; review printed per-user/total LESSON+JOB counts for sanity.
    4. Run `--apply`.
    5. Re-run `--apply` once more → assert `Favorite` count unchanged from step-4 result (idempotency on real data).
    6. `SELECT count(*) FROM "LessonProgress";` MUST equal the step-2 value (untouched).
    7. Spot-check a known user in /learn/favorites — their former «Мои уроки» + добавленные решения appear under Избранное; /learn/plan shows only diagnostic sections.
    This task touches PROD data — requires owner action / prod DATABASE_URL.
  </how-to-verify>
  <resume-signal>Type "migrated" once apply + idempotent re-run pass and LessonProgress count is unchanged, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test && pnpm typecheck` green (favorite, migrate-track, AgentSearch suites included).
- Prod: Favorite populated; LessonProgress count unchanged; idempotent re-run stable.
- Staging QA: heart toggle on all 3 card types; Избранное listing + filter; План diagnostic-only.
</verification>

<success_criteria>
План/Избранное split complete (D-03): manual additions migrated idempotently to Favorite without touching LessonProgress, FavoriteButton live on all entity cards, Избранное page functional, План purely diagnostic, all 3 getRecommendedPath consumers consistent.
</success_criteria>

<output>
Create `.planning/phases/61-learning-2-0/61-07-SUMMARY.md` when done
</output>
