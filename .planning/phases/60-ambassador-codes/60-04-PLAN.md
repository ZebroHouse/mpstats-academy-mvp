---
phase: 60-ambassador-codes
plan: 04
type: execute
wave: 3
depends_on: ["60-01", "60-02", "60-03"]
files_modified:
  - apps/web/tests/e2e/ambassador-code.spec.ts
  - .planning/phases/60-ambassador-codes/60-HUMAN-UAT.md
  - .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Playwright E2E spec exists and runs on staging — admin creates code, incognito user registers, trial granted, currentUses=1"
    - "Migration applied to STAGING first, verified, then applied to PROD via prisma migrate deploy from MAAL repo"
    - "Migration on prod is additive — no DROP/TRUNCATE in applied SQL (verified by re-grepping after deploy)"
    - "Phase 53A regression suite passes on staging post-deploy"
    - "Manual UAT checklist lives at 60-HUMAN-UAT.md and is checked off by owner before phase close"
  artifacts:
    - path: "apps/web/tests/e2e/ambassador-code.spec.ts"
      provides: "Playwright E2E: admin-create → incognito-register → trial-assert"
      min_lines: 80
    - path: ".planning/phases/60-ambassador-codes/60-HUMAN-UAT.md"
      provides: "Manual UAT checklist with at least 6 scenarios"
      contains: "AMB-TEST"
    - path: ".planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md"
      provides: "Step-by-step staging→prod migration commands with rollback procedure"
      contains: "prisma migrate deploy"
  key_links:
    - from: ".planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md"
      to: "packages/db/prisma/migrations/20260528000000_add_referral_code_table/migration.sql"
      via: "deploy command applies this exact migration"
      pattern: "20260528000000_add_referral_code_table"
---

<objective>
Close Phase 60 with: (a) Playwright E2E spec that proves end-to-end ambassador flow on staging, (b) HUMAN-UAT checklist for owner, (c) DEPLOY-RUNBOOK that applies the additive migration from 60-01 to staging then prod, (d) blocking owner checkpoint after staging verification before prod migration.

Purpose: Phase 60 ships nothing until migration is applied to prod and code is verified end-to-end with real Supabase. This plan converts test-passing code into production reality.
Output: Ambassador codes live in prod, owner-tested manual scenarios green, regression intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/60-ambassador-codes/60-CONTEXT.md
@.planning/phases/60-ambassador-codes/60-SPEC.md
@.planning/phases/60-ambassador-codes/60-01-SUMMARY.md
@.planning/phases/60-ambassador-codes/60-02-SUMMARY.md
@.planning/phases/60-ambassador-codes/60-03-SUMMARY.md
@packages/db/prisma/migrations/20260528000000_add_referral_code_table/migration.sql

<interfaces>
<!-- Staging + prod deploy workflow from CLAUDE.md -->

Staging:
- URL: https://staging.platform.mpstats.academy (basic auth `team`)
- Deploy: `ssh deploy@89.208.106.208 && cd /home/deploy/maal && git checkout phase-60-ambassador-codes && docker compose -p maal-staging -f docker-compose.staging.yml up -d --build`
- After staging deploy MUST `git checkout master` on VPS BEFORE any prod redeploy.
- Rule from `feedback_staging_docker_no_cache_required.md`: `--no-cache` flag + bundle content-check via `grep` in `.next/server/app` BEFORE declaring healthy.

Prod migration command (from packages/db/package.json):
- `pnpm --filter @mpstats/db exec prisma migrate deploy` — applies pending migrations from `packages/db/prisma/migrations/` to DATABASE_URL.
- Must run from MAAL repo only (PROD DATABASE SAFETY rule #1, incident 2026-05-12).

Existing test user: `tester@mpstats.academy` (per CLAUDE.md QA section).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Playwright E2E spec for ambassador signup flow</name>
  <files>apps/web/tests/e2e/ambassador-code.spec.ts</files>
  <read_first>
    - apps/web/tests/e2e/ (list existing specs for setup patterns — page object, baseURL, login helper)
    - .planning/phases/60-ambassador-codes/60-SPEC.md (Sprint 60-04 E2E acceptance)
    - .planning/phases/60-ambassador-codes/60-03-SUMMARY.md (admin UI selectors confirmed)
  </read_first>
  <action>
Create `apps/web/tests/e2e/ambassador-code.spec.ts`. Single test «ambassador code flow: admin create → user register → trial granted».

Steps:
1. **Admin login** — use existing helper or inline Supabase admin login (cookies set via API per existing E2E patterns). Identify the project's E2E login mechanism by reading 1-2 existing specs in `apps/web/tests/e2e/`.
2. **Create code via admin UI** — `page.goto('/admin/referrals/codes')`, click «+ Создать код», fill label=«E2E Test»+nonce, refereeTrialDays=14, code=`AMB-E2E${Date.now().toString().slice(-6)}` (deterministic nonce), submit, assert toast «Код создан».
3. **Capture code** — read row from table or capture from mutation response. Save `testCode` variable.
4. **Logout admin** — clear cookies / new browser context.
5. **Incognito register** — `await context.clearCookies()`, `page.goto('/register?ref=' + testCode)`. Assert referral banner «+14 дней» visible OR similar (whichever exists). Fill register form with `e2e+${Date.now()}@mpstats.academy` + password + phone. Submit.
6. **Confirm DOI** — call test-only confirm endpoint OR mock via Supabase admin (existing patterns in repo). If repo has no DOI-bypass helper, use `prisma.userProfile.update({...})` via test setup hook to mark user confirmed. Then call `/auth/confirm` route or trigger `issueReferralOnSignup` directly (last resort: trigger via tRPC test helper).
7. **Assert DB state** via tRPC introspection endpoint OR direct prisma query in test:
   - `prisma.subscription.findFirst({ where: { userId: newUserId, status: 'TRIAL' } })` — expect non-null, `currentPeriodEnd` ≈ NOW + 14 days (tolerance ±60 seconds).
   - `prisma.referral.findFirst({ where: { referredUserId: newUserId } })` — expect `codeType === 'AMBASSADOR'`, `referrerUserId === null`, `codeId === <created code id>`.
   - `prisma.referralCode.findUnique({ where: { code: testCode } })` — expect `currentUses === 1`.
8. **Cleanup** — delete test user (cascade removes Subscription + Referral) AND delete test ReferralCode row in afterAll hook.

If the repo lacks DOI-bypass infrastructure for E2E, mark this test `.skip()` with comment `// SKIP: requires DOI bypass — UAT will validate manually via 60-HUMAN-UAT.md` and surface this gap in the SUMMARY. Owner has already authorized a fallback to manual UAT in `.planning/phases/56-entry-flow-redesign/56-HUMAN-UAT.md` precedent.
  </action>
  <verify>
    <automated>pnpm --filter web test:e2e -- ambassador-code</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/tests/e2e/ambassador-code.spec.ts` exists with at least 80 lines
    - Spec is either runnable (test:e2e exits 0) OR explicitly `.skip()`'d with reason
    - `grep -c "AMB-E2E\\|testCode" apps/web/tests/e2e/ambassador-code.spec.ts` ≥ 2
    - `grep -c "currentUses" apps/web/tests/e2e/ambassador-code.spec.ts` ≥ 1
    - `grep -c "codeType.*AMBASSADOR\\|AMBASSADOR.*codeType" apps/web/tests/e2e/ambassador-code.spec.ts` ≥ 1
  </acceptance_criteria>
  <done>E2E spec exists. Either passing on local/staging OR explicitly skipped with manual UAT fallback documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write HUMAN-UAT checklist and DEPLOY-RUNBOOK</name>
  <files>
    .planning/phases/60-ambassador-codes/60-HUMAN-UAT.md,
    .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md
  </files>
  <read_first>
    - .planning/phases/56-entry-flow-redesign/56-HUMAN-UAT.md if exists (template precedent)
    - CLAUDE.md sections «Deploy», «Staging», «PROD DATABASE SAFETY»
  </read_first>
  <action>
**File 1: `60-HUMAN-UAT.md`** — markdown checklist. Sections:

1. **Pre-conditions** — staging deployed, migration applied to staging.
2. **Happy path** — admin creates AMB-UAT01 with refereeTrialDays=1, copies link, opens incognito + different network (4G hotspot or VPN), registers fresh email, confirms DOI, navigates to /profile, asserts «Trial до <дата+1 день>». Returns to admin, asserts activations=1 on the code's row.
3. **Limit: maxUses** — admin creates AMB-UAT02 with maxUses=1, refereeTrialDays=1. Activate via incognito #1 → success. Try again via incognito #2 → no trial granted, currentUses still 1, Sentry shows `referral.ambassador.limit_hit`.
4. **Limit: expiresAt** — admin creates AMB-UAT03 with expiresAt=NOW-1h via SQL (or wait), tries registration → no trial.
5. **Limit: isActive=false** — admin creates AMB-UAT04, toggles inactive via UI, registration → no trial.
6. **D-03 stale user** — admin creates AMB-UAT05, copy link, sign in to EXISTING tester@mpstats.academy account → no Subscription change, no Referral row, Sentry `referral.ambassador.stale_user`.
7. **Cross-table collision** — admin tries to create code with value matching existing UserProfile.referralCode (any REF-* in DB) → UI shows error «Код уже занят».
8. **Phase 53A regression** — test user with REF-XXXXXX shares link, friend registers → original 14-day trial + bonus package flow works (unchanged from 53A).
9. **CQ verification** — open CarrotQuest UI, find UAT01 friend lead, confirm `pa_referral_source = "Блогер UAT01"`, `pa_referral_trial_days = 1`, event `pa_ambassador_signup` fired.

Each item: `- [ ] Item` checkbox, expected result on the right.

**File 2: `60-DEPLOY-RUNBOOK.md`** — sequential commands:

1. **Pre-deploy checks**:
   ```
   git status                                # clean
   git branch --show-current                 # phase-60-ambassador-codes
   pnpm --filter @mpstats/api test           # all green
   pnpm --filter web test                    # all green
   ```

2. **Staging deploy**:
   ```
   ssh deploy@89.208.106.208
   cd /home/deploy/maal
   git fetch && git checkout phase-60-ambassador-codes && git pull
   pnpm --filter @mpstats/db exec prisma migrate deploy   # applies 20260528... migration to STAGING DB (must verify DATABASE_URL points to staging)
   docker compose -p maal-staging -f docker-compose.staging.yml up -d --build --no-cache
   # Wait for healthy, then bundle content-check
   docker exec maal-staging-web grep -lE "createAmbassadorCode|listAmbassadorCodes" /.next/server -r | head -3
   git checkout master   # CRITICAL — leave VPS on master for next prod redeploy
   ```

3. **Staging UAT** — owner runs 60-HUMAN-UAT.md scenarios 1-8 on staging.platform.mpstats.academy.

4. **Prod migration** (only after staging UAT green):
   ```
   # Local MAAL repo, on phase-60-ambassador-codes branch:
   # Set DATABASE_URL to PROD (prefix saecuecevicwjkpmaoot)
   echo $DATABASE_URL | grep saecuecevicwjkpmaoot  # confirms prod
   pnpm --filter @mpstats/db exec prisma migrate deploy
   # Verify migration recorded:
   psql $DATABASE_URL -c "SELECT migration_name, finished_at FROM _prisma_migrations ORDER BY finished_at DESC LIMIT 3;"
   ```

5. **Prod code deploy** (after merge to master):
   ```
   ssh deploy@89.208.106.208
   cd /home/deploy/maal
   git checkout master && git pull
   docker compose down && docker compose build --no-cache && docker compose up -d
   ```

6. **Post-deploy verification**:
   - /admin/referrals/codes loads for admin user.
   - Create AMB-PROD-SMOKE-01 code with maxUses=1, trial=1 day.
   - Activate via incognito with throwaway email.
   - Confirm trial in /profile, currentUses=1 in admin UI.
   - Delete the smoke code via SQL after verification.

7. **Rollback procedure** — schema additive ⇒ no functional rollback needed. To disable feature: SQL `UPDATE "ReferralCode" SET "isActive"=false WHERE "codeType"='AMBASSADOR';` (kills all active codes instantly). Code-level rollback via `git revert` of merged PR.

8. **PITR safety note** — if anything goes wrong, Supabase PITR can restore to pre-migration timestamp. See CLAUDE.md «Recovery procedure».
  </action>
  <verify>
    <automated>test -f .planning/phases/60-ambassador-codes/60-HUMAN-UAT.md && test -f .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AMB-UAT" .planning/phases/60-ambassador-codes/60-HUMAN-UAT.md` ≥ 5 (covers scenarios 2-6)
    - `grep -c "prisma migrate deploy" .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md` ≥ 2 (staging + prod)
    - `grep -c "saecuecevicwjkpmaoot" .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md` ≥ 1 (prod ref check)
    - `grep -c "Rollback\\|PITR" .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md` ≥ 1
    - `grep -c "git checkout master" .planning/phases/60-ambassador-codes/60-DEPLOY-RUNBOOK.md` ≥ 1 (staging→master switch rule)
  </acceptance_criteria>
  <done>Two markdown files exist with full UAT scenarios and deploy commands. Cover D-01..D-04 limits, D-03 stale-user, 53A regression, CQ verification, and rollback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING — apply migration to STAGING + run UAT before prod</name>
  <what-built>
    Plans 60-01..60-03 produced schema, orchestrator, admin UI. Plan 60-04 Task 1 produced E2E spec. Plan 60-04 Task 2 produced runbook + UAT checklist.

    Owner must:
    1. Apply migration to STAGING via 60-DEPLOY-RUNBOOK.md step 2.
    2. Run all 9 scenarios in 60-HUMAN-UAT.md against staging.platform.mpstats.academy.
    3. Approve or block before any prod deploy.
  </what-built>
  <how-to-verify>
    1. SSH into VPS: `ssh deploy@89.208.106.208`.
    2. Follow `60-DEPLOY-RUNBOOK.md` step 2 (staging deploy) exactly.
    3. Verify migration applied: `docker exec maal-staging-web psql $DATABASE_URL -c "SELECT migration_name FROM _prisma_migrations WHERE migration_name LIKE '20260528%';"` returns one row.
    4. Run all 9 UAT scenarios in `60-HUMAN-UAT.md` on staging.
    5. Verify CQ events arrive in CarrotQuest (scenario 9).
    6. Confirm 53A regression (scenario 8) — friend-of-friend flow works.
  </how-to-verify>
  <resume-signal>Type "uat-passed" to approve prod deploy, or describe failures to block. On block, fix code and re-run staging deploy before re-approval.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: BLOCKING — owner runs prisma migrate deploy on PROD from MAAL repo</name>
  <what-built>
    Migration verified additive (Plan 60-01 Task 3) + UAT green on staging (Plan 60-04 Task 3). Only manual step required: apply migration to live PROD Supabase from MAAL repo.

    This is a true human-action checkpoint: command must run with prod credentials on owner's machine, and per CLAUDE.md PROD DATABASE SAFETY rule #1, only the owner may run prisma against prod.
  </what-built>
  <how-to-verify>
    From local MAAL repo, on `phase-60-ambassador-codes` branch:

    1. Confirm `$DATABASE_URL` points to prod: `echo $DATABASE_URL | grep -c saecuecevicwjkpmaoot` returns 1.
    2. Confirm pending migration: `pnpm --filter @mpstats/db exec prisma migrate status` shows `20260528000000_add_referral_code_table` as pending.
    3. Apply: `pnpm --filter @mpstats/db exec prisma migrate deploy`.
    4. Verify: `psql $DATABASE_URL -c "SELECT migration_name, finished_at FROM _prisma_migrations WHERE migration_name = '20260528000000_add_referral_code_table';"` returns one row with `finished_at` set.
    5. Verify schema: `psql $DATABASE_URL -c "\\d \"ReferralCode\""` shows the table with expected columns.

    On any error: rollback via PITR (see CLAUDE.md). Do NOT run `prisma db push` as recovery.
  </how-to-verify>
  <resume-signal>Type "migration-applied" to proceed with code deploy, or "migration-failed: <reason>" to block and trigger PITR rollback.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update CLAUDE.md «Active Branches» + ROADMAP — record Phase 60 shipped state</name>
  <files>
    CLAUDE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - CLAUDE.md (sections «Current Status», «Active Branches», «Last Session»)
    - .planning/ROADMAP.md (Milestones section)
  </read_first>
  <action>
1. In `CLAUDE.md`:
   - Add row to «Current Status» milestones table: `| v2.0 Ambassador Codes | Shipped <DATE> (Phase 60 — admin-managed external blogger codes with custom trial duration) |` (use today's date).
   - Update «Active Branches» — note phase-60 merged (or pending merge).
   - Append «Last Session» bullet summarizing: schema (new ReferralCode table + AMBASSADOR enum + Referral.codeId), orchestrator extension, admin UI under /admin/referrals/codes, deploy via prisma migrate deploy.

2. In `.planning/ROADMAP.md`:
   - Add Phase 60 entry under the appropriate milestone section (likely a new v2.0 or extend v1.5 Growth & Monetization).
   - Mark as `[x] Phase 60: Ambassador Codes` with commit/PR refs.

Use HEREDOC-free Edit tool calls — the existing CLAUDE.md is large, use targeted edits.
  </action>
  <verify>
    <automated>grep -c "Phase 60\\|ambassador-codes\\|Ambassador Codes" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 60\\|Ambassador" CLAUDE.md` ≥ 2
    - `grep -c "Phase 60" .planning/ROADMAP.md` ≥ 1
    - `git diff CLAUDE.md` shows only additions, no removed sections (use `git diff --stat`)
  </acceptance_criteria>
  <done>CLAUDE.md and ROADMAP reflect Phase 60 shipped state. Ready for branch merge + post-deploy commit.</done>
</task>

</tasks>

<verification>
- E2E spec exists (runnable or explicitly skipped with reason)
- 60-HUMAN-UAT.md has all 9 scenarios with checkboxes
- 60-DEPLOY-RUNBOOK.md has staging + prod commands + rollback
- Staging UAT all-green (Task 3 checkpoint)
- Prod migration applied without error (Task 4 checkpoint)
- CLAUDE.md + ROADMAP updated (Task 5)
</verification>

<success_criteria>
- Migration `20260528000000_add_referral_code_table` recorded in prod `_prisma_migrations` table with `finished_at` set.
- /admin/referrals/codes accessible by admin user on https://platform.mpstats.academy.
- One smoke AMB-PROD-SMOKE-01 code successfully activated by throwaway email, verified in admin stats, then deleted.
- No Phase 53A regression (existing REF-* user-to-user flow works on prod).
- CQ events `pa_ambassador_signup` visible in CarrotQuest dashboard.
</success_criteria>

<output>
Create `.planning/phases/60-ambassador-codes/60-04-SUMMARY.md` with: deploy timestamp, migration verification SQL output, UAT scenario results (9/9 or list failures), smoke code id + cleanup confirmation.
</output>
