---
phase: 60-ambassador-codes
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - apps/web/src/lib/referral/issue.ts
  - apps/web/src/lib/referral/issue.test.ts
  - apps/web/src/lib/referral/fraud-checks.ts
  - apps/web/src/lib/referral/fraud-checks.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "New user with cookie ref=AMB-XXX gets TRIAL Subscription with refereeTrialDays duration on signup"
    - "Referral row created with codeType=AMBASSADOR, referrerUserId=null, codeId=<ambassador.id>"
    - "ReferralCode.currentUses increments atomically inside transaction; race-overflow rolls back"
    - "User older than 5 minutes at attribution time → silent skip, no Subscription, no Referral, no currentUses increment (D-03)"
    - "Ambassador code with expiresAt past / isActive=false / maxUses reached → Sentry info log + early return"
    - "checkFraudSignals tolerates referrerId=null (ambassador branch) without throwing"
    - "Phase 53A user-to-user branch unchanged — all 53A regression tests still pass"
    - "pa_ambassador_signup CQ event fires on success (best-effort, error swallowed to Sentry)"
  artifacts:
    - path: "apps/web/src/lib/referral/issue.ts"
      provides: "issueReferralOnSignup with ambassador branch"
      contains: "type === 'ambassador'"
    - path: "apps/web/src/lib/referral/issue.test.ts"
      provides: "8-case test coverage for ambassador branch"
      min_lines: 200
    - path: "apps/web/src/lib/referral/fraud-checks.ts"
      provides: "checkFraudSignals with nullable referrerId support"
      contains: "referrerId: string | null"
  key_links:
    - from: "apps/web/src/lib/referral/issue.ts"
      to: "@mpstats/api resolveReferralCode"
      via: "import + first call inside try block (replaces direct prisma.userProfile.findUnique)"
      pattern: "resolveReferralCode\\(args\\.refCode\\)"
    - from: "apps/web/src/lib/referral/issue.ts"
      to: "prisma.referralCode.update increment"
      via: "$transaction race-safe increment with post-update re-check vs maxUses"
      pattern: "currentUses.*increment"
---

<objective>
Extend `issueReferralOnSignup` orchestrator with AMBASSADOR branch: resolve code → enforce D-03 5-min-window → enforce per-code limits (D-04) → race-safe transaction (create Referral with codeId + create TRIAL Subscription with refereeTrialDays + increment currentUses with rollback-on-overflow) → fire CQ events (D-09). Refactor `checkFraudSignals` to null-safe `referrerId` so ambassador branch (no referrer) reuses it without forking the function.

Purpose: This is the runtime engine of Phase 60. Without it, codes exist in DB but signups do nothing.
Output: Same call-sites (`/auth/confirm`, Yandex callback) now handle AMBASSADOR codes transparently. 53A regression preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/60-ambassador-codes/60-CONTEXT.md
@.planning/phases/60-ambassador-codes/60-SPEC.md
@apps/web/src/lib/referral/issue.ts
@apps/web/src/lib/referral/fraud-checks.ts
@packages/api/src/services/referral/activation.ts
@.planning/phases/60-ambassador-codes/60-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 60-01 output -->

From @mpstats/api (added in 60-01):
- `resolveReferralCode(code: string): Promise<ResolvedReferralCode | null>` — runs full validation (active + not expired + currentUses<maxUses).
- `resolveReferralCodeRaw(code: string): Promise<ReferralCode | null>` — raw row without validation, used for Sentry reason logging.
- `ResolvedReferralCode = { type: 'ambassador'; code: ReferralCode } | { type: 'user'; userProfile: { id: string; name: string | null } }`.
- `createTrialSubscription({ userId, durationDays, prismaClient: tx })` — existing, reused for ambassador branch with `durationDays = code.refereeTrialDays`.

From apps/web/src/lib/referral/issue.ts (current 53A implementation — lines 38-158):
- `IssueArgs { refCode: string; friendUserId: string }`
- Outer try/catch with Sentry capture
- Resolves referrer → fraud check → mode flag → transaction → CQ events
- Returns `Promise<void>` (fire-and-forget from call sites)

From apps/web/src/lib/referral/fraud-checks.ts:
- `CheckArgs { referrerId: string; friendId: string }` — needs to become `referrerId: string | null`
- Current self-ref check short-circuits on `args.referrerId === args.friendId`. For ambassador (null referrerId), skip self-ref entirely.
- Current cap-per-week check queries `prisma.referral.count({ where: { referrerUserId: args.referrerId, ... } })`. For null referrerId, this query is meaningless — skip and return `{ verdict: 'OK' }` short-circuit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor checkFraudSignals to null-safe referrerId</name>
  <files>
    apps/web/src/lib/referral/fraud-checks.ts,
    apps/web/src/lib/referral/fraud-checks.test.ts
  </files>
  <read_first>
    - apps/web/src/lib/referral/fraud-checks.ts (full file — 60 lines)
    - apps/web/src/lib/referral/fraud-checks.test.ts if exists (preserve existing 53A test behavior)
  </read_first>
  <behavior>
    - Test 1 (existing 53A): `referrerId === friendId` → `BLOCKED_SELF_REF` (unchanged)
    - Test 2 (existing 53A): email match → `BLOCKED_SELF_REF` (unchanged)
    - Test 3 (existing 53A): 5 recent referrals → `PENDING_REVIEW` (unchanged)
    - Test 4 (existing 53A): clean referrer → `OK` (unchanged)
    - Test 5 (NEW): `referrerId: null, friendId: 'u1'` → `OK` without any prisma/supabase calls (ambassador case — skip self-ref + skip cap check, verify mock prisma.referral.count NOT called, supabase.auth.admin.getUserById NOT called)
  </behavior>
  <action>
1. Change `CheckArgs.referrerId` to `string | null`.
2. At top of `checkFraudSignals`, add early branch: `if (args.referrerId === null) return { verdict: 'OK' };` — short-circuits before any external call.
3. Keep all existing logic intact for the non-null path.
4. Update test file: add new test case 5 from `<behavior>`. Existing tests must still pass.
  </action>
  <verify>
    <automated>pnpm --filter web test -- fraud-checks</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- fraud-checks` exits 0 with 5/5 tests passing
    - `grep -c "referrerId: string | null" apps/web/src/lib/referral/fraud-checks.ts` == 1
    - `grep -c "args.referrerId === null" apps/web/src/lib/referral/fraud-checks.ts` == 1
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>fraud-checks accepts nullable referrerId. Ambassador branch short-circuits to OK. All 5 tests pass. 53A behavior preserved (Tests 1-4 unchanged).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend issueReferralOnSignup with ambassador branch — race-safe transaction</name>
  <files>
    apps/web/src/lib/referral/issue.ts,
    apps/web/src/lib/referral/issue.test.ts
  </files>
  <read_first>
    - apps/web/src/lib/referral/issue.ts (full file)
    - apps/web/src/lib/referral/issue.test.ts if exists (preserve 53A test coverage)
    - .planning/phases/60-ambassador-codes/60-CONTEXT.md (D-01 through D-09)
    - packages/api/src/services/referral/activation.ts (lines 22-60 transaction pattern)
  </read_first>
  <behavior>
    - Happy path: resolveReferralCode returns `{type:'ambassador', code}`, friend is fresh (createdAt within 5 min) → creates Referral(codeType=AMBASSADOR, referrerUserId=null, codeId=code.id, status=CONVERTED), createTrialSubscription(durationDays=code.refereeTrialDays), referralCode.update increment currentUses. CQ events fire (pa_ambassador_signup + setUserProps with pa_referral_source=code.label).
    - D-03 stale user: friend createdAt > 5 min ago → early return, no DB writes, Sentry info log `referral.ambassador.stale_user`.
    - Limit reached at resolve time: resolveReferralCode returns null but resolveReferralCodeRaw returns row → Sentry log with reason (expired / max / disabled), no Subscription, no Referral.
    - Race overflow: maxUses=1, currentUses=0 at validate; inside transaction another tx already incremented to 1. After our increment, row reads currentUses=2 which exceeds maxUses=1 → throw inside transaction → rollback. No Subscription, no Referral persisted. Sentry warning.
    - Fraud PENDING_REVIEW: checkFraudSignals returns PENDING_REVIEW (cap-per-week not applicable for null referrer, but same-IP/device extension future-proof) → Referral.status=PENDING_REVIEW, NO Subscription, NO currentUses increment.
    - CQ failure tolerant: CQ throws → caught inside inner try/catch, Sentry log, transaction still committed.
    - User branch unchanged: resolveReferralCode returns `{type:'user', userProfile}` → executes existing 53A logic byte-identical (mocked).
    - Unknown code: resolveReferralCode returns null AND resolveReferralCodeRaw returns null → Sentry info `referral.unknown_code` (existing 53A behavior).
  </behavior>
  <action>
Rewrite `issueReferralOnSignup` body (preserve signature `IssueArgs → Promise<void>`):

1. **Resolve via new utility.** Replace existing `prisma.userProfile.findUnique({ where: { referralCode: args.refCode } })` (lines 46-49) with `const resolved = await resolveReferralCode(args.refCode);` (import from `@mpstats/api`).

2. **Branch on resolved type:**
   - `if (resolved === null)`: fetch `const raw = await resolveReferralCodeRaw(args.refCode);` — if `raw` exists, Sentry log `referral.ambassador.limit_hit` with `extra: { code: args.refCode, isActive: raw.isActive, expiresAt: raw.expiresAt, currentUses: raw.currentUses, maxUses: raw.maxUses }`; else `referral.unknown_code` (existing log). Return.
   - `if (resolved.type === 'user')`: execute existing 53A logic exactly (mode flag, fraud check, transaction, CQ). Use `resolved.userProfile` instead of old `referrer` variable. **No behavioral changes to this branch.**
   - `if (resolved.type === 'ambassador')`: execute new branch below.

3. **Ambassador branch:**
   a. Fetch `friend = await prisma.userProfile.findUnique({ where: { id: args.friendUserId }, select: { createdAt: true, name: true } })`.
   b. D-03 5-min window check: `if (!friend || (Date.now() - friend.createdAt.getTime()) > 5 * 60 * 1000)` → Sentry info `referral.ambassador.stale_user` with `extra: { code: args.refCode, friendUserId, friendCreatedAt: friend?.createdAt }` → return.
   c. Call `const fraud = await checkFraudSignals({ referrerId: null, friendId: args.friendUserId })`.
   d. Decide status: `const referralStatus = fraud.verdict === 'PENDING_REVIEW' ? 'PENDING_REVIEW' : 'CONVERTED';`. (`BLOCKED_SELF_REF` impossible for null referrer.)
   e. Wrap in `prisma.$transaction(async (tx) => { ... })`:
      - `tx.referral.create({ data: { code: args.refCode, codeType: 'AMBASSADOR', referrerUserId: null, referredUserId: args.friendUserId, codeId: resolved.code.id, status: referralStatus, conversionTrigger: referralStatus === 'CONVERTED' ? 'registration' : null, convertedAt: referralStatus === 'CONVERTED' ? new Date() : null } })`.
      - **No** ReferralBonusPackage (D-02).
      - If `referralStatus === 'CONVERTED'`: `await createTrialSubscription({ userId: args.friendUserId, durationDays: resolved.code.refereeTrialDays, prismaClient: tx })`.
      - If `referralStatus === 'CONVERTED'`: increment + race-check. `const updated = await tx.referralCode.update({ where: { id: resolved.code.id }, data: { currentUses: { increment: 1 } } });` then `if (updated.maxUses !== null && updated.currentUses > updated.maxUses) throw new Error('AMBASSADOR_RACE_OVERFLOW');` — throws rollback the entire transaction (D-04 race protection).
   f. After transaction success: CQ inner try/catch identical pattern to 53A lines 124-151. Calls:
      - `cq.setUserProps(args.friendUserId, { pa_referral_source: resolved.code.label, pa_referral_trial_days: resolved.code.refereeTrialDays, pa_referral_trial_until: formatDateRu(<NOW + refereeTrialDays days>), pa_referral_trial_until_tech: <ISO> })` — only when CONVERTED.
      - `cq.trackEvent(args.friendUserId, 'pa_ambassador_signup')` — only when CONVERTED.
      - On CQ throw → `Sentry.captureException(err, { tags: { area: 'referral', stage: 'cq' } });` (matches 53A pattern).
   g. Catch outer: `if (err.message === 'AMBASSADOR_RACE_OVERFLOW')` → `Sentry.captureMessage('referral.ambassador.race_overflow', { level: 'warning', extra: { code: args.refCode, codeId: resolved.code.id, friendUserId: args.friendUserId } })`; let other errors propagate to existing outer catch at line 152.

4. **Tests in `issue.test.ts`** — vitest with mocks for `@mpstats/api` (`resolveReferralCode`, `resolveReferralCodeRaw`, `createTrialSubscription`, `isFeatureEnabled`), `@mpstats/db/client` (prisma), `@/lib/carrotquest/client` (cq), `@sentry/nextjs`. Eight test cases mirroring `<behavior>` list above. Each test: arrange mocks, call `issueReferralOnSignup({ refCode, friendUserId })`, assert mock calls (prisma.referral.create with codeId, createTrialSubscription durationDays, referralCode.update increment, cq.trackEvent, Sentry.captureMessage). For race-overflow: mock `tx.referralCode.update` to return `{ currentUses: 2, maxUses: 1 }`, assert NO `tx.referral.create` finalized (rollback proven by checking transaction's resolved state via mock).
  </action>
  <verify>
    <automated>pnpm --filter web test -- issue.test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test -- issue.test` exits 0 with at least 8 ambassador-branch cases passing + all existing 53A user-branch cases still passing
    - `grep -c "resolveReferralCode" apps/web/src/lib/referral/issue.ts` ≥ 1
    - `grep -c "codeType: 'AMBASSADOR'" apps/web/src/lib/referral/issue.ts` == 1
    - `grep -c "AMBASSADOR_RACE_OVERFLOW" apps/web/src/lib/referral/issue.ts` ≥ 2 (throw + catch)
    - `grep -c "pa_ambassador_signup" apps/web/src/lib/referral/issue.ts` == 1
    - `grep -c "5 \\* 60 \\* 1000\\|5\\*60\\*1000\\|STALE_USER_WINDOW_MS\\|FIVE_MIN" apps/web/src/lib/referral/issue.ts` ≥ 1 (D-03 5-min window encoded)
    - `grep -c "referralBonusPackage.create" apps/web/src/lib/referral/issue.ts` == 1 (only the 53A user branch, NOT duplicated in ambassador branch)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>issueReferralOnSignup handles ambassador codes with race-safe transaction, D-03 stale-user skip, per-code limit enforcement, CQ event firing, and Sentry instrumentation. 8/8 ambassador tests + all preserved 53A tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web test` exits 0 (includes both fraud-checks.test + issue.test)
- `pnpm typecheck` clean across packages
- `pnpm lint` clean
- Search confirms no duplicate logic: `grep -c "BonusPackage" apps/web/src/lib/referral/issue.ts` == 1 (user branch only)
- 53A regression preserved: `pnpm --filter web test -- referral` shows no failures in pre-existing 53A specs
</verification>

<success_criteria>
- Ambassador signup creates: Referral (codeType=AMBASSADOR, codeId set, referrerUserId=null), TRIAL Subscription (durationDays=code.refereeTrialDays), ReferralCode.currentUses incremented atomically.
- Stale user (>5 min old) → no DB writes, Sentry info log.
- Race overflow → transaction rolls back cleanly, Sentry warning.
- 53A path byte-for-byte identical behavior.
- CQ event `pa_ambassador_signup` with `pa_referral_source` user prop fires on success.
</success_criteria>

<output>
Create `.planning/phases/60-ambassador-codes/60-02-SUMMARY.md` with test counts, key code-path diffs, any deferred items.
</output>
