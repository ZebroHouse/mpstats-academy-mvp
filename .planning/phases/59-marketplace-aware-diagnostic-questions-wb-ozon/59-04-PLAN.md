---
phase: 59-marketplace-aware-diagnostic-questions-wb-ozon
plan: 04
type: execute
wave: 3
depends_on: [59-03]
files_modified:
  - packages/api/src/routers/diagnostic.ts
  - packages/api/src/routers/__tests__/diagnostic.test.ts
  - apps/web/src/components/diagnostic/Question.tsx
  - apps/web/src/components/diagnostic/__tests__/Question.test.tsx
autonomous: true
requirements: []
tags: [phase-59, diagnostic, wiring, runtime]

## Objective

Заменить в `diagnostic.startSession` вызов `getQuestionsFromBank` (LLM-bank + filter path) на static-deck path из Plan 59-03. Сессия теперь — детерминированная: 15 вопросов из готовых колод, опции перетасованы seedable RNG, исходный «правильный = A» уезжает на новую позицию.

Scoring (`submitAnswer`, `getResults`) **не трогаем** — он читает `correctIndex` из persisted session.questions, что уже будет post-shuffle. Минимальные правки UI: бейдж `marketplace` показывать всегда для BOTH-юзера (а не только mix-user-флагом из 59-02).

## must_haves

  truths:
    - "diagnostic.startSession assembles questions via pickDeckForUser(profile.marketplaces, session.id) + shuffleOptions(q, session.id) for each picked question, BEFORE persisting session.questions"
    - "The persisted session.questions array stores shuffled options and the new correctIndex — submitAnswer logic remains untouched and still passes"
    - "getQuestionsFromBank is no longer called from startSession; LLM-bank refresh logic stays dead-code in question-bank.ts (kept for potential future use, marked dormant via JSDoc)"
    - "For users with userProfile.marketplaces = ['WB'] only WB questions appear in session; for ['OZON'] only Ozon; for ['WB','OZON'] (or empty) 15 mixed questions with balanced 7-8 split"
    - "Same user starting two diagnostics gets DIFFERENT shuffles (sessionId differs); a single session's question order is stable across page reloads (same sessionId)"
    - "Question.tsx shows marketplace badge whenever userMarketplaces is BOTH (= has 2 entries); never shows for single-MP users; badge text matches marketplace tag ('Про Wildberries' or 'Про Ozon')"
    - "All existing diagnostic.test.ts tests still pass after rewiring (regression-safe); the Phase 58 recommendedJobs path in getResults stays unchanged"
    - "CarrotQuest pa_diagnostic_completed event still fires with pa_diagnostic_marketplaces (from profile) and pa_diagnostic_pool_size = 15 (constant now)"

  artifacts:
    - path: "packages/api/src/routers/diagnostic.ts"
      provides: "startSession using static deck + option shuffle"
      contains: "pickDeckForUser"
    - path: "apps/web/src/components/diagnostic/Question.tsx"
      provides: "Badge logic based on user-is-BOTH"
      contains: "isBothMarketplaces"

  key_links:
    - from: "packages/api/src/routers/diagnostic.ts"
      to: "packages/api/src/diagnostic/deck-picker.ts"
      via: "import { pickDeckForUser } from '../diagnostic/deck-picker'"
      pattern: "pickDeckForUser("
    - from: "packages/api/src/routers/diagnostic.ts"
      to: "packages/api/src/diagnostic/option-shuffler.ts"
      via: "import { shuffleOptions } from '../diagnostic/option-shuffler'"
      pattern: "shuffleOptions("

## Tasks

### Task 1 — Rewire startSession (TDD)

1. Extend `packages/api/src/routers/__tests__/diagnostic.test.ts`:
   - New tests for startSession with mocked UserProfile.marketplaces:
     - WB-only profile → all returned questions have `marketplace === 'WB'`, length 15
     - Ozon-only profile → all `marketplace === 'OZON'`, length 15
     - Empty profile → 15 questions, mixed marketplaces with 7-8 split
   - Determinism test: two startSession calls with mocked Prisma assigning two distinct sessionIds → produce different question orderings
   - Stability test: persisted session.questions[i].correctIndex points to an option that, when present in options array, equals the canonical methodology «правильный» (i.e. roundtrip works after shuffle)
   - CarrotQuest test: `cqTrackEvent` was called with name `pa_diagnostic_completed` (already covered by Plan 59-02 tests — keep them green)
   - Commit: `test(59-04): add failing tests for static-deck startSession`
2. Patch `packages/api/src/routers/diagnostic.ts startSession`:
   - Replace `questions = await getQuestionsFromBank(...)` block with:
     ```ts
     const picked = pickDeckForUser(profile.marketplaces ?? [], session.id);
     questions = picked.map((q) => {
       const { options, correctIndex } = shuffleOptions(q, session.id);
       return {
         id: q.id,
         prompt: q.prompt,
         options,
         correctIndex,
         skillCategory: q.axis,
         difficulty: q.level,  // 1|2|3
         explanation: q.explanation,
         marketplace: q.marketplace,
         // sourceData not applicable for static deck
       } as DiagnosticQuestion;
     });
     ```
   - Persist as before via prisma session.update with `questions: questions as any`
   - Keep CQ event fire (`cqTrackEvent`, `cqSetUserProps`) untouched but switch `pa_diagnostic_pool_size` to `questions.length` (still = 15)
   - Commit: `feat(59-04): wire startSession to static deck + seeded shuffle`

### Task 2 — UI badge logic for BOTH user

1. Extend `apps/web/src/components/diagnostic/__tests__/Question.test.tsx`:
   - Test: when `userMarketplaces.length === 2` (BOTH) → badge renders for both WB and Ozon questions
   - Test: when `userMarketplaces = ['WB']` → no badge rendered (single MP)
   - Test: when `userMarketplaces = ['OZON']` → no badge rendered
   - Test: badge text matches marketplace tag ('Про Wildberries' / 'Про Ozon')
   - Commit: `test(59-04): add failing tests for BOTH-user badge logic`
2. Patch `Question.tsx`:
   - Compute `const isBoth = userMarketplaces.length === 2`
   - Badge condition becomes: `isBoth && marketplace !== 'BOTH'` (in static deck, marketplace is always 'WB' or 'OZON', so this == `isBoth`). Keep the `!== 'BOTH'` guard as defense for future cross-MP questions.
   - Commit: `feat(59-04): show marketplace badge only for BOTH users`

### Task 3 — Mark LLM-bank dormant + cleanup

1. Add JSDoc to `getQuestionsFromBank`, `refreshBankForCategory` in `packages/api/src/utils/question-bank.ts`:
   ```
   /**
    * @deprecated Phase 59 v2 (2026-06-01): runtime questions now come from
    * static-deck (packages/api/src/diagnostic/static-deck.ts). This LLM bank
    * path is kept as inactive code for potential future admin-driven generation.
    * Do NOT call from diagnostic.startSession.
    */
   ```
2. Remove import of `getQuestionsFromBank` from `diagnostic.ts` if unused after Task 1
3. Run full test sweep:
   - `pnpm --filter @mpstats/api test`
   - `pnpm --filter web test`
   - `pnpm typecheck` (all 6 packages)
4. Commit: `chore(59-04): mark LLM question-bank path as dormant`

### Task 4 — Verification + SUMMARY

1. Confirm: 0 typecheck errors, 0 test failures across `@mpstats/api` + `web`
2. Write `59-04-SUMMARY.md`: acceptance evidence (test pass counts, sample shuffled question demonstrating same input → different sessionIds → different correctIndex, BOTH-user split count from a sample seed)
3. Commit SUMMARY first, then return

## checkpoints

(none — fully autonomous)

## risks

- **Existing diagnostic.test.ts breakage**: Plan 59-02 added tests that mock `getQuestionsFromBank`. After rewiring those mocks become irrelevant. Mitigation: update test setup to mock `pickDeckForUser` instead; keep CQ event tests intact since they're independent.
- **`difficulty` semantics**: existing `DiagnosticQuestion.difficulty` is `'easy'|'medium'|'hard'` string in some places. Plan 59-03 stores level as `1|2|3`. Mitigation: map level → difficulty string in the rewiring: `1→'easy', 2→'medium', 3→'hard'`. Document the mapping inline.
- **BOTH-user determinism in tests**: stable `sessionId` produces stable order. Use a fixed test sessionId for golden test, separate from production randomness.
- **CarrotQuest pool_size dashboard**: changed from variable to constant 15. Dashboard may need re-tune — flag in SUMMARY as «non-blocking dashboard FYI for CQ team».
