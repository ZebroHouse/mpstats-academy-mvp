# Phase 59 — Handoff Note (2026-05-28)

**Purpose:** Brief you on what happened to `master` while you were working on `phase-59-marketplace-aware-diagnostic`. Read this before resuming.

## TL;DR

`master` ушёл вперёд на **2 merged PR** пока ты работал. Оба уже на проде, БД мигрирована. **Rebase phase-59 на новый master нужен**, но конфликтов не ожидается — пересечений по файлам нет.

## What landed on master

### PR #12 — Phase 58 (diagnostic on jobs) — merged 2026-05-28 10:44 UTC, deployed to prod

Ровно та фаза, на которую ты строишь Phase 59. Теперь на проде:
- Диагностика рекомендует top-3 **джобы** (не россыпь уроков). Bulk CTA «Добавить все 3 в трек».
- Wizard step 2 онбординга: 7 опций маркетплейсов → 2 (WB + Ozon, multi-select).
- One-time SQL backfill `UserProfile.marketplaces` → `{WB, OZON}`-only (~200 юзеров, уже выполнен).
- Legacy `LearningPath` auto-rebuild (~170 юзеров): flat-format → sectioned, `LessonProgress` НЕ трогаем (D-07 hard rule).
- Marketplace-фильтр на джобах: `Job.marketplace ∈ user.marketplaces ∪ {BOTH}`.

**Hard implication для Phase 59:** твоя фаза предполагает, что `UserProfile.marketplaces ⊆ {WB, OZON}` (после backfill 58-02). Это теперь правда. `Job.marketplace`-фильтр джоб (D-15/D-16 Phase 58) уже работает — Phase 59 улучшает только ВХОД (вопросы), не ВЫХОД (matching остаётся как D-01/D-02 Phase 58).

**Файлы Phase 58, которые могут пересечься с твоей работой:**
- `packages/api/src/routers/diagnostic.ts` — Phase 58 добавила `recommendedJobs[]` в `getResults`. Если ты модифицируешь тот же роутер для marketplace-aware-questions — будут merge-конфликты на этом файле. Reconciliation: твои изменения скорее всего в `getQuestions` / `generateQuestionBank` / scoring, а Phase 58 трогала только `getResults` + добавила импорт `getRecommendedJobsFromGaps`. Конфликтов на симантике быть не должно — три-way merge должен пройти чисто.
- `apps/web/src/components/welcome/options.ts` + `StepMarketplaces.tsx` — Phase 58 D-12 схлопнула опции. Если ты их тоже трогал в onboarding-tagging — pull первым, потом adjust.
- `packages/api/src/utils/job-matcher.ts` (новый файл Phase 58) — здесь живёт `getRecommendedJobsFromGaps` + `computeEffectiveMarketplaces`. Можешь переиспользовать `computeEffectiveMarketplaces` для своей логики «WB-only юзер → WB-only вопросы» (он экспортирован).

### PR #13 — Phase 60 (Ambassador Referral Codes) — merged 2026-05-28 16:47 UTC, deployed to prod

**Ортогональна Phase 59.** Расширение реферальной программы — админ-управляемые коды для блогеров (AMBASSADOR). Trial-длительность задаётся админом (7/14/30 дней), стат по конверсиям. Никаких пересечений с диагностикой и вопросами:
- Новая модель `ReferralCode` + nullable FK `Referral.codeId` + enum value `AMBASSADOR`.
- Расширение `apps/web/src/lib/referral/issue.ts` (orchestrator) — touched, но для AMBASSADOR-ветки.
- Новый admin UI: `/admin/referrals/codes`.
- Файлы, которые Phase 60 трогал: `packages/db/prisma/schema.prisma`, `packages/api/src/services/referral/*`, `packages/api/src/routers/referral.ts`, `apps/web/src/lib/referral/*`, `apps/web/src/components/admin/AmbassadorCode*`, `apps/web/src/app/(admin)/admin/referrals/codes/page.tsx`, `apps/web/src/components/admin/AdminSidebar.tsx` (одна строка добавлена).
- Конфликт с Phase 59 ожидается только в `schema.prisma` (если ты тоже его меняешь) и `AdminSidebar.tsx` (если ты добавлял туда новый пункт меню).

## Database state

**Уже применены к prod БД (миграции, которые ты НЕ видел на момент твоего начала работы):**
- `20260521000000_learning_path_added_jobs` (Phase 57 polish, тыс. назад)
- `20260522000000_add_job_embedding` (Track B, тыс. назад)
- `20260526000000_add_user_profile_qualification` (Phase 58 — marketplaces[] + experienceLevel + goals + goalText)
- `20260528000000_add_referral_code_table` (Phase 60 — ReferralCode + Referral.codeId + AMBASSADOR enum)

`packages/db/prisma/schema.prisma` соответствует prod БД. Pull master + `prisma generate` — клиент пересоберётся под актуальный shape.

## Recommended rebase procedure

```bash
# In your phase-59 worktree (or wherever you are):
git status                              # working tree clean
git fetch origin master
git rebase origin/master                # straightforward — no expected conflicts beyond surface text

# If conflicts (unlikely but possible on diagnostic.ts, schema.prisma, AdminSidebar.tsx):
#   - diagnostic.ts: keep both — Phase 58's recommendedJobs + your marketplace filter on questions
#   - schema.prisma: integrate both Phase 60 ReferralCode model AND your Phase 59 additions
#   - AdminSidebar.tsx: add your menu entry alongside the new ambassador-codes entry

# Re-run prisma generate after rebase to refresh client with merged schema:
pnpm --filter @mpstats/db exec prisma generate

# Re-run tests to catch any cross-phase breakage:
pnpm --filter @mpstats/api test
pnpm --filter web test
```

## Owner state (2026-05-28 evening)

- Owner смотрел админ-панель Phase 60, создал тестовый AMBASSADOR код, отдал маркетингу на UAT. Если маркетинг подтвердит выдачу 7-дневного доступа по ссылке — Phase 60 закрывается.
- Ждёт твоего возврата к работе над Phase 59 после того как Phase 60 пройдёт UAT.

## Files you may want to read for context

- `.planning/phases/58-diagnostic-on-jobs/58-CONTEXT.md` — full Phase 58 decisions (D-01..D-17)
- `.planning/phases/58-diagnostic-on-jobs/58-03-SUMMARY.md`, `58-04-SUMMARY.md` — what was actually shipped
- `.planning/phases/60-ambassador-codes/60-CONTEXT.md` — Phase 60 decisions (для понимания что трогалось)
- `.planning/phases/60-ambassador-codes/60-02-SUMMARY.md` — конкретно изменения в `apps/web/src/lib/referral/issue.ts` (если ты вдруг его тоже трогал)

## Don't do

- НЕ делай `git rebase --skip` молча на чужих коммитах. Если конфликт непонятный — surface owner, не auto-resolve.
- НЕ применяй `prisma db push --accept-data-loss` (см. CLAUDE.md PROD DATABASE SAFETY incident 2026-05-12).
- НЕ удаляй файлы, которые ты не создавал — это работа Phase 58 / 60.

---

**Если что-то непонятно** — оставь вопрос в `.planning/phases/59-marketplace-aware-diagnostic-questions-wb-ozon/QUESTIONS-FOR-OWNER.md`. Owner прочитает.
